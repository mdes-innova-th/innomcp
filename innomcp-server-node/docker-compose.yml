version: '3.8'

services:
  innomcp-server-node:
    container_name: innomcp-server-node
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    expose:
      - "3010"
    env_file:
      - .env
    environment:
      - SERVER_HOST=innomcp-server-node
      - SERVER_PORT=3010
    networks:
      - innomcp_network

networks:
  innomcp_network:
    name: innomcp_network
    external: true