# Proposed Changes to THAI_KNOWLEDGE_SCHEMA.json

## Add `HISTORY` support

Update `domain` enum:
`"domain": "geo|law|history|religion|education"` (Already present, verified)

Add specific `attributes` validation for History (optional but recommended for Zod schema).
