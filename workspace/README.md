# INNOMCP Workspace

This directory stores files created by AI agents (Private Agent Studio).

- `projects/` — user project files
- `artifacts/` — auto-saved task outputs from conductor

Files are sandboxed: the backend enforces path traversal protection.
