<!-- cc-team deliverable
 group: G1 (sideapi division)
 member: SIDE-005 role=audit model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":1704,"completion_tokens":2708,"total_tokens":4412,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2148,"image_tokens":0},"cache_creation_input_tokens":0} | 36s
 generated: 2026-06-13T12:08:06.138Z -->
| severity | location | issue | fix |
|----------|----------|-------|-----|
| High | Entire module – all routes | **Missing authentication/authorization.** No middleware enforces identity or access control before exposing court-order and evidence queries. Sensitive data can be enumerated by any anonymous caller. | Add an authentication guard (JWT, API key, or session) to every route, and optionally restrict access to specific roles. |
| High | `/by-caselist/:caseId`, lines 52‑59 | **Unbounded pagination offset leading to denial of service.** The `page` query parameter is only forced to be ≥1 but has no maximum. An attacker can request `?page=999999999&pageSize=200`, producing an enormous `OFFSET` that will severely degrade database performance or lock up the server. | Cap the maximum page number (e.g., `page = Math.min(Math.max(1, ...), 1000)`) or switch to cursor/keyset pagination that does not rely on offset. |
| Medium | All catch blocks (lines 40, 70, 99) | **Information leakage via error messages.** The catch handler returns `e.message` to the client, exposing internal details (database errors, column names, stack traces) that can aid an attacker. | Log the full error on the server and return a generic message like `"Internal server error"` to the client. |
| Medium | All routes | **No rate limiting.** Endpoints are unprotected against brute-force or high‑volume requests, which could be used to scrape data or cause resource exhaustion. | Apply rate limiting middleware (e.g., `express-rate-limit`) with sensible windows per client IP or user. |
| Low | `/has-court-order` (line 23) and `/has-evidence` (line 82) | **Missing input length validation on `url` parameter.** The code does not limit the length of the incoming URL string, which may be passed directly to the database and cause unexpected behaviour or resource consumption. | Add a maximum length check (e.g., 2048 characters) and reject longer values with a 400 response. |
| Low | `/by-caselist/:caseId`, lines 44‑45 (detect bridge branch) | **Unvalidated `caseId` parameter length in detect‑bridge mode.** In the `isDetectBridge()` branch the `caseId` (court_order) string is used without a length limit, potentially hurting database queries if the value is excessively long. | Validate that `caseId` does not exceed a reasonable maximum (e.g., 255 characters) before executing the query. |
