<!-- cc-team deliverable
 group: G1 (sideapi division)
 member: SIDE-010 role=audit model=MiniMaxAI/MiniMax-M3
 finish_reason: stop | tokens: {"prompt_tokens":2735,"completion_tokens":2163,"total_tokens":4898,"prompt_tokens_details":{"cached_tokens":114,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":0,"image_tokens":0},"cache_creation_input_tokens":0} | 48s
 generated: 2026-06-13T12:09:18.556Z -->
# Bug + Security Audit: `detect-evidence-api/src/routes/nip.ts`

| # | Severity | Location | Issue | Fix |
|---|----------|----------|-------|-----|
| 1 | **Critical** | All routes (module-level) | **No authentication / authorization** — endpoints are fully public, but the table is sensitive (court-ordered URL takedowns, 640K+ rows of illegal content evidence). Anyone on the network can enumerate ISP detection volumes and likely infer court order timing/identifiers via `caselist_id`. | Add middleware (e.g. JWT/API key/session) before mounting the router, plus role check. Gate `/nip/*` behind an authn decorator and reject unauthenticated requests with `401`. |
| 2 | **High** | `bkkDate` / `bkkMonday` (lines ~17–27) | **Broken Bangkok timezone math** — uses fixed `+ 7 * 3600_000` offset, which is wrong when Bangkok is UTC+7 *and* the server is already on BKK time, and is **incorrect half the year** because it ignores DST on servers that observe it, and ignores IANA tz rules. Result: date filters can return counts from the wrong day/week. | Use `Intl.DateTimeFormat('en-CA', { timeZone: 'Asia/Bangkok' })` or `toLocaleString('en-CA', { timeZone: 'Asia/Bangkok' })` for the Y-M-D string. For Monday-of-week compute the UTC components and apply the Asia/Bangkok calendar via `Intl`. |
| 3 | **High** | `/stats/isp/month` (`month` param) | **`month` not validated** — `Number("")` is `0` (handled), but `Number("2026-13")` → `13` is accepted, `Number("abc-12")` → `NaN`, and `Number("9999-99")` produces a 4-digit year and month=99, generating a full-table scan that returns `0` rows. Worse: `Number("2025-0x1")` parses as `1` (hex). | Validate strictly with regex `/^\d{4}-(0[1-9]\|1[0-2])$/`, then `parseInt` the two halves and reject if out of range. Reject `NaN` with `400`. |
| 4 | **High** | `/stats/isp/month`, `/stats/top-isp/month` | **No index hint / unbounded range** — `YEAR(create_date)=? AND MONTH(create_date)=?` is **sargability-breaking** (function on column). On a 640K+ row table this can become a full scan per request, and the `LIMIT 20` does nothing to reduce work. | Add a composite index on `nip (create_date, isp_name)` and rewrite as `create_date >= ? AND create_date < ?` (half-open range) with the literals bound from the parsed month. |
| 5 | **High** | All routes using `await query(...)` directly | **No DB error handling** — if `query` throws (connection drop, lock timeout, syntax error from a future change), Express returns the default HTML stack trace with `500`, leaking internals and never logging structured context. | Wrap each handler in `try/catch`, call `next(err)` (or respond `500 { ok: false }`), and log via a central logger with `req.id`. Ensure `query` is a known MySQL/promise wrapper — not the legacy `mysql` callback. |
| 6 | **High** | `/distinct/month` (truncated) | **Comma-separated `isp` list is split but not shown to be validated** — if the implementation does `ispParam.split(",")` and pushes each into the `IN (...)` clause, it must use placeholders (`?,?,?`). If it builds the SQL string with interpolation it is **SQL injection**. (Truncated code makes this unverifiable, so flag as High pending review.) | Confirm it uses `params.push(...canonList)` with one `?` per element. Reject any element that does not pass `canonicalIsp`. |
| 7 | **Medium** | All `isp` query params | **Echo of unsanitized `isp` in response** — `ispFilter: isp` returns the raw user string. If the string contains HTML/script, and any consumer renders it, it's stored XSS on the client side. Not exploitable in pure JSON, but is poor practice and pollutes logs. | Sanitize to `String(isp).slice(0, 64)` and strip control chars before echoing. |
| 8 | **Medium** | `/stats/top-isp/all-time`, `/stats/top-isp/month` | **GROUP BY scan with no date cap on "all-time"** — counts every row in the 640K+ table on each call. No caching, no rate limit. Trivially abusable as DoS amplification. | Add a result cache (e.g. 60s TTL) and a per-IP rate limiter (e.g. `express-rate-limit` 30 req/min) on the whole router. |
| 9 | **Medium** | `bkkDate` IIFE default for `month` | **`new Date()` with no tz uses server tz**, then pairs with `bkkDate`-style logic for the date — inconsistent. Also, default branch is a non-pure IIFE inside `\|\|` making it hard to test. | Extract a `currentBkkMonth()` helper colocated with the other date helpers and unit-test it. |
| 10 | **Medium** | `/stats/isp/delta/today-vs-yesterday` | **`direction: "equal"` when both counts are 0** — semantically ambiguous (no data, or data equals 0?). Clients may misclassify a "service down" state as "stable". | Return a `null` direction (or `unknown`) when `todayCount === 0 && yesterdayCount === 0`, and document. |
| 11 | **Medium** | `LIMIT` handling for `Number(req.query.limit)` | **`NaN` and non-numeric strings silently fall back to 10** — `Number("10; DROP TABLE nip;--")` is `NaN`, fine; but `Number("1e3")` is `1000` then clamped — acceptable. `Number("0x10")` is `16`. Not a vuln, but a request like `?limit=0x10` is surprising. | Use `const n = parseInt(String(req.query.limit), 10);` and reject with `400` if `Number.isNaN(n)`. |
| 12 | **Low** | All routes | **No `Content-Type`/cache headers** — responses are large, recomputed, and uncached; the browser/proxy will not cache, multiplying load. | Set `Cache-Control: public, max-age=60` for stats endpoints and a shorter TTL for delta. |
| 13 | **Low** | All routes | **No request validation schema** — bad input produces silent fallbacks rather than `400`, making integration bugs hard to detect. | Introduce `zod`/`joi` schemas with `safeParse` returning `400` on failure. |
| 14 | **Low** | `ISP_CANONICAL` | **Thai keys contain leading spaces / invisibles are not normalized** — `canonicalIsp` lowercases and trims, which is good, but no Unicode NFC normalization. A user typing a composite combining-mark form (e.g. decomposed `เ` + combining mark) won't match the precomposed form in the map. | Apply `key.normalize('NFC')` before lookup. |
| 15 | **Low** | Module header | **Sensitive column names in JSDoc** — `court_order`, `caselist_id` named in a comment may be auto-published to docs/internal tools. Not a code vuln, but info disclosure. | Move column inventory to a separate internal doc; the route JSDoc only needs the public schema. |
| 16 | **Low** | `/stats/isp/month` | **Non-recognition note leaks full ISP allowlist to anonymous callers** — minor, but combined with #1 it means an unauthenticated probe learns the data model. | Once auth is added, returning the note is fine. Before auth, return generic `400 invalid filter`. |
| 17 | **Informational** | All routes | **No `helmet`/`express-rate-limit`/CORS mention** — needs verification at the app level. | Add `helmet()`, configure CORS allowlist, mount `express-rate-limit` per router. |
| 18 | **Informational** | `query` import | **DB driver identity not visible here** — if it is `mysql2/promise` with `?` placeholders, code is safe. If it is the legacy `mysql` lib with manual escaping, `params` of `any[]` allows accidental type coercion. | Standardize on `mysql2/promise` (or `pg`) with strict typing, and a thin typed `query<T>(sql, params)` wrapper. |

## Top 3 must-fix before deploy

1. **#1 Authentication** — the table holds court-order evidence; current code is unauthenticated.
2. **#2 BKK date math** — counters will be wrong on the day Bangkok differs from server tz.
3. **#4 Sargability + missing index** — `YEAR()/MONTH()` predicates will not scale and will degrade into full scans under any load.

## Verifiable SQL-safety summary
All shown `query(...)` calls use `?` placeholders with parameterized values; the only `String` interpolations in the code (the `LIMIT` numbers and `YEAR/MONTH` literals) are computed from already-validated/coerced `Number(...)` results, so no injection was found in the **visible** code. The truncation at `/distinct/month` (`/* …truncated… */`) prevents a definitive verdict on the comma-list ISP path — re-review once that handler is unhidden.
