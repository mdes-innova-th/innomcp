<!-- cc-team deliverable
 group: G1 (sideapi division)
 member: SIDE-007 role=audit model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":756,"completion_tokens":4812,"total_tokens":5568,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":3853,"image_tokens":0},"cache_creation_input_tokens":0} | 91s
 generated: 2026-06-13T12:10:33.602Z -->
| severity | location | issue | fix |
|---|---|---|---|
| Critical | Global Middleware | **Missing Authentication/Authorization:** No global auth middleware is present. Unless strictly implemented inside every imported route, endpoints (especially `/admin`) are completely open to unauthenticated access. | Implement a global authentication middleware (e.g., JWT, API keys, mTLS) before mounting domain routes. Enforce role-based access control (RBAC) specifically for `/admin`. |
| High | `app.use(cors())` | **Overly Permissive CORS (Injection/CSRF Risk):** Using `cors()` without configuration allows any origin. This enables Cross-Site Request Forgery (CSRF) and unauthorized cross-origin data access if accessed via a browser. | Restrict CORS to trusted origins: `cors({ origin: process.env.ALLOWED_ORIGINS?.split(',') })`. Disable credentials if not strictly needed. |
| High | `/health` route | **Information Disclosure (Health Check):** Returning the raw `db` object (`res.json({ ... db })`) can leak sensitive database connection strings, credentials, internal hostnames, or network topology. | Return only safe, high-level status fields (e.g., `{ status: 'ok', db: 'connected' }`). Never expose raw DB driver objects or internal configuration. |
| High | Error Handler | **Internal Error Message Leakage:** The global error handler returns `err.message` directly to the client. This can expose stack traces, SQL syntax errors, file paths, or internal logic to attackers. | Return a generic message to the client (`{ error: 'Internal Server Error' }`). Log the detailed `err.message` and `err.stack` server-side only. |
| Medium | `express.json()` | **Missing Resource Limits (Body & Rate):** Default JSON limit is 100kb, but there is no explicit rate limiting or strict payload sizing, leaving the API vulnerable to DoS, brute-force, and resource exhaustion. | Explicitly set a smaller body limit if applicable (`express.json({ limit: '10kb' })`) and implement rate limiting via `express-rate-limit`. |
| Medium | Global Setup | **Missing Security Headers:** The API lacks standard HTTP security headers, making it susceptible to clickjacking, MIME sniffing, and other browser-based attacks. | Add the `helmet` middleware (`app.use(helmet())`) to automatically configure secure HTTP headers. |
| Medium | `/health` route | **Hardcoded Fallback Public IP:** `process.env.DETECT_DB_HOST || "209.15.105.27"` hardcodes a public IP. If the env var is missing, it silently connects to an external/production DB, risking data corruption or leakage. | Remove the hardcoded public IP. Fail fast if `DETECT_DB_HOST` is missing in production, or default to `127.0.0.1` for local dev. |
| Medium | Process Listeners | **Incomplete Error Handling (Crash Risk):** While `unhandledRejection` is caught, synchronous `uncaughtException` errors will crash the process immediately without logging or graceful cleanup. | Add `process.on('uncaughtException', ...)` to log the error and trigger a graceful shutdown/restart via your process manager. |
| Low | Global / Routes | **Lack of Global Input Validation:** While SQL injection prevention must be in the routes, there is no global schema validation or sanitization layer to reject malformed requests early at the gateway. | Use a global validation middleware (e.g., `zod`, `joi`, or `express-validator`) and sanitize inputs to prevent injection at the entry point. |
| Low | `app.listen` | **Unsafe Port Parsing:** `Number(process.env.DETECT_API_PORT)` can result in `NaN` if the env var contains non-numeric text, causing `app.listen` to fail unpredictably. | Parse safely and validate: `const PORT = parseInt(process.env.DETECT_API_PORT || '3013', 10); if (isNaN(PORT) || PORT < 1 || PORT > 65535) throw new Error('Invalid port');` |
| Low | Server Lifecycle | **Missing Graceful Shutdown:** The server does not handle `SIGTERM`/`SIGINT` to close active HTTP connections and the database pool, leading to dropped requests and connection leaks during deployments. | Add signal handlers to call `server.close()` and close the DB connection pool before calling `process.exit(0)`. |
