<!-- cc-team deliverable
 group: G1 (sideapi division)
 member: SIDE-008 role=audit model=moonshotai/Kimi-K2.6
 finish_reason: stop | tokens: {"prompt_tokens":1299,"completion_tokens":4644,"total_tokens":5943,"prompt_tokens_details":{"cached_tokens":3,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":4499,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 48s
 generated: 2026-06-13T12:08:54.308Z -->
| severity | location | issue | fix |
|---|---|---|---|
| Critical | All routes (`/contract-map`, `/tables`, `/describe/:table`) | No authentication or authorization on admin/schema-discovery endpoints; anyone can enumerate database structure. | Mount auth middleware (e.g., JWT/API key validation + admin-role guard) on the router before all handlers. |
| High | `CONTRACT_MAP` constant + `GET /contract-map` | Information disclosure: leaks internal table names, approximate row counts (~640K, ~1.98M), key columns, and architectural gaps (unsupported metrics reveal missing tables and external dependencies like “web-d”). | Require strong authentication to access; redact operational details (row counts, raw column names, internal dependency notes) from the public response. |
| High | `GET /tables` | Information disclosure: `SHOW TABLES` returns every table in the database, including system/internal tables, bypassing the `ALLOWED_TABLES` whitelist used elsewhere. | Filter the result set against an explicit curated allowlist before serializing; restrict the DB user to least privilege. |
| High | `GET /tables`, `GET /describe/:table` | Unhandled promise rejections: async route handlers lack `try/catch`. If `query()` throws, the request hangs or crashes the process (DoS/availability impact). | Wrap each handler in `try/catch` and pass errors to `next(e)`, or use an async-handler wrapper; ensure a centralized Express error handler is registered. |
| Medium | `GET /describe/:table` | SQL injection anti-pattern: query identifier is concatenated via template literal `` `SHOW COLUMNS FROM \`${table}\`` ``. The whitelist mitigates this, but manual backtick escaping is fragile and breaks defense-in-depth. | Use the driver’s identifier-escaping function (e.g., `mysql.escapeId`, `pg-format`) for the table name; never use template literals for SQL identifiers. |
| Medium | All routes | No rate limiting or resource throttling on schema-discovery endpoints, enabling automated reconnaissance and potential connection-pool exhaustion. | Apply strict per-IP rate limiting (e.g., `express-rate-limit`) and connection-pool guards specifically to admin routes. |
| Low | `ALLOWED_TABLES` + `GET /describe/:table` | Inconsistent exposure: whitelist includes `entries` and `sip`, which are not documented in `CONTRACT_MAP`, suggesting unintended tables are exposed. | Audit `ALLOWED_TABLES` against the documented contract; remove any unintended entries and keep the list in sync with `/contract-map`. |
| Low | `GET /describe/:table` | Insufficient input validation: `req.params.table` is cast to string but not checked for length or safe format before the whitelist test. | Validate the param against a safe pattern (e.g., `/^[a-z0-9_]+$/i`) and enforce a max length before checking `ALLOWED_TABLES`. |
