<!-- cc-team deliverable
 group: G1 (sideapi division)
 member: SIDE-009 role=audit model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":685,"completion_tokens":1438,"total_tokens":2123,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":966,"image_tokens":0},"cache_creation_input_tokens":0} | 21s
 generated: 2026-06-13T12:08:30.421Z -->
| severity | location                         | issue                                                                                      | fix                                                                                                                              |
|----------|----------------------------------|--------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------|
| Critical | All routes (`/active`, `/offline`, `/status`, `/latest`) | **Missing authentication & authorization** – sensitive machine data (IP, hostnames, online status) is exposed to anyone. | Add an authentication middleware (e.g., JWT or API key) and role checks to restrict access to authorized operators only.        |
| High     | Entire module                    | **No error handling** – database rejections are not caught, causing unhandled promise rejections and no response to the client. | Wrap every route handler with `try/catch` and return appropriate error responses (e.g., 500). Alternatively, use a global error handler. |
| Medium   | `/latest` route, query parameter `limit` | **Unsafe `Number()` coercion** – `Number(req.query.limit)` returns `NaN` for non‑numeric strings, arrays, or multiple values. `Math.max(1, NaN)` → `NaN` propagates to the SQL query, causing a runtime error. | Parse with `parseInt(req.query.limit, 10)` and validate: `if (!Number.isInteger(limit)) limit = 5`. Use a single value extraction (e.g., `String(req.query.limit)` first). |
| Medium   | All routes                       | **No rate limiting** – an attacker can flood the endpoints, exhausting database connections or CPU. | Add rate limiting middleware (e.g., `express-rate-limit`) with per‑IP or per‑user limits appropriate for the API’s usage.         |
| Low      | `/status` route                  | **Multiple sequential queries** – three `COUNT(*)` queries run one after another, unnecessarily delaying the response and increasing load. | Combine into a single query: `SELECT SUM(is_online=1) AS online, SUM(is_online=0) AS offline, COUNT(*) AS total FROM machines`. |
| Low      | `/latest` response               | **Potential information disclosure** – returns internal IP addresses unnecessarily; may aid reconnaissance. | Omit `ip_address` from the response if not needed, or apply field‑level access control.                                          |
