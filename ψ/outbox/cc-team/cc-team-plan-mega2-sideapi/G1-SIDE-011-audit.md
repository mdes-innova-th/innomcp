<!-- cc-team deliverable
 group: G1 (sideapi division)
 member: SIDE-011 role=audit model=deepseek/deepseek-v4-flash
 finish_reason: stop | tokens: {"prompt_tokens":1800,"completion_tokens":2571,"total_tokens":4371,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1603,"image_tokens":0},"cache_creation_input_tokens":0} | 30s
 generated: 2026-06-13T12:09:24.214Z -->
| **Severity** | **Location** | **Issue** | **Fix** |
|--------------|--------------|-----------|---------|
| **Critical** | All routes (lines 6–98) | **No authentication enforcement** – all endpoints are publicly accessible without any token/session check. | Add an authentication middleware (e.g., JWT verification) on the router or individual routes. |
| **Critical** | All async handlers (lines 10, 18, 27, 37, 45, 54, 63, 73, 82) | **Unhandled promise rejections** – async functions are not wrapped in try/catch. If `query()` throws (e.g., DB connection loss), Express does not catch the error, causing unhandled rejection and potential process crash. | Wrap each handler in a try/catch block and call `next(err)` or use a centralized async error wrapper (e.g., `express-async-errors`). Always return a generic 500 response on failure. |
| **High** | `/count/today`, `/count/yesterday`, `/count/yesterday/by-isp`, `/trend/7days`, `/pending`, `/officer-summary` | **Non‑sargable `DATE()` function on indexed column** – `WHERE DATE(create_date) = ?` and `BETWEEN` prevent index usage on `create_date`. On a 2M‑row table this leads to full table scans, causing high latency and potential denial‑of‑service under load. | Rewrite queries to use range predicates: `WHERE create_date >= ? AND create_date < ?` (date+1). For `BETWEEN` use `>= start` and `<= end` (or `< end+1`). Add appropriate index on `create_date`. |
| **High** | `/count/today`, `/count/yesterday`, `/count/yesterday/by-isp`, `/trend/7days`, `/pending`, `/officer-summary` | **Timezone mismatch** – `bkkDate()` adds a fixed 7‑hour offset to UTC. If the database server also stores `create_date` in UTC (common), then comparing with `DATE(create_date)` using Bangkok date produces incorrect results for rows near midnight UTC (e.g., 23:00 UTC = 06:00 next day in BKK). | Store all timestamps in UTC and perform date filtering using the application’s timezone logic (e.g., compute UTC range for the Bangkok date). Alternatively, use `CONVERT_TZ` in the query if the DB supports it. |
| **High** | `/by-nip/:nipNo` (line 47) | **Weak input validation** – `Number.isFinite(nipNo)` accepts floats, non‑integers, and negative numbers. A negative or fractional `nipNo` may cause unexpected query results or unnecessary DB load. | Validate that `nipNo` is a positive integer (e.g., `Number.isInteger(nipNo) && nipNo > 0`). If invalid, return 400. |
| **High** | `/has-evidence` (line 56) | **No URL validation** – the `url` query parameter is only checked for existence. Arbitrarily long or malformed strings can be passed, potentially causing performance issues or diagnostic confusion. | Validate the URL format (e.g., using a regex or built‑in `URL` constructor) and enforce a maximum length (e.g., 2048 characters). Return 400 if invalid. |
| **Medium** | All endpoints | **No rate limiting** – an attacker can flood the API with requests, exhausting database connections or CPU resources. | Integrate a rate‑limiting middleware (e.g., `express-rate-limit`) per route or globally. |
| **Medium** | `/by-nip/:nipNo`, `/top-nip-by-record` | **No pagination for large result sets** – although `/by-nip` returns only 20 rows, `/top-nip-by-record` could still cause heavy sorting on the DB if the table grows. | Ensure LIMIT is always applied (already done) and consider using cursor‑based pagination for potential future expansions. |
| **Low** | `/officer-summary` (line 84) | **Potential N+1 queries** – four separate `query()` calls are executed sequentially; they could be combined into one multi‑query or parallelized with `Promise.all`. | Rewrite with a single SQL batch or use `Promise.all` to run independent queries concurrently for better performance. |
