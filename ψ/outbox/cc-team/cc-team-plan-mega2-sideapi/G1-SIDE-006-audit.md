<!-- cc-team deliverable
 group: G1 (sideapi division)
 member: SIDE-006 role=audit model=deepseek/deepseek-v4-flash
 finish_reason: stop | tokens: {"prompt_tokens":441,"completion_tokens":2486,"total_tokens":2927,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1055,"image_tokens":0},"cache_creation_input_tokens":0} | 30s
 generated: 2026-06-13T12:08:09.555Z -->
Below is the security and bug audit of the `db.ts` module, focusing on input validation, error handling, authentication, injection, and resource limits.

| severity | location                          | issue                                                                                                                                                                                                                                                                                                                                                       | fix                                                                                                                                                                                                                                                                                                                                                                                                    |
|----------|-----------------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| CRITICAL | `getPool()` (lines 10–24)        | **Hardcoded default database credentials** – Host `209.15.105.27`, user `"root"`, and empty password are defined as fallback values when environment variables are missing. This exposes the database to unauthorized access if the environment variables are not set.                                                                                         | Remove all insecure defaults. Require `DETECT_DB_HOST`, `DETECT_DB_USER`, `DETECT_DB_PASSWORD` (and optionally `DET_DB_PORT`) to be provided via environment variables, and throw a clear error during startup if any are missing. Example: `if (!process.env.DETECT_DB_HOST) throw new Error('DETECT_DB_HOST is required');`. Do not fall back to hardcoded IP or root. |
| CRITICAL | `getPool()` (line 17)            | **Default password is empty string** – Using `""` as the default password can lead to authentication bypass if the variable is not set.                                                                                                                                                                                                                      | Same as above – remove default or enforce a non‑empty secret.                                                                                                                                                                                                                                                                                                                                          |
| HIGH     | `getPool()` (line 21)            | **Unlimited connection queue** – `queueLimit: 0` allows an infinite number of queued requests when all connections are in use. If the database becomes slow or unreachable, memory can grow without bound, leading to denial‑of‑service.                                                                                                                      | Set a reasonable `queueLimit` (e.g., 50–100) and implement a fallback behaviour (e.g., reject requests when the queue is full) or use a circuit breaker.                                                                                                                                                                                                                                               |
| HIGH     | `healthCheck()` (line 33)        | **Mixed use of `execute()` for health checks and `query()` for data queries** – The comment says `query()` is used to avoid LIMIT issues with prepared statements, but `healthCheck` uses `execute()`. This inconsistency may lead to unexpected behaviour if the same MySQL version issues affect `execute()` in other contexts.                            | Ensure consistency: either always use `query()` or always use `execute()`. If `execute()` has known LIMIT bugs, use `query()` everywhere (including health checks) and document the reason.                                                                                                                                                                                                             |
| HIGH     | `query()` (line 27)              | **No prevention of SQL injection when SQL string is constructed unsafely by the caller** – The function accepts a raw SQL string and relies on the caller to use placeholders. If the caller concatenates user input into `sql` (e.g., `query("SELECT * FROM users WHERE id = " + userId)`), the module cannot mitigate injection.                                 | Enforce parameterized queries by design: accept only a template string and a parameter list, and refuse to execute queries that contain string concatenation. Alternatively, add a wrapper that validates the SQL string (e.g., ensure it contains only allowed characters and uses `?` placeholders). At minimum, document that callers must never concatenate user input into the SQL string. |
| MEDIUM   | `getPool()` (lines 10–24)        | **No pool timeout or idle timeout** – The pool does not set `idleTimeout` or `maxLifetime`. Stale connections may remain open indefinitely, eventually consuming server resources or causing timeouts.                                                                                                                                                        | Add connection lifecycle settings: e.g. `idleTimeout: 60000` (60s), `maxLifetime: 3600000` (1h).                                                                                                                                                                                                                                                                                                        |
| MEDIUM   | `query()` (line 28)              | **No error handling / logging** – The function throws errors without any logging or sanitisation. Unhandled database errors may leak stack traces or sensitive information (e.g., schema details) to the caller.                                                                                                                                               | Wrap the query call in a try-catch, log the error (without exposing sensitive data), and rethrow a generic error. For example: `try { const [rows] = await pool.query(sql, params); return rows; } catch (err) { console.error('Database query failed', err); throw new Error('Database error'); }`                                                                                              |
| MEDIUM   | `healthCheck()` (line 33)        | **Potential information leak in error response** – The error message is returned directly (`e.message`) and may contain database‑specific details (e.g., host, SQL syntax).                                                                                                                                                                                | Sanitise the error message before returning: return a generic string like `"Database unreachable"` and keep the detailed error for internal logs.                                                                                                                                                                                                                                                            |
| LOW      | `getPool()` (line 14)            | **Default host is a static external IP** – Hardcoding `209.15.105.27` creates a dependency on a specific server and may expose network topology if the source code is leaked.                                                                                                                                                                                 | Remove the default host; require it through environment variable. If a default is needed for local development, use `localhost` and document that it must be overridden in production.                                                                                                                                                                                                            |
| LOW      | `query()` (line 28)              | **No input validation on parameter types** – The `params` array is passed directly to the driver. Unexpected data types (e.g., objects, arrays) could cause runtime errors or undesired SQL behaviour.                                                                                                                                                    | Validate that all elements in `params` are primitive types (string, number, boolean, null, Date). Reject or safely convert non‑primitive values.                                                                                                                                                                                                                                                   |
| LOW      | Entire module                    | **Pool is never closed** – There is no exported method to gracefully close the pool (e.g., during application shutdown or testing). Connections may remain open and prevent the process from exiting cleanly.                                                                                                                                                 | Export a `closePool()` function that calls `await pool.end()` and resets `pool` to `null`. Use it in a `process.on('exit')` or SIGTERM handler.                                                                                                                                                                                                                                                     |

**Additional recommendations (not included in table):**  
- Use strict typing for query results instead of `<T = any>` to improve type safety.  
- Consider using a library like `mysql2/promise` with prepared statements (`execute()`) and work around the LIMIT issue by using a workaround (e.g., passing LIMIT as strings or using `debug()`).  
- Implement a circuit‑breaker or retry mechanism to handle transient database failures gracefully.
