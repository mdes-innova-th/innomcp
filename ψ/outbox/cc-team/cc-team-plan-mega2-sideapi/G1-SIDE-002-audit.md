<!-- cc-team deliverable
 group: G1 (sideapi division)
 member: SIDE-002 role=audit model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":1361,"completion_tokens":4141,"total_tokens":5502,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":3268,"image_tokens":0},"cache_creation_input_tokens":0} | 78s
 generated: 2026-06-13T12:07:59.903Z -->
| severity | location | issue | fix |
|---|---|---|---|
| High | `app.use(cors())` | **CORS Misconfiguration:** Allows requests from any origin. If the API is exposed to the internet, this enables unauthorized cross-origin data access and potential CSRF attacks. | Configure CORS with an explicit whitelist: `cors({ origin: process.env.ALLOWED_ORIGINS?.split(',') })`. |
| High | `/admin/contract-map` & global routes | **Missing Authentication:** The `/admin/` endpoint and other routes lack authentication/authorization, allowing unauthenticated access to internal architecture, contract data, and system modes. | Implement an auth middleware (e.g., API key, JWT, or mTLS) and apply it to `/admin/*` and other sensitive routes. |
| High | End of middleware chain | **Missing Global Error Handler:** No global Express error handler is defined. Unhandled route exceptions will return default Express error pages, potentially leaking stack traces and internal file paths. | Add a global error handler: `app.use((err, req, res, next) => { console.error(err); res.status(500).json({ error: "Internal Server Error" }); });`. |
| Medium | `/health` & `/admin/contract-map` | **Information Disclosure:** Endpoints expose internal database names (`db_aces`), environment variable keys (`requiredEnv`), and internal architecture modes, aiding attacker reconnaissance. | Remove sensitive internal details from public endpoints. Return generic status (e.g., `{"status": "ok"}`) or protect these endpoints behind authentication. |
| Medium | Middleware setup | **Missing Rate Limiting:** No rate limiting is applied. The API is vulnerable to brute-force attacks, scraping, and Denial of Service (DoS) via resource exhaustion. | Implement rate limiting using `express-rate-limit`: `app.use(rateLimit({ windowMs: 15 * 60 * 1000, max: 100 }))`. |
| Medium | Middleware setup | **Missing Security Headers:** Lacks standard HTTP security headers (CSP, X-Frame-Options, HSTS), leaving the service vulnerable to common web attacks like XSS and clickjacking. | Install and use `helmet`: `import helmet from "helmet"; app.use(helmet());`. |
| Low | `const PORT = Number(...)` | **Improper Port Validation (Input Validation):** If `WEBD_API_PORT` is a non-numeric string, `Number()` returns `NaN`. `app.listen(NaN)` behaves unpredictably or crashes without a clear error. | Validate the port: `const PORT = parseInt(process.env.WEBD_API_PORT \|\| "3014", 10); if (isNaN(PORT) \|\| PORT < 1 \|\| PORT > 65535) throw new Error("Invalid port");`. |
| Low | `server.on("error", ...)` | **Silent Failure on Server Errors (Error Handling):** If the server emits an error other than `EADDRINUSE`, it exits with code 1 but does not log the actual error object, making debugging impossible. | Log the error before exiting: `console.error("[WebdAPI] ❌ Server error:", err); process.exit(1);`. |
| Low | `app.use(express.json())` | **Implicit JSON Payload Limit (Resource Limits):** Relies on Express's implicit default (100kb) for JSON payload size. Explicit limits prevent memory exhaustion and clarify resource boundaries. | Explicitly set the limit based on expected payloads: `app.use(express.json({ limit: '50kb' }))`. |
| Low | `const hc = await healthCheck()` | **Unused Variable / Unchecked Health (Bug):** The result of `healthCheck()` is assigned to `hc` but never evaluated. If the DB is completely unreachable, the server still boots and serves 500s later. | Check the result and abort boot if critical: `if (!hc.ok) throw new Error("Health check failed");` or remove the unused variable assignment. |
