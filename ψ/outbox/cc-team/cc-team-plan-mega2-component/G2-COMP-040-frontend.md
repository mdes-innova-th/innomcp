<!-- cc-team deliverable
 group: G2 (component division)
 member: COMP-040 role=frontend model=MiniMaxAI/MiniMax-M3
 finish_reason: stop | tokens: {"prompt_tokens":1437,"completion_tokens":1255,"total_tokens":2692,"prompt_tokens_details":{"cached_tokens":114,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":0,"image_tokens":0},"cache_creation_input_tokens":0} | 31s
 generated: 2026-06-13T12:14:05.042Z -->
# React/Next Component Audit

| Severity | Location | Issue | Fix |
|---------|----------|-------|-----|
| Medium | `route.ts:1–160` (entire route module) | **No rate limiting / abuse protection** — POST/DELETE accept any unauthenticated client payload; cookies are user-controllable and can be used to grow `customProviders[]` or `favoritePrompts[]` arbitrarily large, enabling DoS via huge JSON cookies (4–16 KB limit, but still abusive). | Add per-IP/userId rate limit (e.g., Upstash), and clamp array lengths (`favoritePrompts.slice(0, 100)`, `customProviders.slice(0, 50)`) before persisting. Validate `baseUrl` with `new URL()` allowlist. |
| High | `setPreferencesCookie` (`route.ts:84–90`) | **Cookie written without `secure` flag** and `httpOnly: false` — preferences are exposed to client-side JS and can be intercepted on plain HTTP. | Set `secure: process.env.NODE_ENV === "production"`, keep `httpOnly: false` (needed client-side), and add `maxAge` (e.g., `60 * 60 * 24 * 365`). |
| High | `POST` handler (`route.ts:120–127`) | **No authentication / authorization** — any user can read/write any other user's preferences because the cookie key is global (`userPreferences`) with no user/session binding. | Derive a per-user key (e.g., include a server-issued session id or signed JWT in the cookie name), or store prefs server-side keyed by `userId`. |
| Medium | `sanitizeInput` (`route.ts:42–80`) | **Type-narrowing via `as string` / `as Record<string, unknown>` is unsafe** — runtime values could be objects with `toString` returning allowed strings (e.g., `new String("light")` passes `typeof === "object"` check after cast, or `valueOf` coercion). Also `Array.isArray` check is shallow for `favoritePrompts`/`customProviders`. | Use a proper schema validator (Zod): `z.object({ theme: z.enum(["light","dark","system"]).optional(), favoritePrompts: z.array(z.string().max(500)).max(100).optional(), ... })`. |
| Low | `mergePreferences` (`route.ts:82–85`) | **Spread order is misleading** — `{ ...defaults, ...current, ...input }` is correct but `defaults` after `current` means stale `current` cannot reintroduce a removed default. Functionally fine; flag is purely clarity. | Document intent or use `{ ...current, ...input }` and re-apply `defaults` as fallback only for missing keys. |
| Low | `getCurrentPreferences` (`route.ts:87–99`) | **`return { ...defaults }` discards `current` when cookie is malformed** — silent data loss on a single bad parse; user loses all preferences without notice. | Log the parse error and return a 500, or return `defaults` but write them back via `setPreferencesCookie` so next read is consistent (already happens in GET, but not in POST). |
| Low | `setPreferencesCookie` called inside `GET` (`route.ts:108–112`) | **Side-effect in GET is non-idempotent** — every poll rewrites the cookie, causing unnecessary Set-Cookie headers and breaking HTTP cache semantics; clients/CDNs may behave unexpectedly. | Remove the write from `GET`; only read. Write only on `POST`/`DELETE`. |
| Medium | `GET` handler return type (`route.ts:110`) | **GET mutates cookies but the response object is constructed before the write** — if `setPreferencesCookie` throws (e.g., cookie size > limit due to user bloat), the function rejects with an unhandled 500 and no response is returned. | Wrap `setPreferencesCookie` in try/catch and return `NextResponse.json(prefs)` even on write failure, logging server-side. |
| Low | `sanitizeInput` (`route.ts:68–72`) | **`customProviders[].baseUrl` not validated** — accepts arbitrary strings, including `javascript:` or internal `http://localhost:*` URLs that could be used in SSRF when the client later fetches them. | Validate `baseUrl` with `URL` constructor, require `https:` (or `http:` for dev), and block private IP ranges / localhost. |
| Low | `DELETE` handler (`route.ts:132–135`) | **Returns `defaults` object** but the cookie was just deleted — client may briefly hold stale state. Acceptable, but inconsistent with the "cookie is source of truth" model used elsewhere. | Return `{ ok: true }` or the freshly read defaults via `getCurrentPreferences()`. |
| Info | Whole file | **Module-level `defaults` is a shared mutable reference** — currently safe because no code mutates it, but any future `.push`/assignment would leak across requests on the same Node process. | Freeze with `Object.freeze(defaults)` and deep-freeze nested arrays (`favoritePrompts: [] as const`). |
| Info | `route.ts:8` (`customProviders`) | **Schema duplication** — `UserPreferences`, `defaults`, and `sanitizeInput` all encode the same shape three times. Drift is inevitable. | Define a single Zod schema and derive `defaults` + `sanitizedInput` from it. |

## Summary
- **No React/Next client-side issues** in this file (it's a server-only route handler — no hooks, effects, or JSX).
- **Top 3 to fix first**: (1) add auth/per-user keying, (2) add `secure` + `maxAge` to cookie, (3) adopt Zod for input validation and array bounds.
