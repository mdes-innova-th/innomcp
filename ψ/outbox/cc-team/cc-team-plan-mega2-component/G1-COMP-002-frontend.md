<!-- cc-team deliverable
 group: G1 (component division)
 member: COMP-002 role=frontend model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":2358,"completion_tokens":3247,"total_tokens":5605,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2428,"image_tokens":0},"cache_creation_input_tokens":0} | 64s
 generated: 2026-06-13T12:07:36.589Z -->
| severity | location | issue | fix |
| :--- | :--- | :--- | :--- |
| **High** | `AdminPage` (Component) | **Monolithic Component & Unnecessary Re-renders**: The component holds state for all tabs (users, sessions, mother, etc.). Typing in `dispatchQuery` or updating any single state triggers a re-render of the entire page and all inactive tab logic. | Extract each tab into its own isolated component (e.g., `<UsersTab />`, `<MotherTab />`). Lift state down so state changes only re-render the active tab. |
| **High** | `useEffect` (Auth guard) | **Hydration Risk & Flash of Unauthenticated Content**: Checking auth and redirecting via `router.push` in a client-side `useEffect` causes the protected UI to render briefly on the server and during hydration before redirecting. | Move authentication/authorization checks to Next.js Middleware (`middleware.ts`) to protect the `/admin` route at the edge/server level before rendering. |
| **High** | `useEffect` (Data fetchers) | **Effect Dependency / Memory Leak**: Data fetching effects lack cleanup. If the component unmounts or dependencies change before the fetch resolves, it triggers a state update on an unmounted component. It also eagerly fetches all data on mount regardless of the active tab. | Use `AbortController` to cancel fetches on cleanup. Better yet, use a data-fetching library (React Query/SWR) or only trigger fetches when `activeTab` matches the specific tab. |
| **Medium** | `circuitOpenCount` (State) | **State-in-Render / Redundant State**: `circuitOpenCount` is stored in state but is directly derived from `motherCircuits`. Keeping it in state risks synchronization bugs and unnecessary re-renders when circuits update. | Remove `circuitOpenCount` state. Compute it during render: `const circuitOpenCount = motherCircuits.filter(c => c.state === 'open').length;` |
| **Medium** | `AdminPage` (Component) | **Missing Error Boundaries**: A crash in any complex sub-component (like `AgentLeaderboard` or data tables) will unmount the entire admin dashboard, resulting in a blank screen. | Wrap the main layout or individual tab contents in a React `<ErrorBoundary>` component to gracefully catch and display errors without crashing the whole page. |
| **Medium** | List Rendering (Truncated JSX) | **Missing/Invalid Keys**: When mapping over `users`, `sessions`, `auditLog`, etc., using array indices as keys or omitting them will cause rendering bugs and lost state when lists are sorted, filtered, or paginated. | Ensure all `.map()` calls use a stable, unique identifier (e.g., `user.user_id`, `session.jti`, `log.id`) as the `key` prop. |
| **Low** | `dispatchQuery` / Props | **Prop Drilling (if extracted)**: If the monolithic component is broken down into tab components, passing dozens of state variables and setters down will result in severe prop drilling. | When extracting tabs, keep the state *inside* the tab components. If shared state is truly needed, use React Context or a lightweight state manager (Zustand/Jotai) scoped to the admin layout. |
| **Low** | Audit Logs / Synthesis | **Unsafe `dangerouslySetInnerHTML` Risk**: Admin panels often render raw log `details` or AI `synthesis` using `dangerouslySetInnerHTML`, which is an XSS vector if the backend data isn't strictly sanitized. | If rendering HTML/Markdown from `details` or `synthesis`, use a safe renderer like `react-markdown` or sanitize with `DOMPurify` before passing to `dangerouslySetInnerHTML`. |
