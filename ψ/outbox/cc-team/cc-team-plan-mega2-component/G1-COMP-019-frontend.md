<!-- cc-team deliverable
 group: G1 (component division)
 member: COMP-019 role=frontend model=zai-org/GLM-5.1
 finish_reason: stop | tokens: {"prompt_tokens":675,"completion_tokens":1224,"total_tokens":1899,"prompt_tokens_details":{"cached_tokens":64,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":616,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 11s
 generated: 2026-06-13T12:09:54.731Z -->
| severity | location | issue | fix |
|----------|----------|-------|-----|
| 🔴 Critical | `handleImageRequest` — `imagePath` from `searchParams` | **Path traversal vulnerability**: `imagePath` is user-controlled and joined directly with `process.cwd()` without sanitization. An attacker can pass `?path=../../etc/passwd` to read arbitrary files on the server. | Validate and normalize the path. Reject paths containing `..`, ensure the resolved path stays within an allowed directory: `const resolved = path.resolve(process.cwd(), imagePath); if (!resolved.startsWith(path.resolve(process.cwd(), 'allowed-dir'))) return 400;` Also decode URI components. |
| 🟠 High | `handleImageRequest` — `fs.existsSync`, `fs.statSync`, `fs.readFileSync` | **Blocking synchronous I/O in async handler**: All three `fs` calls are synchronous, blocking the Node.js event loop on every request. Under load this starves other requests. | Replace with async `fs.promises` equivalents: `fs.promises.access()`, `fs.promises.stat()`, `fs.promises.readFile()`. Or use `await fs.promises.readFile()` and catch errors instead of checking existence first. |
| 🟠 High | `handleImageRequest` — `existsSync` → `statSync` → `readFileSync` | **TOCTOU race condition**: File state is checked then read in separate operations; the file could be removed/changed between checks. | Remove `existsSync`/`statSync` guards; attempt `readFile` directly and handle `ENOENT` / `EISDIR` errors in the catch block. |
| 🟡 Medium | `handleImageRequest` — `fs.readFileSync` | **No file size limit**: Entire file is read into a `Buffer`. A very large file could exhaust memory. | Use `fs.promises.stat()` first to check `stat.size` against a max (e.g. 10 MB), or stream the response with `createReadStream` + `Content-Length` header. |
| 🟡 Medium | `handleImageRequest` — `new URL(request.url)` | **Unescaped/decoded path**: `searchParams.get()` auto-decodes, but double-encoding or `%2e%2e` bypasses naive `..` checks if you only check the raw string. | Always resolve and compare against an allow-list directory using `path.resolve()` + `startsWith()`, not string-matching for `..`. |
| 🟢 Low | `GET` handler — `console.log` | **Verbose logging in production**: Logs every request status, leaking operational details. | Use a conditional logger or `process.env.NODE_ENV === 'development'` guard; remove or reduce in production. |
| 🟢 Low | `handleImageRequest` — `catch (err: unknown)` | **Generic 500 masks real errors**: All errors return `"Internal Server Error"` with no distinguishing info, making debugging harder. | Differentiate error types: return 404 for `ENOENT`, 400 for `EISDIR`, and 500 only for truly unexpected errors. Log the full error server-side. |
