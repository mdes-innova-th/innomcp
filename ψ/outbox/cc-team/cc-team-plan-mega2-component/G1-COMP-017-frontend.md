<!-- cc-team deliverable
 group: G1 (component division)
 member: COMP-017 role=frontend model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":1020,"completion_tokens":2424,"total_tokens":3444,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1788,"image_tokens":0},"cache_creation_input_tokens":0} | 37s
 generated: 2026-06-13T12:09:44.208Z -->
| severity | location | issue | fix |
|----------|----------|-------|-----|
| High | `rateLimitMap` usage and logic (line ~5, 46-54) | In-memory rate limiting is per‑process, not shared across serverless instances. This makes the limit ineffective in multi‑instance deployments (e.g., Vercel, AWS Lambda) and can allow bypassing by switching instances. | Replace the in‑memory map with a shared store (e.g., Redis, DynamoDB, or a session‑based token bucket backed by a database). |
| High | `rateLimitMap` (no TTL/eviction strategy) | The map grows indefinitely, leading to a memory leak as new session IDs are added without ever being removed. | Implement a TTL (e.g., `setTimeout` to delete entries after a window) or use a bounded LRU cache with a maximum number of entries (e.g., `lru-cache`). |
| Medium | `ensureLogFile()` and `fs.appendFileSync()` (lines 14‑23, 64) | Synchronous file system calls block the Node.js event loop and are not recommended in serverless environments where each request should be non‑blocking. The file path may also be unwritable in some hosting setups. | Use `fs/promises` async methods (`mkdir`, `appendFile`). Convert `ensureLogFile` to an async function and call it with `await`. Consider a durable write solution (database or object storage) for serverless. |
| Medium | POST handler (no auth) | The endpoint lacks any authentication or authorization. Anyone can submit arbitrary feedback, leading to spam, data pollution, or abuse. | Add a simple API key or require a valid user session. If anonymous feedback is acceptable, implement CSRF tokens and stricter rate limiting. |
| Low | Rate limit logic only applied when `sessionId` is present (line 46) | Requests without a `sessionId` are not rate‑limited, allowing unlimited submissions from clients that omit this field. | Implement a fallback IP‑based or global rate limit to prevent abuse from clients that do not send a `sessionId`. |
| Low/Info | Hardcoded `FEEDBACK_LOG_PATH` (line 10) | The path relies on `process.cwd()` and a `logs` directory. If the app restarts with a different working directory or missing permissions, writes will fail. | Use an environment variable (e.g., `FEEDBACK_LOG_PATH`) or a config module that resolves a consistent, writable location. |
| Info | Comment field stored without sanitization (line 59) | If the feedback comment is later rendered in an admin UI with `dangerouslySetInnerHTML` or without escaping, an XSS attack is possible. Not a bug in this route itself, but a risk for downstream consumers. | Sanitize the comment on input (strip HTML/special characters) or ensure any rendering layer escapes it properly. Document the security contract for consumers. |
