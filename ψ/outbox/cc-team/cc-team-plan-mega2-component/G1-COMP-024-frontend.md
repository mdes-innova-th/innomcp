<!-- cc-team deliverable
 group: G1 (component division)
 member: COMP-024 role=frontend model=zai-org/GLM-5.1
 finish_reason: stop | tokens: {"prompt_tokens":1079,"completion_tokens":2227,"total_tokens":3306,"prompt_tokens_details":{"cached_tokens":64,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1381,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 19s
 generated: 2026-06-13T12:10:37.427Z -->
| severity | location | issue | fix |
|---|---|---|---|
| 🔴 High | `return new NextResponse(JSON.stringify({…detail: errorDetail…}))` | **Information disclosure** — upstream error body (`errorDetail`) is forwarded verbatim to the client, potentially leaking internal service details, stack traces, or infrastructure info. | Strip or sanitize `errorDetail`; only return a generic message to the client. Log the full detail server-side only. |
| 🔴 High | `body = (await request.json()) as ProxyRequest` | **No request body size limit** — an attacker can send an arbitrarily large JSON payload, consuming memory and CPU on the server. | Add a size check before parsing (e.g., `content-length` header check, or use a limiter middleware / `next.config.js` `api.bodyParser.sizeLimit`). |
| 🟠 Medium | `request.signal.addEventListener('abort', …)` | **Abort listener never removed** — the listener on `request.signal` is added but never cleaned up with `removeEventListener`, which can prevent GC of the `controller` / closure until the signal is GC'd. | Use `{ once: true }` option: `request.signal.addEventListener('abort', handler, { once: true })`, or call `request.signal.removeEventListener` in a `finally` block. |
| 🟠 Medium | `clearTimeout(timeoutId)` after `await fetch(…)` | **No timeout on response body reads** — the timeout only covers the fetch itself. After headers arrive, `upstreamResponse.text()` / `.json()` can hang indefinitely while reading the body. | Wrap the body-read calls (`text()`, `json()`) in a `Promise.race` against a new timeout, or re-use the abort controller with a fresh timeout. |
| 🟠 Medium | `if (!body.model \|\| …)` | **Insufficient input validation** — `model` is passed to upstream without allow-list validation; `messages` array contents are not validated (role/content shape). A malicious client could inject unexpected values. | Validate `model` against an allow-list; validate each message has `role` ∈ `{system, user, assistant}` and `content` is a string; clamp `temperature` to [0,2] and `max_tokens` to a sane upper bound. |
| 🟠 Medium | Streaming branch: `new NextResponse(upstreamResponse.body, …)` | **No client-disconnect cleanup** — if the client disconnects mid-stream, the upstream connection may remain open until it finishes or times out, wasting resources. | Pipe through a `TransformStream` that detects `request.signal` abort and calls `upstreamResponse.body.cancel()`, or add a `waitUntil`-style cleanup. |
| 🟡 Low | `headers['Authorization'] = \`Bearer ${apiKey}\`` | **API key in environment variable with no rotation/fallback** — if `MDES_OLLAMA_API_KEY` is empty string (set but blank), the `Bearer ` header is still sent, which may cause auth errors. | Change condition to `if (apiKey && apiKey.trim())` or use `!!apiKey`. |
| 🟡 Low | `console.log(…timestamp=${new Date().toISOString()}`) | **Verbose request logging** — logs every request with model/stream/params. In high-traffic scenarios this can fill logs and may leak usage patterns. | Use structured logging with log levels; consider `console.debug` or a proper logger; avoid logging per-request in production. |
| 🟡 Low | Non-stream error branch: `upstreamResponse.text()` | **No size cap on error body read** — `upstreamResponse.text()` reads the entire error response into memory; a misbehaving upstream could return a huge error body. | Limit the read (e.g., `response.clone().text()` with a byte cap, or read only the first N bytes via a `ReadableStream` tee). |
| 🟡 Low | `const UPSTREAM_URL = 'https://ollama.mdes-innova.online/v1/chat/completions'` | **Hardcoded upstream URL** — changing the upstream endpoint requires a code change and redeploy. | Move to an environment variable (`process.env.MDES_UPSTREAM_URL`) with the current value as default/fallback. |
| 🟡 Low | Missing | **No rate limiting** — the proxy endpoint is unauthenticated and unthrottled, allowing abuse/DoS against the upstream service. | Add rate-limiting middleware (e.g., `next-rate-limit`, a reverse-proxy rule, or an in-memory token bucket). |
| 🟡 Low | Missing | **No CORS / origin validation** — any origin can hit this API route from a browser, enabling CSRF-like abuse. | Add CORS headers or validate `Origin` / `Referer`; consider restricting to same-origin via Next.js middleware. |
