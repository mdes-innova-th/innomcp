<!-- cc-team deliverable
 group: G1 (concurrency division)
 member: CONC-007 role=concurrency model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":1312,"completion_tokens":4359,"total_tokens":5671,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":3781,"image_tokens":0},"cache_creation_input_tokens":0} | 81s
 generated: 2026-06-13T12:08:51.568Z -->
| severity | location | race/issue | fix |
|---|---|---|---|
| High | `CacheManager` (Design) | **Cache Stampede / Check-Then-Act Race**: The cache lacks an async `getOrSet(key, resolver)` method. Callers forced to use `if (!cache.has(k)) { val = await fetch(); cache.set(k, val); }` will suffer from check-then-act races and cache stampedes (dog-piling) when multiple concurrent async requests miss the same key simultaneously. | Implement `getOrSet<T>(key, resolver: () => Promise<T>)` using a singleflight pattern (e.g., tracking in-flight requests in a `Map<string, Promise<T>>`) to ensure the async resolver is executed only once per key concurrently. |
| Medium | `cleanupExpired`, `configure`, `size`, `stats` | **Event Loop Starvation (Async Interleaving)**: `cleanupExpired()` iterates the entire `Map` synchronously. `size()` and `stats()` trigger this full scan, and `configure()` can trigger massive synchronous eviction loops. In a high-concurrency async environment, these block the event loop, delaying pending Promises and I/O callbacks (causing tail latency spikes). | Make cleanup incremental (e.g., process a batch of $N$ items per interval tick). Remove `cleanupExpired()` from `size()`/`stats()`. For bulk evictions in `configure()`, yield to the event loop periodically (e.g., via `setImmediate`) or allow temporary over-capacity. |
| Low | `constructor` / `cleanupTimer` | **Resource/Timer Leak**: The `setInterval` is never cleared. While `.unref()` prevents it from keeping the Node.js process alive on its own, if the process stays alive for other reasons, the timer and its closure references leak indefinitely. There is no `destroy()` or `close()` lifecycle method. | Add a `destroy()` method that calls `clearInterval(this.cleanupTimer)`, clears the `entries` Map, and optionally resets the singleton instance. Call it during graceful shutdowns or test teardowns. |
| Info | Class-wide (Shared State) | **Shared Mutable State (Thread Safety)**: The class strictly assumes a single-threaded event loop. If ever used in Node.js Worker Threads (sharing memory via `SharedArrayBuffer`) or runtimes with different threading models, the linked list pointer mutations and `Map` updates will cause severe data corruption and race conditions. | Explicitly document that the class is single-threaded and not thread-safe. If cross-thread safety is required, refactor to use thread-safe primitives (`Atomics`, `SharedArrayBuffer`) or a message-passing architecture. |
