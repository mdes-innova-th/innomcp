<!-- cc-team deliverable
 group: G1 (concurrency division)
 member: CONC-005 role=concurrency model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":2148,"completion_tokens":3382,"total_tokens":5530,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2688,"image_tokens":0},"cache_creation_input_tokens":0} | 49s
 generated: 2026-06-13T12:07:59.741Z -->
| Severity | Location | Race/Issue | Fix |
|----------|----------|------------|-----|
| High | `AuditLogger.log` & `rotateIfNeeded` (filesystem) | **Multi-process check-then-act / shared mutable state without locks** – Multiple Node processes (e.g., cluster workers) writing to the same `audit.jsonl` file can interleave `appendFileSync` calls, corrupting JSONL lines. Additionally, `rotateIfNeeded` uses `existsSync`/`statSync` then `renameSync` without any cross-process coordination, so two processes may both attempt rotation, causing one to rename the freshly created file or lose the previous day’s log. | Add cross-process advisory file locking (e.g., `proper-lockfile` or `flock`) around log writes and rotation. Alternatively, delegate all file writes to a single dedicated writer process via IPC, or use a logging library that handles multi-process rotation safely. |
| High | `AuditLogger.log` & `AuditLogger.clear` | **Shared mutable state without locks** – `clear` reads lines, filters, and rewrites files or deletes them when empty. Concurrent execution of `log` (appending) and `clear` (rewriting/deleting) from multiple processes can lead to lost log entries, partial files, or `EBUSY`/`ENOENT` errors. | Same as above: synchronise all file access with a per‑file lock. Consider an append‑only design with periodic compaction handled by a single process. |
| Medium | `AuditLogger.rotateIfNeeded` (internal state) | **Check-then-act on `lastDate`** – While unreachable in a synchronous single‑threaded environment, if the code were ever called from asynchronous contexts (e.g., a future refactor), the `lastDate` check and subsequent `rename` would be susceptible to TOCTOU races even within the same process. Currently low risk.  | Guard the rotation logic with a mutex or ensure the method remains synchronous and blocking. Better yet, use an atomic operation like `fs.rename` with a well‑known temp file naming convention to avoid conflicts even without a lock. |
| Medium | `AuditLogger` (filesystem) | **Ordering assumption** – The `getEntries`, `exportCSV`, and `exportJSON` methods read files directly while writes may be in progress. On some operating systems, `appendFileSync` writes are atomic only up to a certain length; a reader could obtain a truncated/corrupted line. | Use a readers‑writer lock, or read from a consistent snapshot (e.g., via a copy‑on‑write scheme). For reporting, consider reading from a separate aggregated store instead of raw log files. |

**Additional notes**  
- The `authorizer` property is set by `setAuthorizer` but never read – it is dead code and not a concurrency issue.  
- All file I/O is synchronous, so in a single‑process event loop there are no interleaved executions; the races listed above are relevant only when the singleton is used across multiple Node.js processes (e.g., `cluster`, `pm2`, or Docker replicas). The module provides no protection for this very common deployment pattern.
