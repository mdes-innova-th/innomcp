<!-- cc-team deliverable
 group: G1 (concurrency division)
 member: CONC-019 role=concurrency model=zai-org/GLM-5.1
 finish_reason: stop | tokens: {"prompt_tokens":1412,"completion_tokens":4723,"total_tokens":6135,"prompt_tokens_details":{"cached_tokens":1408,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":4488,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 51s
 generated: 2026-06-13T12:14:05.694Z -->
| severity | location | race/issue | fix |
|----------|----------|------------|-----|
| **Medium** | `getDefaultMcpClient()` | **Check-then-act race on singleton** — two concurrent calls can both see `null` and construct separate instances; safe today only because the constructor is synchronous (no yield point), but fragile if init ever becomes async | Use `defaultClient ??= new McpClient()` for conciseness; for true async-safety, gate behind a promise/mutex so concurrent awaiters share one initialization |
| **Medium** | `callBatch()` → `Promise.all` | **Promise.all short-circuit violates stated contract** — comment says "One failed call does not abort the others," but `Promise.all` rejects on the *first* rejection, discarding all other results. Currently safe only because `callTool` internally catches everything and returns `{success:false}`, but any future change that lets an exception escape will silently drop sibling results | Replace with `Promise.allSettled`; map fulfilled→value, rejected→`{success:false, error: reason.message}` to structurally guarantee independence |
| **Medium** | `isAvailable()` | **Response body never consumed on success path** — `return resp.ok` leaves the body stream unread; the underlying TCP socket is not returned to the keep-alive pool, causing connection leaks under sustained health-check load | Drain the body before returning: `await resp.text(); return resp.ok;` or `resp.body?.cancel(); return resp.ok;` |
| **Low** | `callTool()` retry loop | **Linear backoff without jitter** — under concurrent load, multiple clients retry in lockstep (thundering herd / synchronized retry storm) | Add random jitter: `sleep(RETRY_BACKOFF_MS * (attempt + 1) * (0.5 + Math.random()))` or use exponential backoff with full jitter |
| **Low** | `callTool()` | **No request deduplication (cache stampede)** — concurrent calls with identical `toolName + params` all fire separate HTTP requests; under burst traffic this amplifies load on the downstream MCP server | Add an inflight-map keyed by `${toolName}:${stableStringify(params)}`; first caller owns the real request, subsequent callers await the same promise |
