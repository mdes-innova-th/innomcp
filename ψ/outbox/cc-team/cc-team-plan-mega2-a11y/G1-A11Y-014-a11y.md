<!-- cc-team deliverable
 group: G1 (a11y division)
 member: A11Y-014 role=a11y model=zai-org/GLM-5.1
 finish_reason: stop | tokens: {"prompt_tokens":2211,"completion_tokens":3138,"total_tokens":5349,"prompt_tokens_details":{"cached_tokens":64,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1162,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 24s
 generated: 2026-06-13T12:10:15.403Z -->
# Accessibility & UX Audit — `ArtifactPanel.tsx`

| Severity | Location | Issue | Fix |
|---|---|---|---|
| 🔴 Critical | `MarkdownPreview` — `<li>` generation | List items (`<li>`) are emitted without wrapping `<ul>`/`<ol>`, producing invalid HTML. Screen readers cannot announce list semantics; ordered vs. unordered context is lost. | Wrap consecutive `<li>` runs in the appropriate parent (`<ul>` / `<ol>`) during conversion, or add `ALLOWED_TAGS: […, 'ul', 'ol']` and post-process the HTML to group them. |
| 🔴 Critical | Artifact selector buttons (line ~`artifacts.map`) | No `role="tablist"` / `role="tab"` pattern. Selected state is purely visual (class change). Screen readers cannot identify which artifact is active or that these are a single-select group. | Wrap the list in `<div role="tablist" aria-label="เลือก artifact">`. Add `role="tab"`, `aria-selected={selected === a.id}`, and `aria-controls="<panel-id>"` to each button. |
| 🔴 Critical | Tab buttons (`preview` / `raw` / `download`) | Tabs lack ARIA tab pattern (`role="tablist""/"tab"`). No `aria-selected`. No arrow-key navigation between tabs. Content panel has no `role="tabpanel"`. | Use `<div role="tablist">` parent, `role="tab" aria-selected` on each button, `role="tabpanel"` + `aria-labelledby` on content div. Implement left/right arrow key cycling with `onKeyDown`. |
| 🔴 Critical | Emoji icons on artifact buttons, empty state, source link | Emojis (📄📝💻🔧📊📈✕🔗) are announced by screen readers as verbose Unicode names (e.g., "page facing up"), providing no meaningful context and creating noise. | Add `aria-hidden="true"` to decorative emoji `<span>`s and provide accessible text via `aria-label` on the parent button, e.g. `aria-label={`${a.name} (${a.type})`}`. |
| 🟠 High | Close `<button>` | Uses ✕ character as visual label. Although `aria-label` exists, the ✕ can still be read by some AT. No focus management after close — focus disappears. | Replace ✕ with an SVG/X icon with `aria-hidden="true"` (keep `aria-label`). In `onClose`, `returnFocus` to the element that opened the panel. |
| 🟠 High | Source link `<a target="_blank">` | Opens external link with no warning to screen-reader users. No indication it opens in a new window. 🔗 emoji is decorative but announced. | Add `aria-label="ดูต้นฉบับ (เปิดในแท็บใหม่)"`. Add `aria-hidden="true"` on the 🔗 span. Consider a visually-hidden `(opens in new tab)` suffix. |
| 🟠 High | `text-muted-foreground/60` (close button default state) | 60 % opacity on `muted-foreground` almost certainly fails WCAG 1.4.3 contrast minimum (4.5:1 for small text). | Use a full-opacity token that meets contrast, e.g. `text-muted-foreground` without opacity modifier, or `text-foreground/50` with verified contrast. |
| 🟠 High | `text-[10.5px]` source link, `text-[11px]` tabs/buttons | Text below 12 px is very hard to read and magnifies contrast failures. WCAG 1.4.4 requires text to be resizable to 200 %. | Raise minimum to `text-xs` (12 px) or `text-[12px]`. Use font-weight ≥ 400 at small sizes. |
| 🟠 High | `text-foreground/90` in `CodePreview` | 90 % opacity reduces contrast below the 4.5:1 minimum for body text, especially on `bg-muted/40`. | Use `text-foreground` (full opacity) or verify the computed contrast ratio ≥ 4.5:1 against the actual background. |
| 🟠 High | Content panel — no `aria-live` region | When the user switches artifacts or tabs, the content area updates silently. Screen readers receive no announcement of the change. | Add `aria-live="polite"` and `aria-atomic="true"` to the content container. On artifact switch, announce e.g. `aria-label={`แสดง ${current?.name}`}`. |
| 🟡 Medium | Panel wrapper `<div>` | No landmark role. The panel is a major page region that AT users cannot jump to. | Use `<aside aria-label="Artifact panel">` or `<section aria-label="Artifacts">` instead of a plain `<div>`. |
| 🟡 Medium | Header `<p>` — "Artifacts (N)" | A `<p>` is used for what is semantically a heading. Screen-reader navigation by headings skips it. | Change to `<h2 className="…">Artifacts ({artifacts.length})</h2>` (or appropriate level). |
| 🟡 Medium | Artifact list container `<div>` | Not a semantic list. AT users cannot use list navigation commands. | Use `<ul role="tablist" aria-label="เลือก artifact">` with `<li role="presentation">` wrappers, or at minimum add `role="list"`. |
| 🟡 Medium | Empty state — emoji 📄 | Emoji is purely decorative but will be announced as "page facing up". | Wrap emoji in `<span aria-hidden="true">📄</span>` and ensure the adjacent `<p>` carries the meaning. |
| 🟡 Medium | `MarkdownPreview` — `dangerouslySetInnerHTML` | The rendered HTML has no accessible label or role. Screen readers may announce the raw markup or skip it. | Add `role="document"` and `aria-label="เนื้อหา markdown"` to the wrapper `<div>`. |
| 🟡 Medium | `CodePreview` — `<pre><code>` | No language identification for screen readers. Long code blocks are read linearly with no navigation. | Add `aria-label={`ซอร์สโค้ด${language ? ` ${language}` : ''}`}` on `<pre>`. Consider a "copy" button with `aria-label`. |
| 🟡 Medium | Chart preview `dangerouslySetInnerHTML` | SVG content injected without an accessible name or title. Screen readers may announce every SVG element. | Add `role="img"` and `aria-label="แผนภูมิ"` to the wrapper. Ensure the SVG has a `<title>` element. |
| 🟡 Medium | Download tab button | Clicking "⬇ Download" triggers `handleDownload` but provides no success/failure feedback to screen readers. | Announce result via a visually-hidden live region: `aria-live="polite"` → "กำลังดาวน์โหลด {name}". |
| 🟡 Medium | No keyboard shortcut to close panel | Users must Tab to the close button; no `Escape` key handler. | Add `onKeyDown={e => e.key === 'Escape' && onClose?.()}` on the panel root. |
| 🟢 Low | `border-primary/40`, `bg-primary/8` | Low-opacity borders/backgrounds may be invisible for users with low vision or dark-mode high-contrast settings. | Test in Windows High Contrast Mode; add `forced-colors` fallbacks or increase opacity. |
| 🟢 Low | `relTime()` returns Thai-only strings | No localization hook; non-Thai users get Thai-only timestamps with no `lang` attribute. | Add `lang="th"` on the timestamp `<span>` or internationalize the function. |
| 🟢 Low | `truncate` class on artifact names | Truncated text is invisible to sighted users; screen readers read the full name but sighted users can't see it. | Add `title={a.name}` for hover, or a tooltip component. |
| 🟢 Low | Focus ring not explicitly styled | Tailwind default focus rings may be overridden by `transition-colors` or absent in some themes. | Add `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-1` to all interactive elements. |

---

### Summary of top-priority fixes

1. **Implement ARIA Tabs pattern** for both the artifact selector and the preview/raw/download tabs (`role="tablist"`, `role="tab"`, `aria-selected`, `role="tabpanel"`, arrow-key navigation).
2. **Hide decorative emojis** with `aria-hidden="true"` and supply meaningful `aria-label` text on parent elements.
3. **Fix invalid HTML** — wrap orphan `<li>` elements in `<ul>`/`<ol>` inside `MarkdownPreview`.
4. **Raise contrast & font size** — remove `/60` opacity on muted text, increase `10.5px`/`11px` sizes to ≥ 12 px.
5. **Add landmark & heading** — `<aside>` wrapper, `<h2>` header.
6. **Add `aria-live="polite"`** on the content panel so artifact/tab switches are announced.
7. **Add `Escape` key** to close, and **return focus** to the opener on close.
