<!-- cc-team deliverable
 group: G1 (a11y division)
 member: A11Y-024 role=a11y model=zai-org/GLM-5.1
 finish_reason: stop | tokens: {"prompt_tokens":2192,"completion_tokens":3153,"total_tokens":5345,"prompt_tokens_details":{"cached_tokens":64,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2285,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 44s
 generated: 2026-06-13T12:14:32.277Z -->
| severity | location | issue | fix |
| :--- | :--- | :--- | :--- |
| **High** | `<textarea>` (implied by `textareaRef` / `rotatingPlaceholder`) | **Missing accessible label:** The input relies on a rotating placeholder for context. Placeholders disappear on input and often fail contrast ratios, leaving screen readers with no label. | Add a visually hidden `<label htmlFor="chat-input">` or `aria-label="Chat message"` to the `<textarea>`. |
| **High** | `<textarea>` (implied) | **Rotating placeholder:** Auto-updating placeholders (every 4s) can trigger screen reader announcements (aria-live trap) and violate WCAG 2.2 "Animation from Interactions" for cognitive accessibility. | Remove the auto-rotation. Use a static, clear placeholder, or only rotate when the field is empty AND unfocused, ensuring it doesn't use `aria-live`. |
| **High** | `CHAR_LIMIT` / `showCharCounter` logic | **Hidden character limit:** Screen readers won't know the 4000-character limit exists until they hit 3200 characters. If they exceed the limit, there is no error announcement. | Add `aria-describedby="char-counter-id"` to the textarea. Always render the character limit text for screen readers (use `sr-only` class when < 3200 chars). Announce errors via `aria-live="assertive"`. |
| **High** | `handleSendWithCsv` | **Silent error handling:** The `catch` block is empty. If the CSV analysis fails, the user gets no feedback, violating WCAG 3.3.1 (Error Identification). | Implement an error state (e.g., toast/inline message) and announce it using `aria-live="assertive"`. |
| **High** | `handleSendWithCsv` / `sendMessage` | **Missing focus management:** After sending a message, focus remains on the send button or input. Users must manually tab back to the textarea to continue chatting. | Programmatically return focus to the `<textarea>` after the message is sent and the input is cleared. |
| **Medium** | `<Image alt="preview" ... />` | **Non-descriptive alt text:** `alt="preview"` doesn't convey the meaning or content of the attached image to screen readers. | Change to `alt="Attached image preview"` or dynamically inject the file name: `alt={\`Preview of \${selectedFile.name}\`}`. |
| **Medium** | `<FontAwesomeIcon icon={faPaperclip} />` | **Icon accessibility:** The paperclip icon lacks `aria-hidden="true"`. Screen readers will announce "paperclip" out of context, or ignore it entirely if it conveys meaning. | Add `aria-hidden="true"` since the adjacent text describes the attachment. If it were alone, it would need `aria-label`. |
| **Medium** | `<div className="truncate ...">` (File name) | **Truncation trap:** Visual users see a truncated file name, but screen readers will read the entire string. Sighted users cannot see the full name. | Add a `title` attribute with the full file name: `title={selectedFile.name}` so sighted users can hover, and screen readers get the full context. |
| **Medium** | `counterTone` classes | **Color contrast risks:** Opacity modifiers (`text-muted-foreground/85`) and warning colors (`text-amber-600`, `text-rose-500`) frequently fail WCAG 1.4.3 contrast ratios against default backgrounds. | Audit the exact hex codes of these classes against your background using a contrast checker. Ensure 4.5:1 ratio. Remove opacity modifiers if they drop contrast. |
| **Medium** | `isDragging` state (implied drop zone) | **Missing drop zone roles/announcements:** Drag-and-drop areas are typically invisible to screen readers and keyboard users. | Ensure the drop zone has `role="region"`, `aria-label="File drop zone"`, and `tabIndex={0}` with keyboard handlers (Enter/Space to trigger file input). Announce drag states via `aria-live`. |
| **Low** | `attachmentSizeLabel` logic | **Unit formatting localization:** File size logic uses hardcoded English "MB" / "KB" and doesn't localize numbers. | Use `Intl.NumberFormat` for file sizes if supporting multiple locales, or ensure consistent formatting. |
