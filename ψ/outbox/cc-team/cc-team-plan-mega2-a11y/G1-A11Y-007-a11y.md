<!-- cc-team deliverable
 group: G1 (a11y division)
 member: A11Y-007 role=a11y model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":999,"completion_tokens":4197,"total_tokens":5196,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":3551,"image_tokens":0},"cache_creation_input_tokens":0} | 79s
 generated: 2026-06-13T12:09:47.011Z -->
| severity | location | issue | fix |
|---|---|---|---|
| **High** | `familyStyles` (bg colors) | **Color Contrast Failure:** White text (`text-white`) on 500-level Tailwind backgrounds (e.g., `bg-amber-500`, `bg-sky-500`) fails WCAG AA contrast requirements (ratios range from ~2.1:1 to 3.2:1). | Use darker background shades (e.g., `bg-amber-600`, `bg-sky-700`, `bg-indigo-700`) or adjust text color to ensure a minimum 4.5:1 contrast ratio for normal text. |
| **Medium** | `<div>` (`aria-label`) | **Insufficient Screen Reader Context:** `aria-label={`${family} agent`}` only announces the family (e.g., "gemma agent") and omits the specific model version. If unrecognized, it announces "default agent". | Include the full model name for precise context: `aria-label={`AI Agent: ${model}`}` or `aria-label={`${model} (${family} family)`}`. |
| **Medium** | `<span>` (inner text) | **Potential Redundant Announcements:** While `role="img"` typically creates a leaf node, some browser/AT combinations may still expose the inner text (`style.initial`), reading "G gemma agent". | Add `aria-hidden="true"` to the inner `<span>` to strictly enforce that it is ignored by assistive technologies. |
| **Medium** | `active` state (`ring-offset-2`) | **Dark Mode Visual Glitch (UX/a11y):** `ring-offset-2` defaults to a white offset color in Tailwind. On dark backgrounds or in dark mode, this creates a harsh white gap that breaks the UI and reduces visibility. | Replace `ring-2 ring-offset-2` with `outline outline-2 outline-offset-2` (which doesn't use an offset color), or explicitly set `ring-offset-transparent`. |
| **Low** | `familyStyles` (ring colors) | **Low Contrast Active Indicator:** 300-level ring colors (e.g., `ring-sky-300`) have low contrast against light backgrounds, making the "active" state difficult to perceive for low-vision users. | Use 400 or 500-level shades for the ring (e.g., `ring-sky-500`) to ensure the active indicator is clearly visible across different background themes. |
| **Low** | Component API / `<div>` | **Redundancy if Name is Present:** If the agent's name is already rendered in an adjacent text element (e.g., a chat header), announcing the avatar creates screen reader verbosity. | Add an `isDecorative` boolean prop. If true, apply `aria-hidden="true"` and remove `role="img"` to hide the avatar from the accessibility tree. |
