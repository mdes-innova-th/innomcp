<!-- cc-team deliverable
 group: G1 (a11y division)
 member: A11Y-009 role=a11y model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":2435,"completion_tokens":2145,"total_tokens":4580,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1066,"image_tokens":0},"cache_creation_input_tokens":0} | 32s
 generated: 2026-06-13T12:08:53.625Z -->
```markdown
| Severity | Location                                      | Issue                                                                                       | Fix |
|----------|-----------------------------------------------|---------------------------------------------------------------------------------------------|-----|
| **High**   | `STATUS_CONFIG` via `dot` emoji               | Status indicators rely exclusively on emoji (🟢🔵🟡🔴) without any textual equivalent. Screen readers may ignore them, read them inconsistently, or announce them as "green circle" without meaning. | Replace emoji with a styled `<span>` + visually hidden text or an `aria-label`. Example: `<span aria-label="Online" role="img">🟢</span>` or use an `<svg>` with `aria-hidden="true"` and adjacent accessible text. |
| **High**   | Entire leaderboard container (assumed)        | If the leaderboard is built with `<div>` elements instead of native `<table>`, it becomes a **non-semantic** table that screen readers cannot navigate by rows/columns. The markup likely lacks `role="table"`, `role="row"`, `role="columnheader"`, and `role="cell"`. | Use a native `<table>` element with proper `<thead>`, `<th>` (with `scope="col"`), and `<tbody><tr><td>`. If a `<table>` is absolutely unsuitable, implement the ARIA grid pattern with explicit roles and keyboard navigation. |
| **Medium** | `getProviderBadge` / badge styling            | Text inside provider badges (e.g., `text-purple-700` on a `bg-purple-500/15` background) may not meet **WCAG 1.4.3 contrast minimum** (4.5:1 for normal text). The 15%-opacity background creates an unpredictable foreground contrast depending on the underlying page background. | Calculate contrast ratios. Either darken the background or lighten the text so the badge text remains ≥4.5:1 against its immediate background. Consider using a solid (non-opacity) background colour that is carefully paired with the text colour. |
| **Medium** | Sorting controls (assumed)                    | If column headers are clickable for sorting, they are likely implemented as `<div>` or `<span>` with `onClick` but missing **`role="button"`, `tabIndex="0"`, and keyboard handlers** (`onKeyDown` for Enter/Space). | Add `role="button"`, `tabIndex="0"`, and handle `Enter` and `Space` key presses. Also provide an `aria-label` like "Sort by Name" and `aria-sort` to indicate current sort direction. |
| **Medium** | Dynamic data updates / loading state          | There is **no `aria-live` region** to announce when leaderboard data refreshes or when loading completes. Screen reader users won't be notified. | Wrap the table container in `<section aria-live="polite" aria-atomic="true">` or use `aria-busy="true"` on the table while loading. Provide a visually hidden loading message ("Loading leaderboard…") and an error announcement with `role="alert"`. |
| **Medium** | Column‑visibility toggles (`OPTIONAL_COLS`)   | If checkboxes or toggle buttons exist to show/hide columns, they likely lack accessible names and are not associated with the columns they control. | Label each toggle with the column name (e.g., `<input type="checkbox" id="col-winrate" aria-label="Show win rate column">`). Use `aria-controls` to point to the column header element. |
| **Low**    | Provider/status colour reliance               | Provider colours (`PROVIDER_COLORS`) and status badge classes convey meaning through colour alone (e.g., green for Online, rose for Offline). Users with colour blindness may not distinguish them. | Supplement colour with a visible text label (already present in `STATUS_CONFIG.label`) and consider adding an icon or shape indicator. Ensure the text label is always visible, not just the coloured dot. |
| **Low**    | `resolveBackendUrl` fallback                  | The hard‑coded fallback URL `http://localhost:3015` is used for API calls. This is not an a11y issue per se, but in a production build it could fail silently. | Replace with environment‑driven configuration and a meaningful error message if the backend is unreachable. |
| **Low**    | `formatLatency` and `formatTime`              | No accessibility issues with the functions themselves, but if the returned values are used in the UI, they are purely presentational. | If latency values are critical for understanding agent performance, consider providing a **text alternative** (e.g., "200 milliseconds") via `aria-label` on the cell. |
```

*Note:* The audit assumes the component renders a grid/table based on the imported `LeaderboardCard` and typical leaderboard patterns. A full review would require the complete component JSX to identify all interactive elements, focus management for panels (`ProviderHistoryPanel`), and any images needing `alt` text.
