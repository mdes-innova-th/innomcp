<!-- cc-team deliverable
 group: G1 (a11y division)
 member: A11Y-025 role=a11y model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":2913,"completion_tokens":4113,"total_tokens":7026,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":3152,"image_tokens":0},"cache_creation_input_tokens":0} | 55s
 generated: 2026-06-13T12:15:22.791Z -->
```markdown
| Severity   | Location | Issue | Fix |
|------------|----------|-------|-----|
| **Critical** | `SlashCommandMenu` | No keyboard support: arrow‑key navigation, Escape to close, Home/End, or focus management. Buttons are not semantic menu items. Screen‑reader users cannot operate the menu. | Add `role="menu"` to the container, `role="menuitem"` to each `<button>`, handle `onKeyDown` (ArrowUp/Down, Home/End, Escape), manage focus (move focus to first item on open, return to textarea on close), set `tabIndex={-1}` on all items with roving index. |
| **Critical** | `ChatInputEnhanced` (textarea) | The slash popup opens without any screen‑reader announcement or programmatic association. Textarea lacks `aria-expanded`, `aria-haspopup`, and `aria-controls`. | Add `aria-expanded={slashMenuVisible}`, `aria-haspopup="menu"`, and point `aria-controls` to the menu’s `id`. |
| **High**   | `SlashCommandMenu` / `ChatInputEnhanced` | `onClose` is never called via Escape key or click outside, so the menu will stay open until an item is selected. | Add a `useEffect` that listens for `keydown` and calls `onClose` on Escape. Use a `ref` and `mousedown`/`focus` outside detection to close. Return focus to textarea on close. |
| **High**   | `ProviderStatusBar` | `text-gray-400` on `bg-gray-950/80` has a contrast ratio of ~2.6:1, failing WCAG AA (minimum 4.5:1). Text is hard to read for low‑vision users. | Change to `text-gray-200` or `text-gray-300` (or use a lighter background) to meet at least 4.5:1 contrast. |
| **Medium** | `MDESStreamIndicator` | The streaming status message is not announced to assistive technology. Screen‑reader users may not know a response is being generated. | Add `role="status"` and `aria-live="polite"` to the container `<div>`, ensuring the message is announced when it appears. |
| **Medium** | `MDESStreamIndicator` | Decorative animated dots (ping circles) are exposed to screen readers as empty spans, creating noise. | Add `aria-hidden="true"` to the containing `<span>` or each inner `<span>`. |
| **Medium** | `ComposerEnhancedBar` | Inline SVG icons in buttons and the agent‑count `<div>` have no `aria-hidden` or `focusable="false"`, causing screen readers to read the SVG paths. | Add `aria-hidden="true"` and `focusable="false"` to every `<svg>` element. |
| **Medium** | `SlashCommandMenu` | No focus‑trap when the menu is open. Pressing Tab can move focus outside the menu, breaking the expected keyboard experience. | Implement a focus trap while `visible` is true (e.g., keep focus within the menu container until Escape or selection). Alternatively, use a non‑modal pattern where Tab closes the menu and advances focus. |
| **Low**    | `SlashCommandMenu` | The heading “คำสั่ง” is a `<p>` instead of a semantic heading, making it harder for screen‑reader users to navigate by regions. | Change to `<h2 id="menu-heading">` and reference it with `aria-labelledby="menu-heading"` on the menu container. |
| **Low**    | `ComposerEnhancedBar` | The group of action buttons lacks a toolbar landmark, making it harder to navigate by landmark. | Wrap the buttons in a `<div role="toolbar" aria-label="Composer actions">`. |
| **Low**    | `ChatInputEnhanced` | The slash‑detection logic (`input.startsWith("/")`) triggers immediately, which can cause visual flicker as the user types or backspaces. | Debounce or check that there are additional non‑whitespace characters after the slash to reduce false activations. |

*Note: The ChatInput inner component (not shown) may also need a visible label properly associated with the textarea via `<label>` or `aria-label`. Verify that association is present.*
