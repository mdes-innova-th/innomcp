<!-- cc-team deliverable
 group: G2 (a11y division)
 member: A11Y-038 role=a11y model=moonshotai/Kimi-K2.6
 finish_reason: length | tokens: {"prompt_tokens":2185,"completion_tokens":5000,"total_tokens":7185,"prompt_tokens_details":{"cached_tokens":72,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":4671,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 65s
 generated: 2026-06-13T12:19:54.263Z -->
| severity | location | issue | fix |
|---|---|---|---|
| **Critical** | `StatCard` return | Interactive card is a non-semantic `<div>` with `onClick`; completely unreachable via keyboard and lacks screen-reader role/semantics. | Replace with `<button>` (or add `role="button"`, `tabIndex={0}`, `onKeyDown` handling for Enter/Space) and ensure a visible `:focus` outline. |
| **High** | `DashboardView` polling `useEffect` | Auto-refresh every 10 s updates stats/tasks silently; assistive technology receives no announcement of changing values. | Add an `aria-live="polite"` region summarizing changes (e.g., “2 tasks running”) or set `aria-live="polite"` on the stats container with `aria-atomic="false"`. |
| **High** | `handleExportLog` catch | Native `alert()` traps screen-reader focus, blocks navigation, and does not return focus to the trigger on dismiss. | Use an in-app toast/notification with `role="alert"` and `aria-live="assertive"`; return focus to the Export button after the user dismisses it. |
| **High** | `StatCard` label / sub text | `text-muted-foreground/70` at `10px`/`10.5px` fails WCAG 2.1 AA contrast (4.5:1) and is below a readable font size. | Increase font size to ≥12 px (ideally 16 px) and verify contrast ratio ≥ 4.5:1 against the background. |
| **High** | `<MotherStatusBar motherActive … />` | Prop is passed as JSX boolean shorthand (`motherActive` = `true`), ignoring the actual `motherActive` variable; screen readers may announce the wrong status. | Pass the variable explicitly: `motherActive={motherActive}`. |
| **Medium** | `StatCard` icon `<span>` | Raw emoji string lacks `aria-hidden` or text alternative; screen readers may emit verbose or meaningless descriptions. | Add `aria-hidden="true"` if decorative, or wrap in `<span role="img" aria-label="Description">`. |
| **Medium** | `STATUS_STYLE` map | Task status (`completed`/`running`/`failed`) is conveyed solely by color (emerald/blue/rose); indistinguishable for color-blind users. | Supplement color with visible text labels, icons + text, or distinct patterns; do not rely on color alone. |
| **Medium** | Loading skeleton container | Skeleton state has no `aria-busy` or `aria-live`; screen readers may traverse and read empty placeholder elements. | Wrap the loading container in `<div aria-busy="true" aria-live="polite">` and announce “Loading dashboard” once. |
| **Medium** | `DashboardView` load transition | No focus management when real content replaces skeletons; keyboard/screen-reader focus may
