<!-- cc-team deliverable
 group: G2 (a11y division)
 member: A11Y-026 role=a11y model=deepseek/deepseek-v4-flash
 finish_reason: stop | tokens: {"prompt_tokens":2455,"completion_tokens":1517,"total_tokens":3972,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":792,"image_tokens":0},"cache_creation_input_tokens":0} | 20s
 generated: 2026-06-13T12:14:52.166Z -->
| Severity | Location | Issue | Fix |
|---|---|---|---|---|
| **Critical** | APOD image section (likely `<img>` in truncated part) | Missing `alt` attribute on NASA APOD image. Screen readers will describe the image as “image” or rely on file name, providing no context. | Add `alt` attribute with a meaningful description (e.g., `alt={structuredContent?.title || "NASA Astronomy Picture of the Day"}`). For decorative images, use `alt=""`. |
| **Serious** | Copy chart code button (in `handleCopyChartCode`) | Button has no accessible label—likely only an icon or inline SVG without `aria-label`. Keyboard users won’t understand its purpose. | Add `aria-label="Copy chart SVG code"` to the button (or visible text via `sr-only` span). Also use `role="button"` and ensure it is focusable. |
| **Serious** | Download chart button (in `handleDownloadChart`) | Same issue as copy button: no accessible name, no visible text, and no `aria-label`. | Add `aria-label="Download chart as SVG"` and consider a tooltip or visible label. |
| **Serious** | “เปิดภาพขนาดเต็ม” link in APOD header | Link contains only an SVG icon with `title` attribute, which is not reliably exposed by screen readers. No visible text makes it inaccessible. | Replace `title` with an `aria-label` (or better, visible text). For example: `<a aria-label="Open full-size image in new tab" ...>`. Keep the decorative SVG with `aria-hidden="true"`. |
| **Moderate** | Color‑contrast risks | Custom violet/sky gradients (e.g., `text-violet-700`, `bg-violet-500/12`) may have insufficient contrast against light/dark backgrounds, especially for smaller fonts. | Use WCAG‑compliant colors. Check against `#6b21a8` (violet‑700) on white/cream backgrounds – may need tweaking. Add a `prefers-contrast: more` media query to increase contrast. |
| **Moderate** | Copied state for copy button | After copying, `copiedChart` is set but the button’s visual change (if any) is not communicated to screen readers. | Use an `aria-live="polite"` region to announce “Chart code copied” or toggle the button’s text to “Copied!” with an `aria-label` update. Alternatively, use a brief `role="status"` message. |
| **Minor** | Chat message wrapper `<div>` | The outermost `<div>` has no semantic role. For a chat application, each message should be marked as an article or list item to help navigation. | Add `role="article"` (or `<article>`) to the wrapper, plus `aria-label` if necessary to indicate sender (e.g., `aria-label="Assistant message"`). |
| **Minor** | Animation indicator (`isAnimating`) | The blinking cursor that signals ongoing streaming may not be announced by screen readers, leaving users uncertain about the message’s completeness. | While `isAnimating` is true, add an `aria-busy="true"` attribute to the message container, and announce “Message is being generated” via a live region. Remove `aria-busy` when streaming completes. |
