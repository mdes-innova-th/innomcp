<!-- cc-team deliverable
 group: G2 (a11y division)
 member: A11Y-029 role=a11y model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":2175,"completion_tokens":2525,"total_tokens":4700,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1687,"image_tokens":0},"cache_creation_input_tokens":0} | 36s
 generated: 2026-06-13T12:15:58.759Z -->
| Severity | Location | Issue | Fix |
|----------|----------|-------|-----|
| **Critical** | `useScrollBehavior` – scroll container (implied `<div>` with `messagesRef`) and `unreadCount` state | New messages are not announced to screen readers. No `aria-live` region is used on the chat feed, and `unreadCount` state changes are not communicated programmatically. | Add `aria-live="polite"` and `role="log"` to the messages container element. Create a hidden `<div aria-live="polite" aria-atomic="true">` that announces the unread count when it changes, e.g., "2 new messages". |
| **High** | `useScrollBehavior` – scroll-to-bottom logic (around line 166) | Auto‑scrolling to the bottom when new content arrives may cause disorientation for keyboard and screen‑reader users because focus is not managed. Focus can remain on an earlier message, causing the viewport to jump unexpectedly. | After adding new content and auto‑scrolling, programmatically move focus to the latest message or to a "New messages" button using `element.focus()`. If the user is not at the bottom, keep focus stable and notify via the live region. |
| **High** | `useScrollBehavior` – scroll container (element referenced by `messagesRef`) | The scrollable chat area lacks a keyboard‑accessible way to scroll. Non‑mouse users cannot focus the container to use arrow keys or Page Up/Down. | Add `tabindex="0"` to the messages container so it receives keyboard focus. Consider wrapping it in a region with a label like `aria-label="Chat conversations"` to help screen readers identify the area. |
| **Medium** | `useScrollBehavior` – auto‑scroll effect (ResizeObserver callback, line ~160‑170) | No respect for the user’s `prefers-reduced-motion` setting. Users with motion sensitivity may find automatic scrolling uncomfortable or disorienting. | Check `window.matchMedia('(prefers-reduced-motion: reduce)').matches`. If true, disable auto‑scroll or change it to instant `scrollTop` without smooth behaviour. Allow users to opt‑out of auto‑scroll altogether. |
| **Medium** | `useScrollBehavior` – unread count state (lines 116‑117) | The unread count is purely visual. If the implementing component renders it as a badge or button, it may lack accessible name and role (e.g., a badge without `aria-label` or a button without `aria-pressed` state). | Ensure the unread count is rendered inside a `<button>` with `aria-label="New messages (3)"` or as a `role="status"` element with appropriate text. |
| **Medium** | Implied usage – message list rendering (based on `useChatMessages`) | The messages are likely rendered inside `<div>` elements without semantic list markup. Screen readers may not convey the structure correctly, and the total number of messages is not announced. | Render the chat messages inside a `<ul role="list">` with each message as an `<li>`. Use `role="listitem"` on each message. |
| **Low** | Implied usage – date separators in `groupMessagesByDate` | Date separators are computed but the component may render them without a semantic `<time>` element, making it harder for assistive technology to understand the timeline. | Wrap date separators in `<time datetime="YYYY-MM-DD">` with `aria-hidden="true"` on decorative elements or use `aria-label` on the group container. |

**Additional notes:** The hooks themselves are not directly responsible for the markup, but the issues above are introduced by the expected integration of these hooks into a chat UI. A complete audit would also require reviewing the JSX that uses `useChatMessages`, `useAgentStats`, and `useScrollBehavior`.
