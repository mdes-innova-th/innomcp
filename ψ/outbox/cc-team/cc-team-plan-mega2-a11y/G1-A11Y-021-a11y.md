<!-- cc-team deliverable
 group: G1 (a11y division)
 member: A11Y-021 role=a11y model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":2635,"completion_tokens":3395,"total_tokens":6030,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2224,"image_tokens":0},"cache_creation_input_tokens":0} | 49s
 generated: 2026-06-13T12:15:20.325Z -->
| Severity | Location | Issue | Fix |
|----------|----------|-------|-----|
| **Critical** | Overlay `<div>` and content `<div>` wrapping the modal (`bg-white rounded-2xl...`) | No accessible dialog role. The modal is just `<div>`s, so screen readers cannot identify it as a dialog or modal, and landmarks/roles are missing. | Change the outer container to a `<dialog>` element or add `role="dialog"`, `aria-modal="true"`, and `aria-labelledby` referencing the heading’s `id`. Use `ref` to manage focus when opened. |
| **Critical** | Entire component (no `useEffect` for key listeners and focus) | No focus trap, no Escape key handler, and focus is not moved into the modal when it opens. Keyboard users can tab to elements behind the overlay. | Add a `useEffect` that listens for `keydown` to call `onClose` on Escape. Implement a focus trap (or use a library like `focus-trap-react`). On mount, move focus to the close button or the first interactive element. On unmount, return focus to the trigger element. |
| **Critical** | Loading indicator (`<div className="text-center...">`) | Loading state is not announced to assistive technology; screen readers do not know content is being loaded. | Wrap the loading message in an element with `role="status"` and `aria-live="polite"`, e.g., `<div role="status" aria-live="polite" className="text-center py-8 text-gray-500">กำลังคำนวณ...</div>`. Also set `aria-busy="true"` on the dialog region while loading. |
| **High** | Export buttons (`<button key={fmt.key} ...>`) | Accessible name is derived from all inner text, including emoji icon, label, description, and size badge. Screen readers read the whole chunk, which can be verbose and confusing: “📄 PDF พิมพ์เป็น PDF ผ่านเบราว์เซอร์ ~1.2 KB”. | Add a clear `aria-label` that concisely identifies the action, e.g., `aria-label={`ส่งออกเป็น ${fmt.label} — ขนาดประมาณ ${sizes[fmt.key]}`}`. Mark decorative elements (icon and size badge) with `aria-hidden="true"`. |
| **High** | Size badge `<div className="text-xs text-gray-400 bg-gray-100 ...">` | Color contrast is likely insufficient for small text (gray-400 on gray-100). WCAG AA requires 4.5:1 for text this small. | Use darker text color, e.g., `text-gray-600` or `text-gray-700`, and maintain a background that provides at least 4.5:1 ratio. For a badge, `text-gray-700 bg-gray-200` or `text-blue-700 bg-blue-50` would work. |
| **High** | Footer text `<div className="mt-4 text-xs text-gray-400 ...">` | Same contrast issue: gray-400 on white background fails minimum contrast for small text. | Change footer text to a darker gray, e.g., `text-gray-600` or `text-gray-700`. |
| **Medium** | Decorative emoji `<span className="text-2xl">{fmt.icon}</span>` | Emoji are not hidden from screen readers; they will be announced (e.g., “page with text”). This clutters the button’s accessible name. | Add `aria-hidden="true"` to the `<span>` so the emoji is purely decorative. Rely on the button’s `aria-label` for the name. |
| **Medium** | Print (PDF) export flow (`handleExport` for `"pdf"`) | When printing, a new window is opened and `print()` is called without any announcement or feedback to screen readers. Users may not be aware a print dialog appeared. | Before opening the window, announce the action with a live region: “กำลังเปิดหน้าต่างพิมพ์…” After the print dialog is triggered, optionally announce completion. Ensure the print HTML’s `lang` attribute is set (it is) and the new window is accessible. |
| **Medium** | Export format list wrapper `<div className="space-y-3">` | The list of export options is not marked as a list, which can reduce navigability for screen reader users (e.g., quick navigation by lists). | Wrap the buttons in a `<ul>` with `role="list"` (or use a `<ul>` natively) and each button in a `<li>`. This provides list semantics and count. |
| **Low** | Close button `aria-label="ปิด"` | The label is generic (“close”) and does not indicate what is being closed. | Change to `aria-label="ปิดหน้าต่างส่งออกการสนทนา"` for more context. |

**Additional recommendations**
- Use `<dialog>` element with `showModal()` and `close()` for native backdrop and focus management.
- When the modal opens, set `inert` on the main app container or use `aria-hidden="true"` on sibling content to prevent background interaction.
- Ensure the generated PDF HTML has proper semantics (e.g., headings, `main` landmark) and that the printed page itself meets accessibility standards.
- Test the modal with screen readers (VoiceOver, NVDA) under keyboard and screen-reader navigation.
