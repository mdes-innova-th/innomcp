<!-- cc-team deliverable
 group: G1 (a11y division)
 member: A11Y-002 role=a11y model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":2138,"completion_tokens":3805,"total_tokens":5943,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2656,"image_tokens":0},"cache_creation_input_tokens":0} | 75s
 generated: 2026-06-13T12:07:46.563Z -->
| severity | location | issue | fix |
|---|---|---|---|
| **Critical** | `showAddModal`, `showEditModal`, `showSuccessModal` | **Focus Management & Screen Reader Traps:** Modals likely lack focus trapping, initial focus placement, and focus restoration. Keyboard and screen reader users can tab behind the modal into the background content. | Use a robust accessible modal library (e.g., Radix UI, Headless UI) or manually implement a focus trap. Add `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, and restore focus to the trigger button on close. |
| **High** | `FaPlus`, `FaTrash`, `FaPen`, `FaCopy`, etc. | **Missing Accessible Names:** Icons used inside buttons without accompanying text lack accessible names. Screen readers will announce them as empty "button" or ignore them entirely. | Add an `aria-label` to the parent button (e.g., `aria-label="Delete API key"`). Alternatively, add visually hidden text inside the button and set `aria-hidden="true"` on the `<Fa... />` icon. |
| **High** | `handleRevokeApiKey`, `handleDeleteApiKey`, `catch` blocks | **Native `confirm()` and `alert()` Dialogs:** Native browser dialogs block the main thread, create a jarring UX, and can disrupt the screen reader's virtual cursor/focus context when dismissed in a React SPA. | Replace with custom accessible confirmation modals (with focus trapping) and use toast notifications with `role="status"` or `aria-live="polite"` for success/error alerts. |
| **High** | `formError`, `handleAddApiKey`, `handleEditApiKey` | **Form Error Announcements & Association:** Form errors are likely displayed visually but not announced to screen readers, nor programmatically associated with the invalid input fields. | Add `role="alert"` or `aria-live="assertive"` to the error message container. Apply `aria-invalid="true"` and `aria-describedby="error-id"` to the specific input field that failed validation. |
| **High** | `paginatedApiKeys` rendering | **Non-Semantic Data Markup:** API key lists are often rendered using nested `div`s, making it impossible for screen reader users to navigate data rows and columns efficiently. | Use a semantic `<table>` with `<thead>`, `<tbody>`, `<th scope="col">`, and `<th scope="row">`. If using CSS Grid/Flexbox, apply `role="table"`, `role="row"`, `role="columnheader"`, and `role="cell"`. |
| **Medium** | `currentPage`, `totalPages`, Pagination UI | **Pagination Accessibility:** Pagination controls often lack semantic grouping, active state indication, and proper disabled states, confusing keyboard and screen reader users. | Wrap controls in `<nav aria-label="Pagination">`. Add `aria-current="page"` to the active page button. Ensure "Previous/Next" buttons have `aria-label`s and use the `disabled` attribute (not `hidden`) when at bounds. |
| **Medium** | `ApiKey.status` ("active", "inactive", "revoke") | **Color Contrast & Use of Color:** Relying solely on color (e.g., green/red/yellow badges) to convey status fails WCAG 1.4.1 (Use of Color) and poses severe color-contrast risks for visually impaired users. | Ensure text/background contrast meets WCAG AA (4.5:1). Add text labels, distinct icons (e.g., `FaCheck`, `FaTimesCircle`), or patterns in addition to color to indicate status. |
| **Medium** | `isLoading`, `LoadingSpinner` | **Loading State Announcements:** Loading states are purely visual. Screen readers are not notified when data is fetching or when it finishes, leading to a "dead" experience. | Add `aria-live="polite"` and `role="status"` to the loading spinner container (e.g., "Loading API keys..."). Alternatively, apply `aria-busy="true"` to the main data container while fetching. |
| **Medium** | `copiedKeyId`, Copy to Clipboard UI | **Missing Copy Feedback for Screen Readers:** Visual feedback (like changing the icon to `FaCheck`) is not announced to assistive technology users. | Implement a visually hidden live region (`<span aria-live="polite" className="sr-only">`) that updates with "API key copied to clipboard" when the action succeeds. |
| **Medium** | Form Inputs (`apiKeyName`, `expireDate`, etc.) | **Missing Labels & Associations:** Inputs may rely solely on placeholders instead of explicit labels, which disappear on focus and are not reliably read by all screen readers. | Ensure every input has a corresponding `<label htmlFor="...">` or `aria-label`. Use `aria-describedby` to link inputs to helper text (e.g., date formats) or specific error messages. |
| **Low** | `itemsPerPage` Dropdown | **Custom Dropdown Keyboard Navigation:** If the items-per-page selector is a custom component, it likely lacks standard keyboard navigation (Arrow keys, Enter, Escape) and ARIA roles. | Use a native `<select>` element for guaranteed accessibility. If a custom dropdown is required, implement `role="listbox"`, `role="option"`, `aria-expanded`, and full keyboard event handling. |
