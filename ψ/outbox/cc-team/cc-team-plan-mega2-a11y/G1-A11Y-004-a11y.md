<!-- cc-team deliverable
 group: G1 (a11y division)
 member: A11Y-004 role=a11y model=zai-org/GLM-5.1
 finish_reason: stop | tokens: {"prompt_tokens":507,"completion_tokens":2615,"total_tokens":3122,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1812,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 19s
 generated: 2026-06-13T12:07:17.510Z -->
| severity | location | issue | fix |
| :--- | :--- | :--- | :--- |
| **Critical** | Outer `<div>` | **Color-Contrast Risk**: `text-muted-foreground/60` at `text-[10.5px]` almost certainly fails WCAG AA (requires 4.5:1 for normal text). Low opacity on small text is extremely difficult to read. | Remove the `/60` opacity modifier and ensure the hex color passes a 4.5:1 contrast ratio against the background. Increase font size to at least `text-xs` (12px) for better readability. |
| **High** | Green dot `<span>` | **Missing Accessible Name**: The green pulsing dot conveys "active/healthy" visually, but screen readers will skip this empty `<span>`. | Add visually hidden text: `<span className="sr-only">Active:</span>` or add `aria-label="Active"` to the span. |
| **High** | Outer `<div>` | **Non-Semantic Markup & Missing Live Region**: The component uses a generic `<div>` for status updates. When the provider appears or latency updates, screen readers won't announce the change. | Change `<div>` to `<div role="status" aria-live="polite">`. This tells assistive tech that it's a live status region and updates are announced automatically. |
| **Medium** | Warning emoji `<span>` | **Missing Alt/Role on Emoji**: The ⚠️ emoji is used to convey a warning visually, but emojis can be read inconsistently by screen readers (often ignored or read awkwardly) and lack semantic structure. | Replace the emoji with a proper icon component, or wrap it: `<span role="img" aria-label="Warning">⚠️</span>`. |
| **Medium** | Amber latency `<span>` | **Color-Contrast Risk**: `text-amber-500` frequently fails WCAG AA contrast ratios on light backgrounds, and relying solely on color to indicate "slow" violates WCAG 1.4.1 (Use of Color). | Ensure `text-amber-500` passes contrast on the background. The addition of the ⚠️ icon helps satisfy 1.4.1, but ensure the icon has an accessible name (see above). |
| **Medium** | Latency `<span>`s | **Missing Context for Screen Readers**: "1234ms" might be read as "one thousand two hundred thirty-four milliseconds" or "twelve thirty-four ms" without clear context. | Add visually hidden context: `<span className="sr-only">Latency: </span>{latencyMs}ms`. |
| **Low** | `useEffect` fetch | **Silent Error Handling**: `.catch(() => {})` swallows errors. If the API fails, the component silently returns `null`. While not a strict A11y violation, it hides system states from all users. | Set an error state and display a fallback UI (e.g., "Status unavailable") so users aren't left wondering if the feature is missing or broken. |
| **Low** | Outer `<div>` | **Font Size / Readability (UX)**: `text-[10.5px]` is significantly smaller than the standard minimum readable size (usually 12px-14px). | Increase to `text-xs` (12px) or `text-sm` (14px) to improve readability for users with low vision or cognitive load issues. |
