<!-- cc-team deliverable
 group: G2 (a11y division)
 member: A11Y-045 role=a11y model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":1439,"completion_tokens":2839,"total_tokens":4278,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2153,"image_tokens":0},"cache_creation_input_tokens":0} | 47s
 generated: 2026-06-13T12:24:44.308Z -->
| Severity | Location | Issue | Fix |
|----------|----------|-------|-----|
| **High** | Reply `<input>` (line ~86) | Missing accessible label for the text input. The placeholder alone is not a label; screen-reader users may not perceive the field’s purpose when focused. | Add a `<label>` element (visually hidden with `sr-only` class) and associate it with `htmlFor`, or use `aria-label="ข้อความตอบกลับใหม่"` on the `<input>`. |
| **Medium** | Post list container (lines ~56‑79) | Posts are rendered as plain `<div>` elements; no semantic list structure. Assistive technology cannot navigate by list items or announce list boundaries. | Replace the outer `<div className="flex-1 space-y-3">` with a `<ul>` and each post `<div>` with an `<li>` (or wrap in `<article>`). Keep the same styling classes. |
| **Medium** | Vote `<button>` inside each post (line ~71) | Button label is only `👍 {votes} โหวต` – no indication of which post is being voted on when navigating by buttons out of context. | Add `aria-label={`โหวตให้ ${post.agent}`}` to the button, or hide the emoji with `aria-hidden` and use a `<span>` for the agent name that is referenced via `aria-labelledby`. |
| **Medium** | Entire post list / form submit handler | After a new post is added, no feedback is given to screen-reader users and focus stays on the (now empty) input. The user may not notice the new post. | Wrap the posts list in a container with `aria-live="polite"` and update a hidden message like “โพสต์ใหม่ถูกเพิ่ม” (or programmatically move focus to the newly added post using a ref). |
| **Low** | Avatar `<span>` (line ~62) | Emojis such as 👑, 💻 are decorative but are exposed to screen readers, which may announce distracting descriptions (e.g., “crown”). | Add `aria-hidden="true"` to the `<span>{post.avatar}</span>` to remove it from the accessibility tree. |
| **Medium** | Post text container & vote area | Small text (11px) with `text-foreground/85` and `bg-background/25` (both use opacity) likely fails WCAG contrast ratios against unknown background. For example, #aaaaaa at 85% opacity yields ~#d4d4d4, which can have a low ratio. | Replace opacity-based utilities with solid colour values that meet 4.5:1 contrast for normal text. Use a contrast checker or define `text-foreground-muted` with a solid high-contrast colour. |
| **Medium** | Vote button & emoji | The thumbs-up emoji 👍 inside the button is read by screen readers as “thumbs up” before the number, which could be verbose or unclear. | If the button’s accessible name is provided via `aria-label`, add `aria-hidden="true"` to the emoji to prevent double speaking, or replace it with a CSS background image. |
