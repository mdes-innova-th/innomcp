<!-- cc-team deliverable
 group: G1 (a11y division)
 member: A11Y-018 role=a11y model=moonshotai/Kimi-K2.6
 finish_reason: length | tokens: {"prompt_tokens":1482,"completion_tokens":5000,"total_tokens":6482,"prompt_tokens_details":{"cached_tokens":3,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":0,"image_tokens":0},"cache_creation_input_tokens":0} | 56s
 generated: 2026-06-13T12:12:09.760Z -->
The user wants an accessibility (a11y) and UX audit of a React/Next.js component (`ChatContextMenu.tsx`). They want the output in a table format with columns: severity | location | issue | fix.

 Let me analyze the component thoroughly for accessibility and UX issues.

 Component analysis:
 1. It's a context menu (right-click menu) rendered via portal to document.body
 2. It has keyboard navigation (ArrowDown, ArrowUp, Enter, Escape)
 3. It uses `role="menu"` and `role="menuitem"`
 4. It has animation states
 5. It handles click outside and right-click outside
 6. It adjusts position to avoid viewport overflow

 Issues I can identify:

 **Critical/High Severity:**
 1. **Focus trap/management**: When menu opens, focus is not moved to the menu. The menu has `tabIndex={-1}` but there's no `useEffect` to focus it when visible. Screen reader users won't know it appeared.
 2. **No `aria-label` or `aria-labelledby` on menu**: The menu has no accessible name.
 3. **No `aria-activedescendant` or roving tabindex**: The keyboard navigation sets `activeIndex` but doesn't move DOM focus (`tabIndex` stays -1 on all items). Screen readers won't know which item is active. Should use roving tabindex or aria-activedescendant.
 4. **Emoji as only icon/label**: Using emoji (📋, 🔄, etc.) without text alternatives or with text that includes emoji. The label includes emoji as text. Screen readers may read emoji descriptions (like "clipboard" or "counterclockwise arrows button") which might be confusing, but more importantly, the emoji is decorative and should be hidden from AT if text label exists, OR if emoji is the only thing, it needs proper labeling. Here the label includes emoji + text, so screen readers will announce both. But the emoji span is separate. Better to add `aria-hidden="true"` to emoji spans.
 5. **No `aria-haspopup`, `aria-expanded`, `aria-controls` on trigger**: The trigger element (not in this file but implied) would need these, but since this is the menu component itself, we should note that the component should receive focus when opened and ideally the trigger should be informed. However, the component itself doesn't manage focus return on close. When `onClose` is called (e.g., Escape), focus should return to the trigger element. But there's no trigger ref passed in.
 6. **Color contrast**: `text-gray-800` on `bg-white` is fine (approx 10:1). `hover:bg-indigo-50` with `text-gray-800` is fine. But the focus/active state only uses background color change with no outline/border. For keyboard users, the `bg-indigo-50` might not be sufficient if color perception is an issue. Need visible focus indicator (outline/ring).
 7. **Missing `aria-orientation`**: Not required but menu usually vertical (default).
 8. **Click handling on `li`**: The items are `<li>` elements with `onClick`. They are not `<button>` or `<a>`. While they have `role="menuitem"`, they won't be keyboard focusable individually (tabIndex=-1) and won't respond to Space key (only Enter). Native buttons respond to Space. Also, `menuitem` should support Enter, but Space is expected for buttons. Since these are actions, they should probably be `role="menuitem"` with click/enter handlers, but missing Space is a deviation from expected button behavior. Actually, ARIA menu patterns typically use Enter, not Space. But since these are not in a menubar, it's acceptable. However, using `<button>` inside `<li>` would be much better.
 9. **No `aria-hidden` or `inert` when closed**: The menu is removed from DOM when `rendered` is false, so that's fine. But during animation (opacity 0), it's still in DOM and focusable/screen-reader accessible. `opacity-0` doesn't remove from accessibility tree. Should add `hidden` or `inert` or `aria-hidden="true"` when not animateIn, or better yet, don't render until animation is needed. Actually, `rendered` controls mount, but when `animateIn` is false (closing), it's still in DOM with `opacity-0` for 200ms. During that time, it's still accessible. Should add `aria-hidden={!animateIn}` or `inert={!animateIn}` (if browser support acceptable) or `pointer-events-none` (doesn't remove from AT). Better: `visibility: hidden` or `aria-hidden="true"` when `!animateIn`.
 10. **Viewport overflow logic**: `window.innerWidth` and `window.innerHeight` are used during render. In Next.js, this might cause hydration mismatch if rendered server-side, but since it's a client component and only rendered when `visible` is true (likely after interaction), it's probably fine. But `window` might not be defined during initial render if not careful. However, this is more of a functional/Next.js issue than a11y.
 11. **Right-click outside**: `contextmenu` event closes the menu. Good.
 12. **Missing `useId` for menu and items**: No `id` attributes, so `aria-activedescendant` can't be implemented easily.
 13. **Menu should have `aria-labelledby` or `aria-label`**: Since it's a context menu, it might not have a visible label, so `aria-label="Message actions"` or similar would help.
 14. **Focus loss on unmount**: When menu closes, focus is lost to body because there's no return focus to trigger. This is a major issue.
 15. **MouseEnter resets activeIndex**: `onMouseEnter={() => setActiveIndex(-1)}` on the container resets keyboard selection on hover. This is weird UX. If I'm hovering and then press arrow keys, where does focus go? It should probably not reset, or should sync hover and keyboard.
 16. **No `aria-disabled`**: If any items were disabled, they'd need it, but none seem disabled here.
 17. **Portal to document.body**: Good for z-index, but missing `aria-hidden` handling if the rest of the app should be inert. For a context menu, usually you don't need to modalize, but sometimes it's recommended. Not strictly necessary for a context menu unless it's modal.
 18. **Emoji-only understanding**: The label text includes emoji at the start. Screen readers will announce "Clipboard copy" or similar depending on OS. The Thai text "คัดลอก" is fine. But the emoji span is separate from text span. If the emoji is purely decorative, it should have `aria-hidden="true"`.
 19. **No `alt` text**: Not applicable since no images, but emoji act like images. They are text content though.
 20. **Keyboard navigation loop**: ArrowDown from last goes to 0, ArrowUp from 0 goes to last. This is standard for menus, so fine.
 21. **Home/End keys**: Missing for jumping to first/last item. Nice to have but not critical.
 22. **Typeahead**: Missing (typing letters to jump to items). Nice to have for ARIA menus.
 23. **Positioning and screen readers**: The menu uses fixed positioning. Screen readers should handle this fine.
 24. **Click on li vs button**: The `li` has `onClick`. If a user has a screen reader and clicks (or triggers via voice), it works. But keyboard users only get Enter, not Space. Also, the item is not in the tab order (tabIndex=-1), which is correct for a menu (roving tabindex or aria-activedescendant pattern), but since focus isn't on the menu at all, it's moot.

 Let me structure the table. Severity levels: Critical, High, Medium, Low.

 Issues to include:

 1. **Critical - Focus management on open**: Menu doesn't receive focus when opened. Screen reader users won't perceive it. Fix: `useEffect` to focus menuRef when visible.
 2. **Critical - Focus loss on close**: When menu closes (Escape, click outside, action), focus is not returned to trigger element. Fix: Accept `triggerRef` or `onClose` should handle focus return; use `useEffect` cleanup to return focus.
 3. **High - Keyboard active item not exposed to AT**: `activeIndex` state changes visual styling but doesn't expose which item is active to screen readers because all items have `tabIndex={-1}` and there's no `aria-activedescendant`. Fix: Add `aria-activedescendant` on the menu pointing to active item `id`, or use roving `tabIndex` (move `tabIndex=0` to active item and `.focus()`).
 4. **High - Menu items are non-interactive elements**: `<li>` with `onClick` instead of `<button>`. Missing Space activation, native focus styling, and semantic behavior. Fix: Render `<button>` inside each `<li>` (or make `<li>` a button, though invalid HTML). Use `<button className="w-full text-left ...">` inside `<li>`.
 5. **High - Invisible during exit animation but still accessible**: `opacity-0` keeps menu in accessibility tree during 200ms exit. Fix: Add `aria-hidden={!animateIn}` or `inert={!animateIn}` or remove from DOM immediately without animation, or use `visibility: hidden`.
 6. **Medium - Decorative emoji exposed to screen readers**: Emoji icons are not hidden from AT, causing redundant/verbose announcements. Fix: Add `aria-hidden="true"` to the emoji `<span>`.
 7. **Medium - Missing accessible name on menu**: `role="menu"` has no `aria-label` or `aria-labelledby`. Fix: Add `aria-label="Message actions"` (or localized equivalent).
 8. **Medium - Insufficient focus indicator**: Active item only changes background color (`bg-indigo-50`) with no visible focus ring/outline. Fix: Add `focus-visible:ring` or `outline` to items (especially if using buttons).
 9. **Medium - Hover resets keyboard selection**: `onMouseEnter` on container resets `activeIndex` to -1, breaking continuity between mouse and keyboard interaction. Fix: Remove container `onMouseEnter` reset; sync hover with keyboard state or keep them independent.
 10. **Medium - Missing `aria-haspopup`/`aria-expanded` on trigger (implied)**: Since this component is the menu, the trigger element outside this file needs these. But as part of the component API, it should document/require trigger props or accept a trigger ref to set attributes. However, since the prompt asks to audit this component, I can note that the component should expose an ID or ref for the trigger. But maybe better to note as a component API issue: "Component does not provide trigger association props". Severity: Medium.
 11. **Low - Missing Home/End/Typeahead**: Standard ARIA menu pattern supports Home/End and typeahead. Fix: Add keyboard handlers for Home/End and character navigation.
 12. **Low - No `aria-controls` linking**: Menu and trigger aren't linked. Fix: Generate unique IDs and accept trigger `aria-controls`/`aria-haspopup` props.
 13. **Low - `window` access during render**: Could cause SSR issues in Next.js, though client component. Not strictly a11y but UX. I'll skip to keep it a11y focused.
 14. **Low - `key={index}`**: Using array index as key is okay here since items don't reorder, but if `isAI` changes, the list changes. Actually, since items change based on `isAI`, using index is risky if order changes. But not an a11y issue per se. I'll skip or mention briefly.

 Wait, the prompt specifically asks for: missing aria/roles/labels, keyboard nav, focus management, color-contrast risks, non-semantic markup, missing alt, screen-reader traps.

 Let me make sure I cover these categories.

 Let's refine the list to be comprehensive but not redundant.

 | Severity | Location | Issue | Fix |
 |---|---|---|---|
 | Critical | `useEffect` (visible) / `menuRef` | Focus is not moved to the menu when it opens, making it invisible to screen reader users and impossible to keyboard-navigate without first tabbing to it. | Add a `useEffect` that calls `menuRef.current?.focus()` when `visible` becomes true. |
 | Critical | `onClose` handlers / component API | Focus is not returned to the triggering element on close, causing focus loss and forcing screen reader/keyboard users to restart navigation from the top of the DOM. | Accept a `triggerRef` prop or call a `onCloseWithFocusReturn` callback; in a cleanup effect or `onClose`, move focus back to the trigger element. |
 | High | `<li>` menu items | Menu items use non-semantic `<li>` elements with `onClick` instead of `<button>`, so they are not recognized as interactive by native semantics, do not respond to Space key, and lack native focusability. | Wrap each item in a `<button>` (inside `<li>`) or use `<button>` directly, ensuring it handles both `onClick` and keyboard `Space`/`Enter`. |
 | High | `activeIndex` state / `role="menu"` | Keyboard navigation updates visual state only (`activeIndex`); screen readers are not notified because all items have `tabIndex={-1}` and there is no `aria-activedescendant`. | Implement roving `tabIndex` (set `tabIndex={0}` on active item and `.focus()`) OR add unique `id`s to items and `aria-activedescendant` on the menu container pointing to the active item. |
 | High | Exit animation (`animateIn` false) | Menu remains in the accessibility tree during its 200ms fade-out (`opacity-0`), allowing screen readers and sequential focus navigation to reach invisible items. | Add `aria-hidden={!animateIn}` (and ideally `inert={!animateIn}`) to the root `<div>` during the exit phase, or unmount immediately without exit animation. |
 | Medium | Emoji `<span>` elements | Decorative emoji icons are exposed to assistive technology, causing verbose or confusing announcements alongside the text label (e.g., "clipboard copy"). | Add `aria-hidden="true"` to each emoji `<span>` so only the text label is announced. |
 | Medium | Root `<div role="menu">` | The menu lacks an accessible name (`aria-label` or `aria-labelledby`), so screen reader users hear only "menu" without context. | Add `aria-label="Message actions"` (or localized string) to the root menu `<div>`. |
 | Medium | Active item styling (`bg-indigo-50`) | Keyboard focus/active state relies solely on a subtle background color change with no visible focus ring or outline, failing WCAG 2.4.7 Focus Visible and creating risk for low-vision users. | Add a high-contrast focus indicator such as `ring-2 ring-indigo-500 ring-offset-2` or `outline-2 outline-indigo-600` to active/focused items. |
 | Medium | `onMouseEnter` on root `<div>` | Hovering over the menu container resets `activeIndex` to `-1`, breaking the connection between keyboard and mouse interaction and disorienting users who switch input modes. | Remove the root `onMouseEnter` handler; instead, set `activeIndex` on individual item hover (`onMouseEnter` on `<li>`/button) to keep hover and keyboard states synchronized. |
 | Medium | Component props/API | No association between the menu and its trigger element; missing `aria-haspopup`, `aria-expanded`, `aria-controls`, and unique IDs prevents screen readers from understanding the relationship. | Generate a unique `menuId` (e.g., `useId()`), expose it to the parent so the trigger can set `aria-haspopup="menu"`, `aria-controls={menuId}`, and `aria-expanded={visible}`, and pass an `id` to the root `<div>`. |
 | Low | `handleKeyDown` | Standard ARIA menu keyboard patterns (Home, End, and typeahead character search) are not implemented, reducing efficiency for keyboard and screen reader users. | Add `Home` (first item), `End` (last item), and alphanumeric typeahead (focus first item starting with typed character) handlers. |

 I should double-check if there are any color contrast issues. `text-gray-800` on `bg-white` is #1f2937 on #ffffff = ~12.6:1 (pass). `text-gray-800` on `bg-indigo-50` (#eef2ff) is also fine. The issue is not contrast of text, but the focus indicator itself. The prompt mentions "color-contrast risks". The focus indicator color contrast against background might be an issue if only background changes. I'll phrase it as focus visible issue.

 Also, check `tabIndex={-1}` on root. The menu container has `tabIndex={-1}`. This is good so it can receive focus programmatically. But if we use roving tabindex on items, the container might not need to be focusable. Actually, in many menu implementations, the container has `tabIndex={-1}` to receive initial focus, then focus moves to items. Or the first item gets focused immediately. Either way, focusing the container is acceptable as a first step.

 Wait, there's another issue: `useEffect` for keyboard events adds listeners to `document`. This captures all keydowns even if other modals are open. But since it's a context menu, probably fine. However, if multiple menus or dialogs are open, this could conflict. Not critical.

 Also, `menuRef` is a `<div>` with `role="menu"`. Inside is a `<ul>`. ARIA `menu` should contain `menuitem` children directly, not a `<ul>`. Actually, having a `<ul>` inside `role="menu"` is redundant and potentially confusing. The `<ul>` has implicit `list` role. Mixing `list` and `menu` roles can cause screen readers to announce it as a list inside a menu. Better to remove `<ul>` and `<li>` and just use `<div>`s with `role="menuitem"`, OR keep semantic HTML but remove `role="menu"` from the root and put it on the `<ul>` instead. Wait, the root is `<div role="menu">` and contains `<ul>` which contains `<li role="menuitem">`. This is invalid because `menuitem` is not a valid child of `listitem` in ARIA ownership. The `<li>` has `role="menuitem"`, which overrides the `listitem` role, but the parent `<ul>` is still a `list`. Some screen readers might get confused. Better: put `role="menu"` on the `<ul>` and remove it from the `<div>`, or remove the `<ul>/<li>` entirely and use `<div>`s. Or put `role="presentation"` on `<ul>` and `<li>`.

 Actually, looking at ARIA practices: A `menu` should contain `menuitem`, `menuitemcheckbox`, or `menuitemradio`. The structure can be:
 ```html
 <ul role="menu">
   <li role="none">
     <button role="menuitem">...</button>
   </li>
 </ul>
 ```
 Or:
 ```html
 <div role="menu">
   <button role="menuitem">...</button>
 </div>
 ```

 So the current structure `<div role="menu"><ul><li role="menuitem">` is slightly off because `<ul>` is a list and `<li>` is a listitem overridden to menuitem, but the parent `<ul>` doesn't have `role="presentation"` or `role="none"`. It's better to set `role="presentation"` on `<ul>` and `<li>` if keeping semantic HTML, or move `role="menu"` to the `<ul>`.

 I'll add this as a Medium or Low issue.

 Also, the prompt mentions "missing alt". There are no images, but the emojis serve as visual icons. Since they have text labels, they don't strictly need "alt", but they should be hidden as decorative. I'll frame this under decorative emoji.

 Another issue: The component uses emoji characters which on some systems might not render, but that's not a11y.

 Let me reconsider the "focus return" issue. The component doesn't know the trigger. This is a common pattern. The fix is to accept a `triggerRef` or `returnFocus` callback. I'll include it.

 Let me also think about the `aria-hidden` during exit. If `animateIn` is false, opacity is 0. The menu is still in the DOM. A screen reader user could navigate into it via browse mode or if it accidentally receives focus. Adding `aria-hidden={!animateIn}` is a good quick fix. But if `animateIn` is false and the menu is still rendered, `aria-hidden` should be true. However, if we use `inert`, it also removes from focus. `inert` is well-supported now but maybe not 100%. `aria-hidden` + `tabIndex={-1}` on root (already there) helps but children could still be navigated to in browse mode? Actually, `aria-hidden` on parent hides the whole subtree. So that's good.

 Wait, if `aria-hidden` is on the root, and the root has `tabIndex={-1}`, it's fine. But if focus is inside when it starts closing, focus might be lost. But that's okay since closing should move focus out anyway.

 Another issue: The `menuItems` are memoized but the `handleKeyDown` depends on `menuItems`. Since `menuItems` is a new array every render (even with useMemo, it returns a new array only when deps change), the `handleKeyDown` callback changes when menuItems changes. This is fine.

 Actually, looking at `useEffect` for visible:
 ```typescript
 useEffect(() => {
   if (visible) {
     document.addEventListener('keydown', handleKeyDown);
     ...
   }
 }, [visible, handleKeyDown, handleClickOutside]);
 ```
 This means if `handleKeyDown` changes (e.g., `activeIndex` changes), the effect doesn't re-run because `handleKeyDown` is wrapped in `useCallback` with deps `[visible, menuItems, activeIndex, onClose]`. Wait, `activeIndex` is a state variable. If `activeIndex` changes, `handleKeyDown` changes, which means the effect cleans up and re-adds listeners. This is okay but slightly inefficient. Not an a11y issue.

 But wait: `handleKeyDown` is defined as:
 ```typescript
 const handleKeyDown = useCallback((e: KeyboardEvent) => { ... }, [visible, menuItems, activeIndex, onClose]);
 ```
 Since `activeIndex` changes on every arrow key press, `handleKeyDown` changes, causing the effect to re-run. This means the listener is removed and re-added on every arrow key press. This is actually bad because you might miss key events if the timing is off, though unlikely. But more importantly, it's not an a11y issue per se, it's a stability issue. I'll skip it.

 Let me think about color contrast again.
