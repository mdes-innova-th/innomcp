<!-- cc-team deliverable
 group: G2 (a11y division)
 member: A11Y-041 role=a11y model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":1618,"completion_tokens":4710,"total_tokens":6328,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":3794,"image_tokens":0},"cache_creation_input_tokens":0} | 63s
 generated: 2026-06-13T12:20:57.744Z -->
| Severity | Location | Issue | Fix |
|----------|----------|-------|-----|
| **High**   | **Timeline slider container** (`<div className="flex items-center justify-between ...">`) and each step button | Missing ARIA `tablist`, `tab`, and `tabpanel` roles. No `aria-selected`, `aria-controls`, or `aria-labelledby` attributes. Screen-reader users will not understand the tabbed interface or which step is selected. | Add `role="tablist"` to the container. Set `role="tab"` on each button, `aria-selected={activeStepId === step.id}`, and `aria-controls="panel-{step.id}"`. Give each button a unique `id="tab-{step.id}"`. Add `role="tabpanel"`, `id="panel-{step.id}"`, and `aria-labelledby="tab-{step.id}"` to the details panel wrapper. |
| **High**   | **All step buttons** – background/text colour combinations (`bg-emerald-500/25 text-emerald-400`, `bg-blue-500/25 text-blue-400`, `bg-muted text-muted-foreground`) | Potential colour contrast failure for small text (9–11px). The semi‑transparent backgrounds combined with light text likely do not meet the 4.5:1 ratio required by WCAG 2.1 AA. | Replace with solid, high‑contrast colour tokens. Example: use `bg-emerald-50 text-emerald-800`, `bg-blue-50 text-blue-800`, or equivalent Tailwind shades that pass contrast checks against the panel background. |
| **High**   | **Timeline buttons container** | Arrow‑key navigation is not implemented; users cannot move between steps with the keyboard, violating the expected behaviour for a `tablist`. | Add an `onKeyDown` handler (on the container or each button) that intercepts `ArrowLeft`/`ArrowRight` to move focus to the previous/next tab and `Enter`/`Space` to activate it. Prevent default to avoid page scrolling. |
| **Medium** | **Each timeline step button** | The accessible name of the button contains only the phase number and title; the completion status (completed/in_progress/pending) is conveyed purely by colour and is invisible to screen readers. | Provide an `aria-label` that includes the status, e.g., `aria-label={`${step.phase}: ${step.title} – ${step.status}`}`. Alternatively, add a visually hidden `<span className="sr-only"> ({step.status})</span>` inside the button. |
| **Medium** | **Details panel container** (the `{currentStep && (...)}` block) | When a step is selected, the new content is rendered but not announced to assistive technology. Users may miss the update. | Wrap the details panel in a container with `aria-live="polite"` (or add the attribute to the outermost details wrapper) so that changes are read automatically. |
| **Medium** | **Text sizing across the component** – `text-[9px]`, `text-[9.5px]`, `text-[10px]`, `text-[11px]` | Very small font sizes severely impact readability, especially for low‑vision users, and worsen contrast issues. | Increase minimum font sizes to at least `text-xs` (12px) or `text-sm` (14px) to improve legibility. |
| **Low**    | **Changed files list** – `<div className="space-y-1">` containing `<div>` items | Non‑semantic `<div>` elements are used for a list. Screen readers do not announce list structure or item count. | Replace the outer `<div>` with a `<ul>` and each file entry with an `<li>` (preserving the existing classes). This conveys the proper list semantics. |
| **Low**    | **Status badge inside the details panel** (`bg-emerald-500/15 text-emerald-500`, etc.) | Same contrast risk as the timeline buttons but on a smaller element. | Apply the same colour contrast fix as for the step buttons, or ensure the chosen pair meets 4.5:1 on the `bg-background/50` panel. |
