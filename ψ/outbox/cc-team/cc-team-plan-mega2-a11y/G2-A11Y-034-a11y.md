<!-- cc-team deliverable
 group: G2 (a11y division)
 member: A11Y-034 role=a11y model=zai-org/GLM-5.1
 finish_reason: stop | tokens: {"prompt_tokens":976,"completion_tokens":2797,"total_tokens":3773,"prompt_tokens_details":{"cached_tokens":960,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1929,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 41s
 generated: 2026-06-13T12:18:27.503Z -->
| severity | location | issue | fix |
| :--- | :--- | :--- | :--- |
| **High** | `<p className="mt-4 text-xs text-gray-400">` | **Color Contrast Failure**: `text-gray-400` on a white background (`bg-white/80`) yields a contrast ratio of ~2.9:1, failing WCAG AA (requires 4.5:1 for normal text). | Change to `text-gray-500` (~4.6:1) or `text-gray-600` (~7.5:1). |
| **High** | Capability icons row (`<div key={item.label}>`) | **Missing Interactivity / UX Dead-end**: Props `onQuerySelect` and `textareaRef` are passed but never used. Capability cards look clickable (hover shadow effect) but lack interactivity, violating UX expectations. | Make cards interactive by wrapping in `<button>` or adding `onClick={() => onQuerySelect(item.label)}`. Manage focus back to `textareaRef` if needed. |
| **Medium** | `<span className="text-2xl">{item.icon}</span>` | **Emoji Screen Reader Trap**: Emojis are read aloud by screen readers (e.g., "wrench", "robot face", "Thailand flag"), creating verbose, confusing output alongside the visible label. | Add `aria-hidden="true"` to the emoji `<span>` so screen readers only announce the text label. |
| **Medium** | Connection status dot (`<span className={... rounded-full ...}/>`) | **Redundant Status Indicator**: The colored dot is purely visual. While the adjacent text conveys the status, the empty `<span>` might be read unexpectedly or ignored inconsistently. | Add `aria-hidden="true"` to the dot `<span>`. Ensure the text span explicitly describes the status. |
| **Medium** | Connection status dot (`animate-pulse`) & Card entrance (`transition-all duration-700`) | **Vestibular Disorders / Motion Risk**: Continuous pulsing animation and forced entrance animations cannot be disabled by users who prefer reduced motion. | Use Tailwind's `motion-reduce:` modifier (e.g., `motion-reduce:animate-none`, `motion-reduce:transition-none`) or wrap animations in a `@media (prefers-reduced-motion: no-preference)` block. |
| **Medium** | Capability icons row container `<div>` | **Non-Semantic List**: A group of related items (capabilities) is rendered as generic `<div>`s, making it hard for screen readers to navigate or understand the grouping. | Change the container to `<ul role="list">` and the items to `<li>`. (Tailwind resets list styles, so `role="list"` ensures semantics are preserved). |
| **Medium** | Component definition | **Unused Props (UX/Code Smell)**: `providerMode` is destructured but never used in the component render. | Remove `providerMode` from the interface if not needed, or implement conditional rendering based on it. |
| **Low** | `<h1 className="bg-gradient-to-r... text-transparent">` | **Gradient Text Accessibility**: `text-transparent` relies on the background gradient for visual text color. Purple-700 on white passes AA, but high-contrast modes or custom screen reader stylesheets might make the text invisible. | Ensure a fallback `color` is set in CSS for forced-colors/high-contrast modes, or accept the slight contrast risk. |
| **Low** | Gradient bar `<div className="h-1.5...">` | **Decorative Markup Unlabeled**: Empty `<div>` used for visual styling might be ignored by screen readers, but it's best practice to explicitly hide decorative elements. | Add `aria-hidden="true"` to the gradient bar `<div>`. |
