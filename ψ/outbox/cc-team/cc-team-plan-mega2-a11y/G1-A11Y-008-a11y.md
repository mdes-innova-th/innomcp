<!-- cc-team deliverable
 group: G1 (a11y division)
 member: A11Y-008 role=a11y model=moonshotai/Kimi-K2.6
 finish_reason: length | tokens: {"prompt_tokens":1106,"completion_tokens":5000,"total_tokens":6106,"prompt_tokens_details":{"cached_tokens":71,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":0,"image_tokens":0},"cache_creation_input_tokens":0} | 82s
 generated: 2026-06-13T12:09:20.045Z -->
The user wants an accessibility (a11y) and UX audit of a React/Next.js component. I need to analyze the provided code for:

1. Missing ARIA/roles/labels
2. Keyboard navigation issues
3. Focus management problems
4. Color-contrast risks
5. Non-semantic markup
6. Missing alt text
6. Screen-reader traps
7. General UX issues

I need to output a table with columns: severity | location | issue | fix.

Let me go through the code systematically.

**Component structure:**
- It's a client component showing agent coordination status
- Takes events and isStreaming props
- Renders a container div with a header and a ul/li list of agent runs
- Each li has a status dot, name, duration, and optional summary

**Issues found:**

1. **Emoji as only visual indicator / missing text alternative**
   - Location: Header `<span>🤖</span>`
   - Issue: The robot emoji is used as a visual icon but lacks a text alternative or aria-label. Screen readers may announce it as "robot" or something else depending on the OS, but it's not consistent. Also, it's not marked as aria-hidden if it's purely decorative, but here it seems decorative.
   - Fix: Add `aria-hidden="true"` if decorative, or wrap with `<span aria-label="Agent coordination">` if meaningful. Actually, since there's text right after it saying "Agent Coordination", the emoji is decorative. Should be `aria-hidden="true"`.

2. **Status conveyed by color only**
   - Location: The dot spans with colors `bg-blue-500`, `bg-emerald-500`, `bg-rose-500`
   - Issue: The status (running/done/error) is conveyed only through color (blue/green/red). This fails WCAG 1.4.1 Use of Color. Screen readers don't get the status from the color, and colorblind users might not distinguish them well.
   - Fix: Add visually hidden text or aria-label to the dot span indicating status. Or use text alongside the color. The dot has `aria-hidden="true"` which makes it invisible to screen readers, but then the status is completely lost to SR users because there's no other text indicating "running", "done", or "error".
   - Actually, looking closer: the dot is aria-hidden. The status is ONLY conveyed by color. This is a critical issue. Need to add text like "(Running)", "(Done)", "(Error)" or use sr-only spans.

3. **Missing role/aria-live for dynamic updates**
   - Location: The container or list
   - Issue: As new events stream in (isStreaming), the content updates dynamically. Screen reader users won't be notified of new agents starting/finishing or status changes.
   - Fix: Add `aria-live="polite"` and `aria-atomic="false"` to the container so updates are announced. Maybe also `role="log"` or `role="status"`.

4. **Non-semantic markup / Missing heading**
   - Location: The header div with text "Agent Coordination..."
   - Issue: It looks like a heading but uses a generic div/span with styling. The structure lacks semantic heading hierarchy.
   - Fix: Use `<h3>` or `<h2>` (depending on context) instead of a div, or add `role="heading"` and `aria-level`. But since it's a component that could be nested, maybe just ensure it's a heading. Actually, the header is a div containing spans. It should be a heading element.

5. **Focus management - missing tabindex or focus handling on updates**
   - Location: General component
   - Issue: When streaming, new items appear. If the list grows, there's no focus management or scroll-into-view behavior. However, this might be intentional. But for keyboard users, if the list updates while they're reading, focus might not be trapped but content shifts. Not necessarily a trap, but could be disorienting.
   - Actually, is there a screen reader trap? No obvious trap. But the dynamic updates without live region could be considered a UX issue.

6. **Color contrast risks**
   - Location: `text-muted-foreground` and `text-muted-foreground/70` with `text-[10.5px]` and `text-[11px]`
   - Issue: Small font sizes (10.5px, 11px) combined with muted colors (likely gray) may fail WCAG contrast requirements, especially for the summary text at 70% opacity. Also, the duration text at 10.5px.
   - Fix: Ensure contrast ratio is at least 4.5:1 for small text. Avoid opacity reductions on small text.

7. **Truncation without accessible full text**
   - Location: `truncate` on displayName and summary spans
   - Issue: `truncate` uses `text-overflow: ellipsis` which hides content. Screen readers might still read the full text if it's in the DOM (which it is), but visual users can't see it. However, if the text is truncated via CSS, screen readers usually read the full text. But there's no tooltip or title attribute for visual users. More importantly, if the container is too narrow, the text is just cut off visually.
   - Fix: Add `title` attribute to truncated elements so hover reveals full text, or use a tooltip component. But `title` is generally accessible though not ideal. Actually, for a11y, the main issue is that the content is available in DOM so SR gets it. But for cognitive/low vision, maybe not a critical a11y issue. However, if `truncate` is combined with `flex-1`, it might be okay. I'll mention it as a low severity UX issue.

8. **Missing alt text / Decorative images**
   - There are no `<img>` tags, just an emoji. Already covered in #1.

9. **Keyboard navigation**
   - Location: The list items are not interactive, so they don't need to be focusable. But the container itself is not focusable. If this is the only content updating, keyboard users might not know about it unless they tab to it (but it's not focusable). However, live regions handle this. So not a keyboard nav issue per se.

10. **Time information not machine-readable**
    - Location: `{durationLabel}` span
    - Issue: "12.3s" is human-readable but not marked up in a way that's easily parsed. Not a major a11y issue, but could use `<time>` or at least ensure it's clear. Low priority.

11. **isStreaming status not communicated**
    - Location: Header text says "X agents active" but doesn't indicate streaming state to SR users beyond the count.
    - Issue: The `isStreaming` boolean affects the header count but the visual indicator is just the count change.
    - Fix: Already covered by live region.

12. **List semantics**
    - Location: `<ul>` and `<li>`
    - Issue: Actually, this is good. They used semantic list markup. But the list items contain a lot of inline spans. The list is appropriate.

13. **Agent status not exposed to assistive tech**
    - Location: The `li` elements
    - Issue: Each list item represents an agent run with a status. The status is only conveyed by the colored dot (which is aria-hidden). There's no text saying "Running", "Completed", or "Error".
    - Fix: Add a visually hidden span or aria-label to the list item or dot indicating the status.

14. **Reduced motion / Animation**
    - Location: `animate-pulse` on running dot
    - Issue: The pulsing animation could be problematic for users with vestibular disorders. CSS `prefers-reduced-motion` should be respected.
    - Fix: Use `motion-safe:` prefix or media query to disable animation when `prefers-reduced-motion: reduce` is set. Or provide a static alternative.

15. **Font size too small**
    - Location: `text-[10.5px]`, `text-[11px]`, `text-[11.5px]`
    - Issue: While not strictly a WCAG violation (WCAG doesn't specify minimum font size), 10.5px is extremely small and difficult to read for low vision users. This is a UX/accessibility concern.
    - Fix: Use minimum 12px or relative units.

Let me organize these into a table with severity levels.

Severity levels:
- **Critical**: Blocks screen reader users or violates WCAG A/AA in a way that prevents access.
- **High**: Major barrier for some users, easy to fix.
- **Medium**: Noticeable issue, should fix.
- **Low**: Minor issue, nice to have.

Issues to include:

1. **Critical**: Status conveyed by color only (dots) + aria-hidden removes it from SR entirely. The status is invisible to screen readers and indistinguishable for colorblind users.
   - Location: Dot `<span>` inside `<li>`
   - Issue: Agent status (running/done/error) relies solely on color (blue/green/red) and is hidden from assistive tech via `aria-hidden="true"`. Screen readers and colorblind users cannot perceive the state.
   - Fix: Remove `aria-hidden`, add `sr-only` text inside the dot span (e.g., `<span className="sr-only">Running</span>`), or add visible status labels. Ensure color is not the only means.

2. **High**: Dynamic updates not announced.
   - Location: Outer `<div>` container
   - Issue: Streaming events cause content to update (new agents, status changes, durations) without notifying screen readers. Users miss real-time progress.
   - Fix: Add `aria-live="polite"` and `aria-atomic="false"` to the container, or wrap the list in a `<div role="log" aria-live="polite">`.

3. **High**: Emoji without accessible name/hiding.
   - Location: Header `<span>🤖</span>`
   - Issue: Emoji may be announced inconsistently by screen readers and is redundant next to the text "Agent Coordination".
   - Fix: Add `aria-hidden="true"` to the emoji span since it's decorative.

4. **High**: Non-semantic heading.
   - Location: Header `<div>` with spans
   - Issue: The section title is a generic `<div>`, not a heading element, breaking document outline and making navigation by heading impossible.
   - Fix: Replace outer `<div>` with `<h3>` (or appropriate level) or add `role="heading" aria-level="3"`.

5. **Medium**: Color contrast risk on small muted text.
   - Location: `text-muted-foreground` (11px header), `text-muted-foreground/70` (10.5px summary)
   - Issue: Very small text (10.5px–11px) combined with reduced opacity (`/70`) likely fails WCAG AA contrast (4.5:1) against `bg-muted/30` background.
   - Fix: Increase font size to at least 12px and verify contrast ratio. Avoid opacity below 0.8 for small text.

6. **Medium**: Animation not respecting reduced motion.
   - Location: `animate-pulse` class on running status dot
   - Issue: Pulsing animation can trigger vestibular disorders; no `prefers-reduced-motion` fallback.
   - Fix: Replace with static indicator or wrap in `@media (prefers-reduced-motion: reduce)` / use Tailwind `motion-safe:` prefix.

7. **Medium**: Truncated content lacks accessible disclosure.
   - Location: `truncate` spans (displayName, summary)
   - Issue: Long names/summaries are visually truncated. While screen readers may read full text, low-vision users and those using zoom have no way to reveal the full string.
   - Fix: Add `title={run.displayName}` and `title={run.publicSummary}` to truncated elements, or implement a focusable tooltip.

8. **Low**: No programmatic association between list item and its status.
   - Location: `<li>` elements
   - Issue: The list item contains multiple disconnected `<span>` elements. SR users hear the text but not clearly structured.
   - Fix: Add `aria-label` to `<li>` composing the full sentence, e.g., `aria-label={`${run.displayName}: ${run.status}, ${durationLabel}`}`.

9. **Low**: Font sizes below 12px.
   - Location: `text-[10.5px]`, `text-[11px]`, `text-[11.5px]`
   - Issue: Extremely small text is hard to read for low-vision users and may not respect browser minimum font settings well.
   - Fix: Use relative units (`rem`) and minimum 12px (`0.75rem`).

10. **Low**: Missing `<time>` semantic markup for duration.
    - Location: Duration `<span>`
    - Issue: Duration is plain text without semantic time markup.
    - Fix: Wrap in `<time dateTime={`PT${(durationMs/1000).toFixed(1)}S`}>{durationLabel}</time>`.

Wait, is the status dot issue Critical or High? It's both color-only AND aria-hidden. That means screen readers get zero info about status. Colorblind users get zero info about status (unless they can infer from "animate-pulse" which is visual motion, but that's not reliable). So yes, Critical.

Actually, looking at the code again: the dot is `aria-hidden="true"`. The status is not conveyed anywhere else. So a screen reader user hears: "Agent Name 12.3s Summary text". They don't know if it's running, done, or error. That's a critical failure.

Also, the header count changes dynamically. Without a live region, the user doesn't know the count changed. That's high.

Let me double check if there are any other issues.

The `ul` is good. The `li` is good.

What about the `key={run.agentId}`? That's fine for React.

What about focus management? There's nothing interactive to focus. So no focus management issues per se. But if the component appears dynamically (mounts/unmounts), focus might land on it or not. But the code returns `null` when empty. When it appears, it just appears. If it's inserted into the DOM and the user is tabbing, focus doesn't go to it. That's fine for a status region. But if it appears, a live region would announce it.

Is there a screen reader trap? No. The prompt mentions "screen-reader traps" but I don't see any trap (like aria-hidden on a parent with focusable children, or positive tabindex). So I won't list a trap unless I find one. I don't see one.

Wait, the `aria-hidden="true"` on the dot: if a screen reader user navigates by arrow keys in virtual cursor mode, they will skip the dot. That's intended since it's decorative. But because the status is missing elsewhere, it's a problem. Not a trap.

Let me refine the table.

I should make sure the "location" column is specific (line or element).

Let's map line numbers approximately:
- Line 56: `<div className="mt-2...">` (container)
- Line 57-60: Header div with emoji
- Line 61: `<ul>`
- Line 62-90: `<li>` and its contents
- Line 78-82: dot span
- Line 83-85: displayName span
- Line 86-88: duration span
- Line 89-93: summary span

Actually, the code doesn't have line numbers in the prompt, but I can reference the element or approximate location.

Let me structure the table nicely.

| severity | location | issue | fix |
|---|---|---|---|
| Critical | Status dot `<span>` inside `<li>` | Agent status conveyed **only by color** (blue/green/red) and is explicitly hidden from assistive tech via `aria-hidden="true"`. Screen readers and colorblind users cannot determine if an agent is running, done, or errored. | Remove `aria-hidden`. Add a `<span className="sr-only">{run.status}</span>` inside the dot or adjacent to it. Supplement color with visible text labels or icons with text alternatives. |
| High | Outer container `<div>` | Dynamic streaming updates (new agents, status changes, durations) are injected into the DOM without a live region, so screen readers do not announce progress. | Add `aria-live="polite"` and `aria-atomic="false"` to the outer container, or wrap the list in `<div role="log" aria-live="polite">`. |
| High | Header emoji `<span>🤖</span>` | Decorative emoji lacks `aria-hidden` and may produce inconsistent or verbose screen-reader output (e.g., "robot face") next to the visible label. | Add `aria-hidden="true"` to the emoji span because the text "Agent Coordination" already provides the label. |
| High | Header `<div>` (title row) | Section title is a generic `<div>` with styled `<span>` elements instead of a semantic heading, breaking heading navigation and document outline. | Replace the header wrapper with `<h3>` (or contextually appropriate level), or add `role="heading" aria-level="3"`. |
| Medium | Summary & header text (`text-muted-foreground/70`, `text-[10.5px]`) | Very small text (10.5px) combined with reduced opacity (`/70`) likely fails WCAG AA contrast (4.5:1) against the muted background. | Increase font size to at least `12px` (`0.75rem`). Verify contrast ratio; avoid opacity below `0.8` for text smaller than `18px`. |
| Medium | Running dot (`animate-pulse`) | Pulsing animation is not suppressed for users with vestibular disorders (`prefers-reduced-motion`). | Use Tailwind's `motion-safe:animate-pulse` or wrap in `@media (prefers-reduced-motion: reduce) { .animate-pulse { animation: none; } }`. Provide a static color indicator as fallback. |
| Medium | Truncated spans (`truncate` on name/summary) | `truncate` hides overflow visually; low-vision users zooming or using magnification cannot reveal the full agent name or summary. | Add native `title={run.displayName}` and `title={run.publicSummary}` attributes, or implement an accessible tooltip component. |
| Low | `<li>` elements | List item content (name, duration, summary) is presented as disconnected inline spans without a programmatic grouping label, making it harder to parse in screen-reader browse mode. | Add an `aria-label` to each `<li>` summarizing the run: `aria-label={`${run.displayName}: ${run.status}, ${durationLabel}${summary ? ', ' + run.publicSummary : ''}`}`. |
| Low | Duration `<span>` | Elapsed time is plain text lacking semantic `<time>` markup. | Wrap duration in `<time dateTime={`PT${(durationMs/1000).toFixed(1)}S`}>{durationLabel}</time>`. |
| Low | Inline font sizes (`text-[10.5px]`, `text-[11px]`) | Hard-coded pixel-like font sizes below 12px are difficult to read for low-vision users and do not scale well with browser text-only zoom. | Replace arbitrary values with relative rem units (`text-xs` ≈ `0.75rem`/`12px`) and avoid sizes below `12px`. |

This looks comprehensive and directly addresses the prompt's requested categories.

I should double-check if I missed "missing alt". There are no images, so the emoji is the closest thing. I've covered that.

Keyboard nav: There are no interactive controls, so keyboard navigation isn't strictly required. However, if the truncated text needs a tooltip, that would require focus. But I covered truncation.

Focus management: No focusable elements, so no focus management issues. But I should mention that since there are no interactive elements, focus isn't an issue. However, the prompt asks to audit for focus management. If the component mounts/unmounts, focus isn't moved to it, which is correct for a decorative status panel. So no issue there.

Color-contrast: Covered.

Non-semantic markup: Covered (heading issue).

Missing alt: Covered (emoji).

Screen-reader traps: None found, but the `aria-hidden` on the dot combined with missing status text is a "screen reader barrier" though not technically a "trap". I think the table covers the spirit.

One more thing: The `headerCount` logic. When `isStreaming && activeCount > 0`, it shows activeCount, else total count. The text says "X agents active". If `isStreaming` is false and it shows total count, the label still says "active" which might be inaccurate. Wait, let's look:
```tsx
const headerCount = isStreaming && activeCount > 0 ? activeCount : agentRuns.length;
```
And the text: `Agent Coordination ({headerCount} agent{headerCount !== 1 ? "s" : ""} active)`
This means if `isStreaming` is false, `headerCount` = `agentRuns.length`, but the text still says "active". So if there are 3 runs but all done, it says "3 agents active". That's a UX bug and potentially confusing for SR users if they hear it via live region. Is this an a11y issue? It's a content accuracy issue. I should include it.

Actually, looking closely: `headerCount` is either activeCount or total length. The label is hardcoded "active". So when not streaming, it shows total agents but calls them active. That's misleading. It's a content issue.

Let me add this:
- **Medium/High**: Inaccurate status label.
  - Location: Header text span
  - Issue: When `isStreaming` is false, `headerCount` falls back to `agentRuns.length` (total agents) but the label still reads "active", falsely claiming completed/error agents are active.
  - Fix: Change label to reflect reality: `{headerCount} agent{...}` without "active", or use conditional text: `{isStreaming ? `${activeCount} active` : `${agentRuns.length} total`}`.

This is a good catch. It affects screen reader users if they hear the live region announce "3 agents active" when they are done.

Let me adjust the table to include this. Maybe Medium severity.

Also, another issue: The `aria-hidden="true"` on the dot. If the dot is purely decorative, that's fine, but the status must be conveyed elsewhere. So the fix is to expose status text.

One more: The `ul` lacks an `aria-label` or `aria-labelledby` to identify the list. Since there's a header right above it, they could be associated. But the header is not a heading and not linked. I could add this as a low issue: associate the list with the header via `aria-labelledby` or give it an `aria-label="Agent runs"`. But this is minor.

Let me finalize the table. I'll present it cleanly.

Wait, the prompt says "Output table | severity | location | issue | fix |". So I should use markdown table format.

I'll make sure to include the inaccurate "active" label issue because it's a real problem in the code.

Let me review the exact code for the header:
```tsx
const headerCount = isStreaming && activeCount > 0 ? activeCount : agentRuns.length;
// ...
<span>Agent Coordination ({headerCount}
