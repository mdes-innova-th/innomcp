<!-- cc-team deliverable
 group: G2 (a11y division)
 member: A11Y-043 role=a11y model=moonshotai/Kimi-K2.6
 finish_reason: length | tokens: {"prompt_tokens":966,"completion_tokens":5000,"total_tokens":5966,"prompt_tokens_details":{"cached_tokens":72,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":0,"image_tokens":0},"cache_creation_input_tokens":0} | 72s
 generated: 2026-06-13T12:27:14.927Z -->
The user wants an accessibility (a11y) and UX audit of a React/Next.js component. I need to output a table with columns: severity | location | issue | fix.

 Let me analyze the component `FloatingStatusBadge.tsx` carefully.

 Component behavior:
 - Floating badge at bottom center showing AI status
 - Statuses: 'thinking', 'tool-use', 'done', 'idle'
 - Shows emoji + text + elapsed time
 - Uses `fixed` positioning
 - Fades out after 'done' status

 Potential issues:

 1. **Live regions / Screen reader announcements**: The component updates elapsed time every 100ms. This will cause constant screen reader announcements if it's a live region, or if not, the user won't know about status changes. Actually, the text changes every 100ms (`elapsed.toFixed(1)`), which means if a screen reader is reading this, it will be constantly interrupted. This is a critical issue.
    - Severity: Critical
    - Location: `displayTime` span and `text` span
    - Issue: Rapidly updating text (every 100ms) creates a screen reader verbosity trap/chaos. Screen readers will attempt to re-announce the changing content continuously.
    - Fix: Use `aria-live` with `aria-atomic="true"` carefully, OR better, use an `aria-live="polite"` region only for status changes (not the timer), and mark the timer with `aria-hidden="true"`. Also, use a visually hidden live region for status announcements while hiding the visual timer from AT.

 2. **Missing role and aria-label**: The badge is a floating status indicator but lacks semantic meaning.
    - Severity: High
    - Location: Root `<div>`
    - Issue: No `role="status"` or `role="alert"` or `aria-live` region. Screen readers won't announce status changes.
    - Fix: Add `role="status"` and `aria-live="polite"` to the container, or use a separate visually hidden live region. But careful with the timer updates.

 3. **Keyboard focus / Focus management**: The badge appears and disappears but doesn't receive focus. However, since it's a status indicator, it shouldn't necessarily receive focus. But if it appears, should it be announced? Yes, via live region. Also, `pointer-events-none` when invisible is good, but there's no `tabIndex` or focus management needed if it's purely presentational. Actually, no focus issue here since it's not interactive.

 4. **Color contrast**: `bg-indigo-600 text-white` - Indigo 600 on white is generally okay (contrast ratio usually > 4.5:1), but `text-white/80` for the timer might be risky depending on the exact indigo-600 shade. White at 80% opacity on indigo-600 might drop below 4.5:1. Need to check. Tailwind indigo-600 is #4f46e5. White at 80% opacity on this background... if the background is solid, the effective color is rgba(255,255,255,0.8). Contrast ratio of #4f46e5 vs rgba(255,255,255,0.8) - likely passes AA for large text, but for small text (text-sm = 14px), it might be borderline. Actually, white at 80% on indigo-600: let's approximate. #4f46e5 luminance. White 100% is 21:1 roughly. At 80% opacity, the color is blended. The resulting color would be roughly #7770eb? No wait, blending white with indigo. Actually, the background is solid indigo. The text is white 80%. The contrast might be around 4.0:1 or lower? Let's think. Indigo 600 (#4f46e5) has relative luminance around 0.15. White (1.0) at 80% opacity over indigo gives: 0.8*1.0 + 0.2*0.15 = 0.83. Contrast (1.05+0.83)/(1.05+0.15) = 1.88/1.15 = 1.63? No that's not right. Formula: (L1 + 0.05) / (L2 + 0.05). L1=0.83, L2=0.15. (0.88)/(0.20) = 4.4:1. That's close to 4.5:1. Might fail for small text. Let's say it's a risk.
    - Severity: Medium
    - Location: `text-white/80` timer span
    - Issue: Reduced opacity text may fail WCAG AA contrast ratio (4.5:1) for small text (text-sm).
    - Fix: Use solid `text-white` or ensure contrast ratio meets 4.5:1. Use a contrast checker.

 5. **Non-semantic markup**: Using `<div>` and `<span>` for a status indicator. Not terrible, but could be more semantic.
    - Severity: Low
    - Location: Root `<div>`
    - Issue: Generic `<div>` instead of more semantic markup for a status/log item.
    - Fix: Use `<output>` element or `<aside>` with appropriate ARIA, or keep `<div>` but add `role="status"`.

 6. **Missing alt / Emoji accessibility**: The emoji are in a span without `role="img"` and `aria-label`.
    - Severity: High
    - Location: Emoji `<span>`
    - Issue: Emojis (🤖, 🔧, ✅) are read by screen readers with their literal descriptions, which may be verbose or confusing, and lack context.
    - Fix: Wrap emoji in `<span role="img" aria-label="Thinking">` etc. Or use `aria-hidden="true"` on the emoji span and provide visually hidden text.

 7. **Reduced motion / Vestibular disorders**: The component uses `transition-opacity duration-500` but doesn't respect `prefers-reduced-motion`.
    - Severity: Medium
    - Location: CSS transitions
    - Issue: Fade animation may cause issues for users with vestibular disorders.
    - Fix: Wrap transitions in `@media (prefers-reduced-motion: reduce)` or use Tailwind's `motion-safe:` / `motion-reduce:` utilities (e.g., `motion-safe:transition-opacity motion-safe:duration-500`).

 8. **Pointer events none trap**: When `opacity-0 pointer-events-none`, it's good, but during fade out, it's still in the DOM and might block pointer events to content behind it? Actually `pointer-events-none` solves that. But the badge is `fixed bottom-24` and might overlap content. Not necessarily an a11y issue unless it traps focus, but it's not focusable.

 9. **Timer precision and screen reader updates**: The interval is 100ms. Even if we hide the timer from AT, the text `เสร็จแล้ว ${elapsed.toFixed(1)}s` updates only on status change to done. But `displayTime` updates every 100ms. So the main issue is the `displayTime` span.

 10. **Language / i18n**: The text is in Thai ("MDES กำลังคิด..."). The `lang` attribute isn't set on the component, but that's more of an HTML document issue. Not a component-level issue unless it's dynamically injected. I'll skip this as it's document-level.

 11. **Z-index and focus visibility**: `z-50` might overlay other content, but not necessarily an issue unless it blocks access to focusable elements. Since it's centered at bottom and small, probably fine. But if it overlaps buttons, `pointer-events-none` when invisible helps, but when visible it might block clicks. It's a UX issue but not strictly a11y.

 12. **Status change announcement**: When status changes from 'thinking' to 'tool-use' to 'done', these are important state changes. The component should announce these politely.
    - Severity: High
    - Location: Status transitions
    - Issue: No `aria-live` region means screen reader users are unaware of AI activity status.
    - Fix: Add `aria-live="polite"` and `aria-atomic="true"` to the container, but hide the rapidly updating timer from AT.

 13. **Focus management on appearance**: When the badge appears, should focus move to it? Probably not, since it's a status indicator, not an interactive element. It should be announced via live region instead.

 14. **Use of `useRef` with timers**: Not an a11y issue.

 Let me structure the table. I need to be thorough but concise.

 Let's refine the issues:

 **Issue 1: Rapidly updating content (Screen reader verbosity trap)**
 - Severity: Critical
 - Location: `displayTime` span (`{displayTime && <span...>{displayTime}</span>}`)
 - Issue: Elapsed time updates every 100ms (0.1s). Without `aria-hidden`, screen readers will attempt to announce every change, creating a "babble" effect that drowns out all other audio and makes the page unusable for AT users.
 - Fix: Add `aria-hidden="true"` to the timer `<span>`. Status text should be in a separate live region that updates only on meaningful status changes, not timer ticks.

 **Issue 2: Status changes not announced to screen readers**
 - Severity: Critical
 - Location: Root `<div>` container
 - Issue: The badge conveys dynamic system status (thinking, tool-use, done) but lacks `role="status"` and `aria-live`. Screen reader users receive no feedback about AI activity.
 - Fix: Add `role="status"` and `aria-live="polite"` to the root container. Ensure `aria-atomic="true"` so the whole message is read as one unit. Alternatively, use a dedicated visually-hidden `<div aria-live="polite">` for announcements while keeping the visual badge presentational.

 **Issue 3: Emoji lack accessible names**
 - Severity: High
 - Location: Emoji `<span className="shrink-0">{emoji}</span>`
 - Issue: Emojis are conveyed by screen readers with literal descriptions (e.g., "robot face", "wrench", "white heavy check mark") which may be confusing or lack context. Some screen readers may skip them entirely.
 - Fix: Add `role="img"` and meaningful `aria-label` (e.g., `aria-label="Thinking"`, `aria-label="Using tools"`, `aria-label="Complete"`) to the emoji span, or hide the emoji with `aria-hidden="true"` and provide visually hidden text.

 **Issue 4: Color contrast risk on timer text**
 - Severity: Medium
 - Location: Timer `<span className="... text-white/80">`
 - Issue: `text-white/80` (80% opacity white) over `bg-indigo-600` may yield a contrast ratio below 4.5:1 for small text (`text-sm` ≈ 14px), failing WCAG 2.1 AA.
 - Fix: Use solid `text-white` or darken the background. Verify with a contrast checker. If secondary styling is needed, use a darker indigo (e.g., `bg-indigo-700`) or bold text.

 **Issue 5: Animation lacks reduced-motion support**
 - Severity: Medium
 - Location: `transition-opacity duration-500 ease-in-out`
 - Issue: Opacity fade transitions can trigger vestibular disorders (dizziness, nausea) in users with motion sensitivity.
 - Fix: Respect `prefers-reduced-motion`. In Tailwind, use `motion-safe:transition-opacity motion-safe:duration-500` or add a CSS media query `@media (prefers-reduced-motion: reduce) { .badge { transition: none; } }`.

 **Issue 6: Non-semantic markup for status**
 - Severity: Low
 - Location: Root `<div>`
 - Issue: A generic `<div>` is used for a system status message instead of a semantic element like `<output>` or `<aside>`.
 - Fix: Replace root `<div>` with `<output>` (for timer result) or keep `<div>` but add `role="status"`. `<output>` is appropriate since it represents the result of a calculation/process.

 **Issue 7: Potential focus obstruction / z-index stacking**
 - Severity: Low
 - Location: `fixed bottom-24 ... z-50`
 - Issue: Fixed positioning with high z-index can obscure focusable elements (e.g., bottom navigation, buttons). While `pointer-events-none` is applied when invisible, when visible it may intercept clicks.
 - Fix: Ensure `pointer-events-none` is applied when opacity is low enough to be non-interactive, or use `invisible` instead of `opacity-0` when fully hidden to remove it from the accessibility tree completely. Actually, `opacity-0` keeps it in the a11y tree unless `aria-hidden` or `hidden` is used. Wait, `opacity-0` doesn't remove it from the accessibility tree in all browsers/screen readers. It might still be read.
 - Fix: Add `aria-hidden={!isVisible || fadeOut}` or use `hidden` attribute when `status === 'idle'` to fully remove from a11y tree. Or use `invisible` class.

 Let me reconsider the `opacity-0` issue. If `isVisible` is false (idle), it has `opacity-0 pointer-events-none`. But it's still in the DOM and might be read by some screen readers if not hidden properly. Better to conditionally render or add `aria-hidden="true"` when not visible. But the comment says "still render for transition purposes but invisible". So `aria-hidden={!isVisible || fadeOut}` would be good. Or `hidden` attribute when idle. But `hidden` would break the CSS transition. So `aria-hidden` is the way.

 Actually, if we add `role="status" aria-live="polite"` to the container, and it's `opacity-0`, some screen readers might still announce it if the content changes while invisible? That's a risk. Better to use a visually hidden live region that is always present but only contains text when needed, and the visual badge is purely presentational with `aria-hidden`. This is the most robust pattern.

 Let me restructure the recommendations to reflect best practice:

 **Best practice architecture:**
 - Visual badge: `aria-hidden="true"` (purely visual)
 - Live region: A separate visually hidden `<div aria-live="polite" aria-atomic="true">` that contains the status text (without the timer) and updates only on status changes.
 - Timer: `aria-hidden="true"` on the visual timer.

 But the user asked for an audit of the component as-is. I should list the issues and fixes. The fix can suggest this architecture.

 Let's compile the table.

 | severity | location | issue | fix |
 |---|---|---|---|
 | Critical | Timer span (`displayTime`) | Rapid 100ms updates create screen reader "babble" trap, rendering page unusable for AT users | Add `aria-hidden="true"` to the timer span. Move announcements to a dedicated live region that updates only on status changes, not timer ticks. |
 | Critical | Root `<div>` | Dynamic status changes (thinking/tool-use/done) are not announced to screen readers | Add `role="status"` and `aria-live="polite"` with `aria-atomic="true"`. Alternatively, decouple visual badge (`aria-hidden`) from a visually hidden live region. |
 | High | Emoji span (`{emoji}`) | Emojis lack accessible names; screen readers may read inconsistent or confusing descriptions | Add `role="img"` and descriptive `aria-label` per status (e.g., `aria-label="Thinking"`), or hide emoji with `aria-hidden="true"` and use visually hidden text. |
 | Medium | Timer span (`text-white/80`) | `text-white/80` over `bg-indigo-600` risks failing WCAG AA contrast (4.5:1) for small text | Use solid `text-white` or verify contrast ratio. If secondary visual hierarchy is needed, adjust background shade or font weight. |
 | Medium | Transition classes | Opacity fade lacks `prefers-reduced-motion` support | Wrap transition in `motion-safe:` (Tailwind) or `@media (prefers-reduced-motion: reduce) { transition: none; }`. |
 | Low | Root `<div>` | Generic `<div>` for system status lacks semantic meaning | Use `<aside>` or `<output>` element, or add `role="status"`. `<output>` is ideal for representing calculation/process results. |
 | Low | Root `<div>` (`opacity-0`) | Invisible badge (`opacity-0`) remains in accessibility tree and may be read by some AT | Add `aria-hidden={!isVisible \|\| fadeOut}` to ensure hidden states are fully removed from AT. Consider `invisible` class when idle to prevent focus obstruction. |

 Wait, is `fadeOut` a state where it should be aria-hidden? Yes, when fading out, it's visually disappearing, so it should be hidden from AT too, or at least when fully faded. But during the 500ms fade, `aria-hidden` might cut it off abruptly. Better to just have `aria-hidden={!isVisible}` (i.e., hide when idle). When `fadeOut` is true but `isVisible` is true, it's still visible-ish. But since it's `opacity-0`, it's not visible. Actually `isVisible` is true for 'done' even during fade. So `aria-hidden={fadeOut}` might be good. Or simpler: `aria-hidden={status === 'idle'}`.

 Actually, if we use a separate live region, the visual badge can be `aria-hidden="true"` always. That's cleaner.

 Let me refine the "fix" for the Critical root div issue to suggest the decoupled pattern, because putting `aria-live` on the visual badge with the timer inside is dangerous even if the timer is `aria-hidden`, because some screen readers might still pick up changes depending on browser heuristics. The safest fix is indeed decoupling.

 But I should keep fixes practical. The user can add `aria-live` but must hide the timer.

 Let's also consider the `pointer-events-none` when `!isVisible`. That's actually good UX. But when visible, the badge might block clicks to elements behind it. Since it's `fixed bottom-24 left-1/2`, it's centered and might overlay content. Not strictly a11y unless it blocks interactive elements. I'll mention it as Low.

 Another issue: **Focus order / Focus trap**. No, it's not a trap.

 Another issue: **Language of parts**. The text is Thai. If the page lang is not th, it should have `lang="th"`. But this is likely a Thai app, so the page probably has it. I'll skip.

 Another issue: **Elapsed time formatting**. `elapsed.toFixed(1)` gives decimal like "1.2s". Fine.

 Another issue: **No heading structure**. Not relevant here.

 Another issue: **Keyboard navigation**. Since it's not interactive, no keyboard nav needed.

 Let's finalize the table. I'll make sure to cover:
 1. Live region / status announcement (Critical)
 2. Timer updates / screen reader trap (Critical)
 3. Emoji alt text (High)
 4. Color contrast (Medium)
 5. Reduced motion (Medium)
 6. Semantic markup / role (Low)
 7. Hidden state in a11y tree (Low)

 I should present it cleanly in markdown table format.

 Let me double-check the contrast calculation for white/80 on indigo-600.
 Indigo-600: #4f46e5.
 Relative luminance of #4f46e5:
 R = 79, G = 70, B = 229.
 RsRGB = 79/255 = 0.3098. Since < 0.03928, Rlin = 0.3098/12.92 = 0.0240.
 GsRGB = 70/255 = 0.2745. Glin = 0.2745/12.92 = 0.0212.
 BsRGB = 229/255 = 0.8980. Blin = 0.8980/12.92? No, 0.8980 > 0.03928. So Blin = ((0.8980+0.055)/1.055)^2.4 = (0.9033)^2.4 = 0.9033^2 * 0.9033^0.4. 0.9033^2 = 0.816. 0.9033^0.4 ≈ e^(0.4*ln(0.9033)) = e^(0.4*-0.1017) = e^(-0.0407) = 0.960. So 0.816*0.96 = 0.783.
 Wait, let me recalculate properly.
 R = 79. RsRGB = 0.3098. Rlin = 0.0240.
 G = 70. GsRGB = 0.2745. Glin = 0.0212.
 B = 229. BsRGB = 0.8980. BsRGB > 0.03928. Blin = ((0.898039+0.055)/1.055)^2.4 = (0.9531)^2.4.
 0.9531^2 = 0.9084.
 0.9531^0.4 = e^(0.4 * ln(0.9531)) = e^(0.4 * -0.0480) = e^(-0.0192) = 0.9810.
 So Blin = 0.9084 * 0.9810 = 0.891.
 L = 0.2126*0.0240 + 0.7152*0.0212 + 0.0722*0.891 = 0.0051 + 0.0152 + 0.0643 = 0.0846.
 L2 = 0.0846.

 Now white at 80% opacity over this background.
 The resulting color when blending white (1,1,1) with background (0.3098, 0.2745, 0.8980) at 80% opacity:
 Result = alpha * foreground + (1-alpha) * background.
 R = 0.8*1 + 0.2*0.3098 = 0.8 + 0.06196 = 0.86196.
 G = 0.8*1 + 0.2*0.2745 = 0.8 + 0.0549 = 0.8549.
 B = 0.8*1 + 0.2*0.8980 = 0.8 + 0.1796 = 0.9796.

 Now calculate luminance of result:
 R = 0.86196 > 0.03928. Rlin = ((0.86196+0.055)/1.055)^2.4 = (0.8696)^2.4.
 0.8696^2 = 0.7562.
 0.8696^0.4 = e^(0.4*ln(0.8696)) = e^(0.4*-0
