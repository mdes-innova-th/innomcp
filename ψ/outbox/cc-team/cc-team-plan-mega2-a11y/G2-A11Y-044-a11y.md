<!-- cc-team deliverable
 group: G2 (a11y division)
 member: A11Y-044 role=a11y model=zai-org/GLM-5.1
 finish_reason: stop | tokens: {"prompt_tokens":756,"completion_tokens":3912,"total_tokens":4668,"prompt_tokens_details":{"cached_tokens":64,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":3544,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 81s
 generated: 2026-06-13T12:23:57.627Z -->
| severity | location | issue | fix |
|---|---|---|---|
| **High** | Outer `<div>` | **Non-semantic markup.** Screen readers won't announce the list structure or the total number of suggestions (e.g., "1 of 5"). | Change the outer `<div>` to `<ul role="list">` and wrap each `<button>` in `<li>`. Add `aria-label="Follow-up suggestions"` to provide context. |
| **High** | Suggestion `<button>` | **Animations ignore `prefers-reduced-motion`.** The inline `animation` style forces the animation even if the user has requested reduced motion, risking vestibular disorders. | Conditionally apply the inline `animation` and `animationDelay` styles using a `useReducedMotion` hook, or move animations to CSS and wrap them in `@media (prefers-reduced-motion: no-preference)`. |
| **High** | Scroll container | **Hidden scrollbar removes affordance and functionality.** Hiding the scrollbar (`scrollbarWidth: "none"`, etc.) makes horizontal scrolling undiscoverable for sighted users and removes the ability to drag-scroll for mouse users. | Keep the native scrollbar visible but style it to be subtle, or add visible left/right scroll arrow buttons for mouse users. |
| **Medium** | Suggestion `<button>` | **Tab-only keyboard navigation.** If there are many suggestions, users must press Tab repeatedly to bypass the component. | Implement a roving tabindex with Left/Right arrow keys (e.g., using `role="listbox"` / `role="option"`) and manually call `scrollIntoView()` on the focused item. |
| **Medium** | Suggestion `<button>` | **Truncated text hides visual context.** The `truncate` class cuts off text visually, but screen readers will read the whole string. Sighted users cannot see the full suggestion. | Add `title={suggestion}` to the button, or implement an accessible tooltip that appears on hover and focus to reveal the full text. |
| **Medium** | Gradient overlay | **Color contrast risk.** The gradient fades text out, creating a zone where the text contrast ratio drops below the WCAG 4.5:1 minimum. | Ensure the gradient fades from the solid background color to transparent, and add `scroll-padding-right` or padding so text doesn't rest in the low-contrast fade zone. |
| **Low** | Outer `<div>` | **Missing accessible name.** Screen reader users landing on this component will just hear a list of buttons without knowing what they are for. | Add `aria-label="Follow-up suggestions"` to the outermost container or the `<ul>`. |
