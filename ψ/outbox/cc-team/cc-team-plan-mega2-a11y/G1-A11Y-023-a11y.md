<!-- cc-team deliverable
 group: G1 (a11y division)
 member: A11Y-023 role=a11y model=moonshotai/Kimi-K2.6
 finish_reason: length | tokens: {"prompt_tokens":2094,"completion_tokens":5000,"total_tokens":7094,"prompt_tokens_details":{"cached_tokens":3,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":0,"image_tokens":0},"cache_creation_input_tokens":0} | 45s
 generated: 2026-06-13T12:13:48.203Z -->
The user wants an accessibility (a11y) and UX audit of a React/Next.js component called `ChatHistorySearch.tsx`. They want the output in a table format with columns: severity | location | issue | fix.

Let me analyze the provided code carefully for accessibility issues, keyboard navigation, focus management, color contrast, semantic markup, alt text, screen reader traps, etc.

First, let me look at the code structure:

1. It's a search modal/panel that slides down from the top
2. Has an overlay backdrop
3. Contains an input field with search icon and close button
4. Shows results grouped by date
5. Has keyboard navigation (ArrowDown, ArrowUp, Enter, Escape)
6. Uses highlightIndex for keyboard selection

Now let me identify issues:

**1. Focus Management / Focus Trap**
- The dialog doesn't trap focus. When tabbing, focus can escape to the rest of the page while the modal is open.
- No focus restoration when closing.
- Location: Slide-down panel / dialog
- Severity: High

**2. Missing aria-controls / aria-activedescendant**
- The input should have `aria-controls` pointing to the listbox.
- The input should have `aria-activedescendant` to indicate which option is highlighted for screen readers.
- Location: Search input
- Severity: High

**3. Missing roles/labels on result items**
- The result items use `data-index` but don't seem to have `role="option"` or proper IDs for `aria-activedescendant`.
- The code is truncated but I can infer the list items are likely missing proper roles.
- Location: Result items in listbox
- Severity: High

**4. Non-semantic markup / Missing list structure**
- The results are in a `div` with `role="listbox"` but individual items likely aren't `role="option"`.
- The grouping by date might break the listbox pattern if there are group headers without `role="group"` or proper `aria-label`.
- Location: Results list
- Severity: Medium

**5. Color contrast risks**
- `bg-yellow-200` with default black text might have insufficient contrast (especially yellow).
- `text-gray-400` for icons might be too light against `bg-gray-50` or white.
- `dark:bg-yellow-700` with white text - need to check but likely okay.
- Location: highlightText function / search icon / close button
- Severity: Medium

**6. Missing alt / Decorative icons**
- The SVG search icon is decorative but lacks `aria-hidden="true"` or `role="img"` with aria-label.
- The close button SVG similarly.
- Actually, looking at the code: the parent button has `aria-label="ปิด"` so the SVG inside should be `aria-hidden="true"` or `focusable="false"`.
- The search icon is purely decorative and should be hidden from screen readers.
- Location: SVG icons
- Severity: Low-Medium

**7. Keyboard navigation issues**
- The `handleKeyDown` is on the panel `div`, but if focus is on the input (which it will be), it works because events bubble. However, if focus moves to the close button or elsewhere, keyboard navigation stops working.
- Tab navigation isn't implemented for the results list - only arrow keys. But in a listbox pattern, this is actually correct (tab should move out of the listbox or to other controls).
- However, there's no handling of `Home`, `End` keys for listbox.
- Location: handleKeyDown / listbox
- Severity: Low-Medium

**8. Screen reader announcements**
- No live region for search results count (e.g., "3 results found").
- When results update, screen readers don't know.
- Location: Search results area
- Severity: Medium

**9. Missing aria-expanded?**
- This is more of a combobox pattern. The input should probably have `role="combobox"`, `aria-expanded`, `aria-autocomplete="list"`.
- Currently it's just a text input inside a dialog with a listbox. This is a valid pattern but could be enhanced.
- Actually, since it's a search dialog, the dialog pattern is fine, but the input should still indicate it controls the listbox.
- Location: Search input
- Severity: Medium

**10. Backdrop click to close**
- The backdrop has `aria-hidden="true"` which is good, but it should also have `role="none"` or ensure it's not focusable.
- Location: Overlay backdrop
- Severity: Low

**11. Scroll lock / body scroll**
- When modal opens, body scroll isn't locked. This can cause background content to scroll, which is a UX issue and can disorient screen reader users.
- Location: isOpen effect
- Severity: Medium

**12. Reduced motion**
- The transition uses `duration-300` but no `prefers-reduced-motion` check.
- Location: Slide-down panel CSS
- Severity: Low

**13. Missing id linking**
- The listbox has `aria-label="ผลการค้นหา"` but no `id` to be referenced by `aria-controls` on the input.
- Location: listbox div
- Severity: Medium

**14. Highlight index reset**
- When query changes, `highlightIndex` resets to 0, which is good.
- But if filtered results change and current highlight is invalid, it might crash? Actually it resets to 0 in the debounced search.
- Location: debouncedSearch
- Severity: Low (defensive)

**15. Close button focus**
- The close button is reachable by Tab, but after closing, focus should return to the element that opened the search (focus restoration).
- Location: onClose handler
- Severity: High

**16. Missing aria-live for empty state**
- When no results, should announce "No results found".
- Location: empty results
- Severity: Medium

**17. Date grouping headers**
- If the code renders date headers inside the listbox, they might be interpreted as options. Grouping in a listbox requires `role="group"` with `aria-label` on the group, and options inside.
- But looking at the structure, `groupByDate` returns a Map, and the rendering likely iterates over groups. If they render `<div>` headers with `<div>` children inside the `role="listbox"`, that's invalid.
- Location: grouped results rendering
- Severity: High

**18. Input disabled state**
- `disabled={!isOpen}` - when disabled, screen readers might not read it properly if it's the only focusable element. But since it's inside a dialog, and focus is managed, this might be okay. However, `disabled` removes it from focusability, which could be confusing if the dialog opens and the input is disabled temporarily.
- Actually, when `isOpen` is true, it's not disabled. When closed, the panel is hidden (`-translate-y-full`), so the disabled state might be redundant but harmless.
- Location: input
- Severity: Low

**19. Escape key behavior**
- `e.preventDefault()` on Escape is good for closing, but if the input has text, first Escape should clear the input, second Escape should close. This is standard UX for search dialogs.
- Location: handleKeyDown
- Severity: Low (UX enhancement)

**20. Click on results**
- The truncated code likely has click handlers, but need to ensure they have proper keyboard support (Enter/Space). Since they're in a listbox with arrow keys and Enter, that's covered.

**21. aria-modal and role="dialog"**
- The panel has `role="dialog"` and `aria-modal={isOpen}`. When `isOpen` is false, `aria-modal="false"` but the dialog is still in the DOM (just translated off-screen). Screen readers might still see it.
- It should probably use `hidden={!isOpen}` or `display: none` when closed, or remove from DOM. `inert` attribute could help.
- Location: dialog container
- Severity: High

**22. z-index and backdrop**
- The backdrop is `z-40` and panel `z-50`, which is fine visually but ensure focus order matches visual order.

**23. useEffect focus**
- `useEffect` focuses input when opened, but doesn't check if user prefers reduced motion or if focus was already inside.
- Also, `setQuery('')` when opened clears search, which might be annoying if reopening.

Let me structure these into a table. I need to be careful about the truncated code - I should only flag issues visible in the provided code or strongly inferable from the structure.

Wait, the code is truncated at the end (`{filtered.length === 0 && query.tr`), but I can infer the rest of the component renders results. I should mention issues that are apparent from the existing code.

Let me organize by severity:

**Critical/High:**
1. Focus trap missing - focus can escape modal while open
2. Focus restoration - onClose doesn't return focus to trigger
3. aria-activedescendant missing - screen readers don't know which option is active in listbox
4. Dialog remains in DOM/accessibility tree when closed (aria-modal=false but still there, no hidden attribute)
5. Listbox options missing role="option" and IDs (inferred from structure)

**Medium:**
6. No live region for result updates/empty state
7. No aria-controls on input linking to listbox
8. Date grouping likely breaks listbox semantics (group headers inside listbox without proper roles)
9. Body scroll not locked when open
10. Color contrast: yellow-200 highlight with text
11. Search icon missing aria-hidden (decorative)
12. Input should be combobox pattern or at least have aria-expanded
13. Close button SVG not hidden from AT (though button has aria-label, SVG should be aria-hidden)

**Low:**
14. prefers-reduced-motion not respected
15. Escape should clear query first before closing
16. Missing Home/End keys in listbox
17. Backdrop could have role="none presentation"
18. highlightIndex not bounded if filtered changes length (though it is reset)

Let me refine and make sure each row is actionable and references specific code locations.

Actually, looking at the code again:

- The `div` with `role="dialog"` has `onKeyDown={handleKeyDown}`. But `handleKeyDown` only handles arrow keys, enter, escape. Tab is not handled, so no focus trap.
- The input has no `aria-controls`, `aria-activedescendant`, or `role="combobox"`.
- The listbox `div` has `aria-label="ผลการค้นหา"` but no `id`.
- The `highlightText` uses `bg-yellow-200` which is a known contrast risk.
- The SVGs inside the input container: search icon has no `aria-hidden`, close button SVG has no `aria-hidden`.
- The backdrop has `aria-hidden="true"` which is good.
- The dialog is always rendered, just translated. With `aria-modal={isOpen}`, when false it's still a dialog in the accessibility tree. Should add `hidden={!isOpen}` or remove from DOM.

Wait, the component returns a fragment with the backdrop and panel. The panel is always rendered but visually hidden. This is bad for a11y because screen readers can still navigate to it. Should conditionally render or use `inert`/`hidden`.

Actually, looking at the transition: `isOpen ? 'translate-y-0' : '-translate-y-full'`. When closed, it's off-screen but still focusable and in the a11y tree. This is a major issue.

Also, the close button: the SVG inside should have `aria-hidden="true"` since the button has `aria-label`.

Let me draft the table rows:

| severity | location | issue | fix |
|---|---|---|---|
| Critical | Dialog container (`<div role="dialog">`) | Dialog remains in accessibility tree when closed (`-translate-y-full` only hides visually); screen readers and keyboard users can still reach off-canvas content. | Conditionally render `{isOpen && <div role="dialog"...>}` or add `hidden={!isOpen}` / `inert={!isOpen}` and remove `aria-modal` when closed. |
| Critical | Dialog container | No focus trap: Tab/Shift+Tab cycles to background page elements while modal is open, violating modal behavior. | Implement focus trap (e.g., `react-focus-lock` or manual loop on Tab key) and constrain Tab navigation within the panel. |
| High | `onClose` / component lifecycle | Focus is not restored to the trigger element that opened the search when the panel closes. | Store the active element before opening (`document.activeElement`) and call `.focus()` on it in `onClose` or cleanup. |
| High | Search `<input>` | Input lacks `aria-controls` linking to the listbox and `aria-activedescendant` to indicate the highlighted option to screen readers. | Add `aria-controls="search-results-listbox"` to the input, assign that `id` to the listbox, and set `aria-activedescendant={activeOptionId}` on the input. |
| High | Results items (inferred from `data-index` usage) | Listbox options are likely plain `<div>`s without `role="option"`, unique `id`s, or `aria-selected`, breaking screen reader navigation. | Render each result in a `<div role="option" aria-selected={isHighlighted} id={uniqueId}>` inside the listbox. |
| High | `highlightText` / `<mark>` | `bg-yellow-200` (light yellow) against dark text or `bg-yellow-700` against white may fail WCAG contrast requirements (3:1 for UI components, 4.5:1 for text). | Use a darker highlight background (e.g., `bg-yellow-400` or `bg-blue-200` with dark text) and verify contrast ratios. |
| Medium | Search `<input>` | Input acts as a combobox but lacks `role="combobox"`, `aria-expanded`, and `aria-autocomplete="list"`. | Add `role="combobox"`, `aria-expanded={filtered.length > 0}`, `aria-autocomplete="list"`, and `aria-haspopup="listbox"`. |
| Medium | Results list (`<div ref={listRef}>`) | No live region announces result count changes or empty state to screen reader users. | Add an `aria-live="polite"` region (visually hidden) that announces e.g. `${filtered.length} results found` or "No results". |
| Medium | `useEffect` (isOpen) | Body scroll is not locked when the panel opens, allowing background scrolling and disorientation. | Lock body scroll on open (`document.body.style.overflow = 'hidden'`) and restore on close. |
| Medium | Date grouping (`groupByDate`) | Date headers rendered inside `role="listbox"` without `role="group"`/`aria-label` create invalid ARIA children (group headers are not options). | Wrap each date section in a `<div role="group" aria-label={dateKey}>`, or move headers outside the listbox and use separate lists. |
| Medium | Decorative SVGs (search icon, close button icon) | Icons lack `aria-hidden="true"`; screen readers may announce them as unlabeled images or duplicate the button label. | Add `aria-hidden="true" focusable="false"` to all decorative SVGs. |
| Medium | Backdrop / overlay | Clicking backdrop closes the panel, but there is no keyboard equivalent (e.g., no `role="button"` or visible close control for keyboard-only users who can't easily reach the small X). | Ensure the close button is first in Tab order, or add a visible "Close" button; backdrop click is fine but not a substitute. |
| Low | Slide-down panel CSS | No `prefers-reduced-motion` guard; the `duration-300` translate animation can trigger vestibular disorders. | Wrap transition in `@media (prefers-reduced-motion: reduce)` or use `motion-safe:` Tailwind modifier to disable animation. |
| Low | `handleKeyDown` (Escape) | Escape immediately closes the panel; standard search UX first clears the query on first Escape, then closes on second. | Track query state: if `query` is non-empty, clear it on Escape; otherwise call `onClose()`. |
| Low | `handleKeyDown` (listbox) | Missing `Home` and `End` key support for listbox navigation (WCAG recommended for complex lists). | Add `Home` (jump to first) and `End` (jump to last) cases in `handleKeyDown`. |
| Low | Close `<button>` | Button relies solely on `aria-label="ปิด"`; if CSS fails or speech recognition is used, visible label is absent. | Add visually hidden text or a visible "Close" label for robustness, or ensure the icon is universally understood. |

Wait, I need to check if the close button is actually an issue. It has an aria-label, which is acceptable. But visible label is better. I'll keep it as Low.

Also, I should check the input disabled state. When `isOpen` becomes false, the input is disabled but still in the DOM inside the hidden panel. That's okay since the panel itself should be hidden.

Another issue: The `debounce` helper uses `NodeJS.Timeout` which might cause issues in browser environments (should be `ReturnType<typeof setTimeout>`), but that's TypeScript not a11y.

Another issue: The `listRef` div has `role="listbox"` but if there are no results, it might be empty. An empty listbox is fine, but should probably have `aria-empty` or just be hidden. Not a big issue.

Let me reconsider the "High" vs "Critical" severity. I'll use: Critical, High, Medium, Low.

Critical: Blocks usage for screen reader/keyboard users.
- Dialog in DOM when closed (can navigate to off-screen content)
- No focus trap (can tab out)
- No focus restoration

High: Major inconvenience or WCAG failure.
- Missing aria-activedescendant/controls (screen reader doesn't know what's selected)
- Options missing role/ids
- Color contrast

Medium: Noticeable issues.
- Live region
- Body scroll
- Combobox role
- Grouping semantics
- Decorative icons
- Backdrop keyboard equivalent

Low: Enhancements.
- Motion
- Escape behavior
- Home/End
- Visible close label

I should make sure the table is well-formatted markdown.

One more thing: The `input` has `disabled={!isOpen}`. When the panel is animating closed or if `isOpen` is false but the panel is still transitioning, the input becomes disabled and loses focus, which can be jarring. But since it's inside a dialog that should be removed/hidden, this is secondary.

Also, the `aria-label` on the dialog is in Thai ("ค้นหาประวัติการสนทนา") which is good for i18n, but if the page language isn't set to Thai (`lang="th"`), screen readers might mispronounce it. But that's outside this component.

Let me double-check the SVG close button. The button has `aria-label="ปิด"`. The SVG inside should definitely be `aria-hidden="true"` because some screen readers might read the SVG title/desc if present, or treat it as an image. Since it has no title, it might be ignored, but `aria-hidden` is safer.

The search icon SVG has no `aria-label` and is decorative, so it should be `aria-hidden="true"`.

Another issue: The input placeholder is in Thai ("ค้นหาบทสนทนา..."). Placeholders are not substitutes for labels. The input lacks an associated `<label>` or `aria-label`. Wait, looking at the code: the input has `placeholder="ค้นหาบทสนทนา..."` but no `aria-label` or visible `<label>`. This is a significant issue! Placeholders are not accessible as labels because they disappear when typing and are often not read properly. I should add this as High or Medium.

Actually, the dialog has `aria-label="ค้นหาประวัติการสนทนา"`, but the input itself needs a label. Since it's a search input, it should have an `aria-label` or a visually hidden label.

Location: `<input>` - missing accessible label (placeholder only).
Fix: Add `aria-label="ค้นหาบทสนทนา"` or `<label htmlFor="search-input" className="sr-only">...</label>`.

I'll add this.

Also, the listbox has `aria-label="ผลการค้นหา"` which is good.

Let me reconsider the date grouping. If the rendered output puts `<div>` date headers inside the `role="listbox"`, those headers are not valid children. The listbox should only contain options or option groups. So if they render:
```jsx
<div role="listbox">
  <div>Date Header</div>
  <div role="option">...</div>
</div>
```
That's invalid. They should use `role="group"` with `aria-label` for each date section.

Another issue: The `highlightText` function returns `<mark>` elements. `<mark>` has implicit role, but in a listbox option, the text content is fine. However, the styling `bg-yellow-200` is the issue.

Let me also check if there's an issue with the `useEffect` that sets `highlightIndex(0)` inside `debouncedSearch`. If `filtered` becomes empty, `highlightIndex` is 0, but `handleKeyDown` checks `total === 0` and returns. That's fine.

One more issue: The `handleKeyDown` is attached to the dialog `div`, but the input is inside it. The input's own keydown behavior (e.g., cursor movement) might conflict. For example, ArrowDown in an input normally moves cursor to end or nothing, but here it navigates the list. That's actually standard for comboboxes. But the input should probably handle the keys directly rather than relying on bubbling, or have the handler on the input specifically. Currently it's on the outer div which is fine as long as it bubbles, but if focus is on the close button, arrow keys still navigate the list? That might be weird. Actually, if focus is on the close button and user presses ArrowDown, it will navigate the list. That's unexpected. The handler should probably be on the input specifically, or check `e.target` to ensure it's the input. But this is a minor UX issue.

Actually, looking at the code: `onKeyDown={handleKeyDown}` is on the dialog container. So if focus is on the close button and I press Escape, it closes (good). If I press ArrowDown, it highlights the list (maybe okay, but unexpected). I'll mention this as a low issue or skip it.

Let me finalize the table. I'll aim for around 12-15 rows covering the most important issues.

Row 1: Critical - Dialog hidden only visually (translate) but still in a11y tree.
Row 2: Critical - No focus trap.
Row 3: High - No focus restoration.
Row 4: High - Input missing accessible label (placeholder only).
Row 5: High - Missing aria-activedescendant/controls on input
