<!-- cc-team deliverable
 group: G1 (a11y division)
 member: A11Y-011 role=a11y model=deepseek/deepseek-v4-flash
 finish_reason: stop | tokens: {"prompt_tokens":2540,"completion_tokens":2269,"total_tokens":4809,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":898,"image_tokens":0},"cache_creation_input_tokens":0} | 27s
 generated: 2026-06-13T12:09:47.426Z -->
Here’s the accessibility and UX audit table for the `AgentWorkspacePanel` component, based on the visible code and typical patterns in the truncated portion.

| Severity | Location                             | Issue                                                                                                                                                                                                                                                                                                                                                                                                                 | Fix                                                                                                                                                                                                                                                                                                          |
| -------- | ------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Critical | Progress bar (likely `<div>` with percentage width)   | No ARIA role for progressbar. Screen readers cannot announce progress value or state. Also missing `aria-valuenow`, `aria-valuemin`, `aria-valuemax`, and an accessible label.                                                                                                                                                                                                                                                     | Add `role="progressbar"`, `aria-valuenow={progressPct}`, `aria-valuemin="0"`, `aria-valuemax="100"`, and `aria-label="Task progress"` (or use `aria-labelledby` pointing to the header).                                                                                                                      |
| Critical | `activeToolEvent` / step tracker     | The step item rendering (code not fully shown) likely uses `<div>` elements instead of a semantic list. Non‑semantic markup reduces assistive‑technology navigation options and comprehension of the step sequence.                                                                                                                                                                                                    | Wrap steps in `<ul>` with `<li>` items, each containing the step label. Use CSS to remove list styling. Alternatively, use `role="list"` and `role="listitem"` if divs must remain.                                                                                                                              |
| High     | Collapse/expand button               | The collapse button (`onClick={() => setCollapsed(true)}`) and the expand button inside the collapsed view lack `aria-expanded` attribute. Screen readers cannot tell whether the panel is expanded or collapsed.                                                                                                                                                                                                       | Add `aria-expanded={!collapsed}` to both buttons. Also set `aria-controls` pointing to the panel container’s `id` (give the panel an `id`).                                                                                                                                                                |
| High     | Focus management on collapse/expand  | When the panel collapses, the expand button (now visible) does not receive focus automatically. Keyboard users are left without a clear focus target and may tab into invisible content. Similarly, when expanding, focus should move to the panel heading or first interactive element.                                                                                                                                | Use `useRef` for the expand button and, after collaps state change (in a `useEffect` that runs after render), call `expandButtonRef.current?.focus()`. For expanding, focus a ref on the panel header or the collapse button.                                                                              |
| Medium   | Timer display                        | The timer is plain text (no ARIA attributes). While it is readable, dynamic updates (every second) may not be announced to screen readers because it lacks `aria-live`. Users who rely on auditory feedback will not be informed of the elapsed time changes.                                                                                                                                                           | Wrap the timer `<span>` in a container with `aria-live="polite"` and `aria-atomic="true"` to announce whole value changes. Alternatively, use `role="timer"` (though support varies).                                                                                                                          |
| Medium   | Live indicator (“LIVE” with ●)       | The “●” character is purely decorative but is inside the visible content. Screen readers may read “bullet” or ignore it. The `aria-live` region is on the outer `<span>`, but the inner `<span>` with the dot may still be announced inconsistently. Also the word “LIVE” might not be translated for non-Thai users.                                                                                                     | Add `aria-hidden="true"` to the decorative dot `<span>`. Keep the text “LIVE” inside a visually hidden `<span>` that is the actual content for assistive technology, or use CSS to hide the dot from accessibility tree. Consider adding an `aria-label` to the outer span for clarity. |
| Medium   | Step tracker progress percentage     | The progress is calculated as `Math.round(((total - 1) / Math.max(total, 3)) * 100)`. This formula can produce values below 0 or above 100 in edge cases, and may misrepresent actual completion. Moreover, the percentage is not exposed to screen readers (see progress bar issue above).                                                                                                                            | Fix the formula to properly reflect the completion of steps. Then apply the ARIA progress attributes as described above.                                                                                                                                                                                       |
| Low      | Color contrast                       | Component uses Tailwind classes like `text-muted-foreground` and `animate-pulse`. These may have low contrast against the background, especially for the small “LIVE” label or the collapse button. While not testable here without theme values, this is a common risk with muted colors.                                                                                                                            | Verify contrast ratio (≥4.5:1 for normal text, ≥3:1 for large text) against the background. Replace `text-muted-foreground` with a higher‑contrast class if needed. For the blinking dot, ensure it meets contrast when static.                                                                             |
| Low      | ARIA‑live placement                  | The `aria-live="polite"` is only on the live indicator `<span>`. Additional live announcements (e.g., step changes, task completion) do not have a dedicated live region. Users of screen readers may miss important updates that occur elsewhere in the panel.                                                                                          | Create a dedicated live region (e.g., a container with `aria-live="polite"` and `aria-atomic="true"`) that holds a status message updated whenever a step changes or task finishes. Update it using `useEffect`.                                                                                                |
| Low      | Missing `alt` on icons               | No images are visible in the snippet, but if any `<img>` or icon font elements are used in the step tracker (e.g., emojis), they may lack accessible text. Emoji characters are readable by some screen readers but not all; they should be accompanied by visually hidden text if the emoji conveys meaning.                                                                                                          | For emojis used as badges (🖥️🌐📄📊), add a `title` attribute or wrap them in a `<span>` with `aria-label` describing the tool type. For example, `<span aria-label="Terminal tool" role="img">🖥️</span>`.                                                                                                   |

**Note**: The audit assumes the truncated portion includes a progress bar and a step list that are common in such panels. If the actual markup differs, the severity and fixes should be adjusted accordingly. Always validate with actual rendered DOM and user testing.
