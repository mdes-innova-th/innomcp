<!-- cc-team deliverable
 group: G1 (a11y division)
 member: A11Y-013 role=a11y model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":1871,"completion_tokens":4350,"total_tokens":6221,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":3327,"image_tokens":0},"cache_creation_input_tokens":0} | 63s
 generated: 2026-06-13T12:13:00.995Z -->
| Severity | Location | Issue | Fix |
|----------|----------|-------|-----|
| **Critical** | Dialog container (`<div className="fixed … z-[90] …">`) | Missing `role`, `aria-modal`, `aria-labelledby`, and `aria-describedby`. Screen readers don't recognise the dialog as a modal or convey its purpose. | Add `role="alertdialog"` (it demands immediate response), `aria-modal="true"`, `aria-labelledby="approval-title"`, and `aria-describedby="approval-desc"`. |
| **Critical** | Dialog mount / focus lifecycle | No focus is sent to the dialog when it opens; focus stays on the trigger element. Keyboard users can <kbd>Tab</kbd> out of the dialog and interact with fully‑hidden background elements, creating a focus trap and keyboard‑nav break. | When `request` becomes non‑null, programmatically focus the dialog container (or the first focusable element, e.g. the “deny” button). Implement a focus‑trap that cycles inside the dialog. Store the previously focused element and restore focus when the dialog closes. |
| **High** | Page background (any sibling outside the dialog) | With `aria-modal="true"` alone, assistive tech may still encounter background content. Without additional hiding, screen‑reader users can navigate to off‑screen interactive elements. | Add `aria-hidden="true"` to the main app container (or `inert` attribute) while the dialog is displayed. If possible, use the native `<dialog>` element with `.showModal()` for built‑in background inertness. |
| **High** | Title text `<p className="… font-semibold …">` | The dialog title is a plain paragraph, not a heading. It cannot be referenced by `aria-labelledby` and breaks the heading hierarchy. | Replace with `<h2 id="approval-title" className="…">` and use the same `id` in `aria-labelledby`. Keep the visual styles unchanged. |
| **Medium** | Command code block `<code>` | The command is presented without an accessible label. Screen readers may read raw text with no indication of its purpose (“rm -rf /” alone is not contextualised). | Add `aria-label="Command to be executed"` to the `<code>` element, or include a visually‑hidden label and associate it with `aria-labelledby`. |
| **Medium** | Various colour‑dependent elements | Several text foreground/background combinations risk failing WCAG 2.1 AA contrast (≥4.5:1 for normal text, ≥3:1 for large). Examples: risk‑badge text (`text-sky-700` on `bg-sky-500/8`), tool‑type badge (`text-amber-700` on `bg-amber-500/10`), command block (`text-foreground/80` on `bg-muted/60`), deny button (`text-muted-foreground` on default background). | Validate each contrast pair with a tool (e.g., axe‑core, Colour Contrast Analyser). Adjust Tailwind opacity/saturation or swap palette tokens to meet minimum ratios in both light and dark modes. |
| **Medium** | “Tool:” info line (`<span className="font-mono">`) | The tool name is only visually separated. A screen reader may not distinguish it from the preceding text. | Wrap the “Tool:” label and the tool name in a definition or use a labelledby pattern. For example: `<span id="tool-label">Tool:</span> <span aria-labelledby="tool-label">{request.tool}</span>`. |
| **Low** | Approve and Deny buttons | Emojis (❌, ✅) are read literally by screen readers (e.g., “cross mark ปฏิเสธ”), which may be redundant. | Add `aria-hidden="true"` to the emojis and rely on the Thai text alone for the accessible name. The buttons also lack `type="button"`; add it to prevent accidental form submission if ever placed inside a `<form>`. |
| **Low** | Small helper text (`กด Escape เพื่อปฏิเสธ`) | 10 px text may be hard to read for low‑vision users and isn’t announced as part of the dialog description. | Associate it with the dialog via `aria-describedby` if it’s important, or increase the font size to at least 12 px for readability. |
| **Low** | Tool‑type badge emoji (🖥️, 🌐, etc.) | Decorative emojis are read by screen readers, adding noise. | Apply `aria-hidden="true"` to these emojis; the text label is sufficient. |
