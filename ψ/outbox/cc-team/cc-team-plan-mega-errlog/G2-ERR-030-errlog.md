<!-- cc-team deliverable
 group: G2 (errlog division)
 member: ERR-030 role=errlog model=MiniMaxAI/MiniMax-M3
 finish_reason: stop | tokens: {"prompt_tokens":3463,"completion_tokens":1910,"total_tokens":5373,"prompt_tokens_details":{"cached_tokens":114,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":0,"image_tokens":0},"cache_creation_input_tokens":0} | 55s
 generated: 2026-06-13T12:05:24.722Z -->
# Error-Handling & Logging Audit: `promptAdapter.ts`

> **Note:** The provided code is truncated mid-statement (ends inside a GLOSSARY entry: `"ยืนกลาง": "standing`). Issues below cover what is visible. The file is also notable for **having no logging, no try/catch, and no timeout handling at all** — this audit is dominated by absences.

| # | Severity | Location | Issue | Fix |
|---|----------|----------|-------|-----|
| 1 | 🔴 **High** | Entire file — no `try/catch` anywhere | `adaptImagePrompt` and `normalizePlannerQuery` perform no error handling. A throw from `normalizeThaiQuery` (e.g., non-string input, internal regex crash, or LLM call failure downstream) will produce an **unhandled promise rejection** at the async boundary. | Wrap the core logic in `try/catch` in both exported functions; on error, return a degraded `passthrough` result `{ mode: "passthrough", confidence: 0, reasons: ["adapter-error: <class>"], latencyMs, … }` and propagate the throw **only** if a caller-flag demands strict mode. Never let it bubble silently. |
| 2 | 🔴 **High** | `adaptImagePrompt` → LLM fallback path (env-gated branch) | Comment promises *"short, JSON-only, with strict timeout"* but the file as shown contains **no `AbortController`, no `setTimeout`/`AbortSignal.timeout`, and no `fetch`/`openai` call wrapping**. If the LLM call is added later without a timeout, requests can hang and exhaust the event loop. | Add `const ctl = new AbortController(); const t = setTimeout(() => ctl.abort(), LLM_TIMEOUT_MS);` and pass `signal: ctl.signal` to the fetch; always `clearTimeout(t)` in `finally`. On `AbortError`, fall back to deterministic result. |
| 3 | 🟠 **Medium** | Entire file | **Zero logging.** No `console.*`, no structured logger, no correlation ID. The exported `latencyMs` field is emitted in the return value, but there is no record of (a) which mode was selected, (b) which glossary entries fired, (c) why `llm-fallback` was skipped (env unset? error?), (d) error context for any caught throw. This makes production incidents undebuggable. | Introduce a tiny logger (e.g., `pino` or a project-standard `log`) and emit: `debug` on every glossary hit, `info` on mode selection with `latencyMs` and `originalPrompt.length` (not content — see #5), `warn` on degraded fallbacks, `error` with `{ err, mode, stage }` on caught exceptions. |
| 4 | 🟠 **Medium** | `stripImageCommand` (line ~40) | Swallowed/over-permissive logic: the `for (let i = 0; i < 2; i++)` filler-strip loop silently consumes up to 2 tokens with no observability. A user prompt `"สร้างรูป ของ ที่ ให้ แมว"` (4 fillers) will be partially stripped with no signal. Not a "catch", but the same anti-pattern: silent data loss. | Cap filler-stripping with an explicit reason code: push `"stripped-filler:<token>"` into `reasons[]`; if more than 2 fillers detected, push `"filler-truncated"` so callers know data was discarded. |
| 5 | 🟠 **Medium** | Logging field design (entire file) | When logging is added (per #3), there is a real risk of **logging the raw `originalPrompt` / `rawQuery` verbatim**. Thai user input is highly likely to contain **PII** (real names, phone numbers, addresses in chat) and occasionally **secrets pasted by mistake** (API keys, tokens). The `reasons` array, if it ever echoes matched fragments, would leak them too. | Add a redaction helper (`redactPII(s)` masking emails, phone patterns, and `\b[A-Za-z0-9_-]{20,}\b` key-shape strings). Log only `originalPrompt.length`, `originalPrompt.hash` (sha-256 truncated), and `language: "th"\|"en"`. Never log `adaptedPromptEn` verbatim at `info+` levels — gate behind `debug` with redaction. |
| 6 | 🟡 **Low** | Public API surface (top-of-file docstring) | Doc claims *"strict timeout"* and *"JSON-only"* guarantees but the type signatures do not reflect any failure mode. Callers cannot distinguish a confident deterministic result from a `passthrough` fallback caused by an internal error. | Either (a) add a `degraded: boolean` flag to both result interfaces, or (b) return a discriminated union: `Result \| { ok: false; error: AdapterError; latencyMs }`. Document the failure contract in the JSDoc. |
| 7 | 🟡 **Low** | `normalizePlannerQuery` (not shown, inferred from signature) | Likely mirrors the image-prompt function. Risk of **duplicated error-handling logic** that drifts over time — a classic source of inconsistent log levels (one path `console.error`, the other `logger.warn`). | Extract a shared `runAdapter<T>(fn, raw, kind): Promise<T>` helper that owns try/catch, logging, latency timing, and redaction. Both public functions become thin wrappers. |
| 8 | 🟡 **Low** | `GLOSSARY` array (~120 entries) | Glossary-driven translation has **no input length cap**. A pathological 1 MB string will cause the linear scan + N regex matches to block the event loop for seconds. Combined with no timeout (#2), this is a DoS surface. | At function entry, enforce `if (raw.length > MAX_PROMPT_LEN) return passthrough(...)` and log `warn` with redacted length. Suggested: 2 KB for image prompts, 1 KB for planner queries. |
| 9 | 🟢 **Info** | `IMAGE_COMMAND_PREFIX_RE` etc. (line ~28) | Regexes are constructed at module load with no try/catch around them. Unlikely to fail, but if any future maintainer writes an invalid pattern (e.g., unclosed group), the whole module fails to import — cascading 500s on cold start. | Wrap regex literals in a `try/catch` at module init and assign to `null` on failure, with a startup `error` log; have `stripImageCommand` short-circuit to input passthrough when null. |
| 10 | 🟢 **Info** | `confidence` field (return type) | No defined scale or convention. Callers cannot tell `0.3` (low) from `0.99` (high) without reading the source. Future log lines saying `"confidence": 0.5"` will be unactionable. | Document the scale in the JSDoc (e.g., `0 = no signal, 1 = exact glossary match`) and, when logging, include the **reason codes** that produced the score so the number is interpretable. |
| 11 | 🟢 **Info** | Module header comment | States *"Deterministic-first: never call LLM for ordinary chat"* and *"LLM fallback is opt-in (env-gated)"* — good — but does not state the **default behavior on env var being malformed or unset**. | Document the env var name and the exact fallback chain (`unset → deterministic-only`, `set+invalid → log error, deterministic-only`, `set+valid → use with timeout`). |

---

## Summary

**Most critical gaps (do these first):**
1. Add a `try/catch` in both public functions with a `passthrough` fallback (issue #1).
2. Add the `AbortController` timeout the docstring already promises (issue #2).
3. Introduce structured logging with **PII redaction** before the first production deployment (issues #3, #5).

**Architectural smell:** Issues #1, #3, #7 all point to the same root cause — the file has no `runAdapter` helper. Adding one fixes three classes of bug at once and prevents the "two functions drift in error handling" pattern that produces inconsistent log levels over time.

**Missing from the audit (out of scope of this file but worth flagging):**
- The `llm-fallback` branch itself isn't visible in the truncated snippet — re-audit once that code is added; expect to find JSON-parse `catch` blocks that swallow malformed LLM output silently (a very common shape for this pattern).
- No `package.json` or caller-code shown, so upstream timeout/`Promise.race` wrappers are not assessable.
