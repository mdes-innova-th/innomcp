<!-- cc-team deliverable
 group: G1 (serverdeep division)
 member: SERV-022 role=audit model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":2323,"completion_tokens":5659,"total_tokens":7982,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":4879,"image_tokens":0},"cache_creation_input_tokens":0} | 104s
 generated: 2026-06-13T12:16:03.798Z -->
| severity | location | issue | fix |
|---|---|---|---|
| High | `mcpError` / `mcpText` | **MCP Protocol / Error Envelope**: `mcpError` omits `isError: true`, causing MCP clients to treat tool failures as successful executions. Additionally, `structuredContent` is non-standard in the MCP `CallToolResult` spec and will be stripped or cause strict validation errors. | Add `isError: true` to the returned object in `mcpError`. Replace `structuredContent` with the standard `_meta` field, or serialize the structured payload directly into the `text` content. |
| High | `execute` (`nip_top_isp_this_month`) | **URL Construction Bug**: `ispQs` is prefixed with `&` (`&isp=...`). If the truncated base path doesn't already contain a `?` (e.g., `?month=...`), this creates a malformed URL (`/path&isp=...`), resulting in 404s or silently ignored filters. | Verify the base path includes `?`. If not, change `&isp=` to `?isp=`, or preferably use `URL` / `URLSearchParams` to safely construct the query string. |
| Medium | `callDetectAPI` | **Resource Leak / OOM Risk**: `await res.text()` reads the *entire* error response body into memory before applying `.slice(0, 200)`. A misconfigured upstream API returning a massive HTML error dump can cause a severe memory spike or OOM crash. | Use `res.statusText` instead, or consume the stream with a byte limit (e.g., using a `ReadableStream` reader) to safely truncate large error bodies without buffering them entirely. |
| Medium | `execute` | **Unhandled Rejection / Type Safety**: `args` is typed as `any`. If the MCP SDK passes `null` or `undefined` (e.g., on malformed JSON-RPC payloads), destructuring `const { action } = args;` will throw an unhandled `TypeError`, crashing the tool handler. | Type `args` properly using `z.infer<typeof inputSchema>` and add a defensive guard at the top: `if (!args) return mcpError(...)`. |
| Medium | `callDetectAPI` | **Missing Timeout on Body Parsing**: The `AbortController` signal primarily governs the connection/headers phase. If the upstream server sends headers immediately but stalls the body stream, `res.json()` or `res.text()` can hang indefinitely, bypassing the timeout. | Wrap `res.json()` / `res.text()` in a `Promise.race` with a secondary timeout, or ensure the `signal` is explicitly passed to the body consumption methods if supported by your specific Node.js/undici version. |
| Low | `execute` (`evidence_records_yesterday_by_isp_top`) | **Logic Edge Case**: The "Others" (`อื่นๆ`) row is unconditionally pushed to `tableRows`, even when `total === sumTop`, resulting in a useless UI row with `count: 0`. | Conditionally push the "Others" row only if `total > sumTop`. |
| Low | `bkkMonth` | **Timezone Brittleness**: Hardcoding `+ 7 * 3600_000` for Bangkok time is brittle, ignores exact millisecond boundary edge cases, and doesn't account for potential future timezone rule changes. | Use a robust date library like `date-fns-tz` or `luxon` (e.g., `DateTime.now().setZone('Asia/Bangkok')`). |
