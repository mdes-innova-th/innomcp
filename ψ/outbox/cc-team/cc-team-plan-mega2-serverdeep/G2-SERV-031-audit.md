<!-- cc-team deliverable
 group: G2 (serverdeep division)
 member: SERV-031 role=audit model=deepseek/deepseek-v4-flash
 finish_reason: stop | tokens: {"prompt_tokens":2905,"completion_tokens":3025,"total_tokens":5930,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2232,"image_tokens":0},"cache_creation_input_tokens":0} | 35s
 generated: 2026-06-13T12:17:33.983Z -->
| Severity | Location | Issue | Fix |
|----------|----------|-------|-----|
| **Critical** | All `execute` functions (nwpHourlyByLocationTool, nwpHourlyByPlaceTool, nwpHourlyByRegionTool) | Error responses lack `isError: true` flag per MCP specification. Clients will treat errors as successful tool invocations, misleading the LLM and potentially causing incorrect weather data to be displayed. | Add `isError: true` to every error response object, e.g., `return { content: [...], isError: true };` in both `safeParse` failure and `catch` blocks. |
| **High** | `execute` functions (where axios calls are made – truncated but implied) | No request timeout is applied to axios calls despite `DEFAULT_TIMEOUT` being defined. If the TMD API hangs or becomes unresponsive, the tool will hang indefinitely, blocking the MCP server and causing timeouts at the MCP level. | Attach `timeout: DEFAULT_TIMEOUT` (or `timeout: 15000`) to all axios request configurations, and also add an `AbortSignal` timeout as a safety net. |
| **Medium** | `extractHourlyEntries` function (line ~79) | Returns an empty array when the API response structure is unexpected (none of the candidate keys match). No error is raised, and the tool will return a success response with empty data, potentially confusing the LLM. | After `extractHourlyEntries`, validate that the resulting array is non‑empty. If empty, throw a descriptive error (e.g., `new Error("Unexpected API response structure – no forecast entries found")`) so the catch block returns a proper error. |
| **Medium** | `buildQueryParams` and field handling (line ~126) | If `fields` array is empty (e.g., user passes `[]`), `fields.join(",")` produces an empty string, which may cause the API to return all fields (if supported) or error (if empty not allowed). Either result could lead to unexpected behavior. | Default `fields` to a sensible set (e.g., `["tc","rh","cond"]`) or validate that `fields` is non‑empty before joining. |
| **Medium** | Timezone calculation (lines 131–133, `today` and `hour` definitions) | Uses `Date.now() + 7 * 3600 * 1000` to derive Thai local date/hour. This assumes the server’s system time is UTC. If the server runs in a different timezone (e.g., Thailand itself), the offset is applied twice, yielding incorrect results. | Use a dedicated timezone library (e.g., `Intl.DateTimeFormat` with `timeZone: 'Asia/Bangkok'` or `luxon`) to reliably compute the current Thai date/hour regardless of server locale. |
| **Low** | `getNwpApiKey` function | Environment variable `SMOKE_MODE`, `WEATHER_FIXTURE_W1`, `CHAT_TRACE_QA` are checked without documentation or clear purpose. Logic to block demo keys in live mode is fragile – if an env var is accidentally set, production keys may be unexpectedly blocked. | Document these variables and consider a more robust mechanism (e.g., separate API key validation endpoint or explicit configuration). |
| **Low** | All `execute` functions | No support for `AbortSignal` or request cancellation. MCP allows clients to abort tool execution; ignoring this could leave hanging requests and resource leaks. | Pass an optional `AbortSignal` from the MCP context (if available) to axios via `signal` option, and handle cancellation gracefully. |
