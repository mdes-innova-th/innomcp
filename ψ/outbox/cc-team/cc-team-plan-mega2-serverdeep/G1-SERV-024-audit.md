<!-- cc-team deliverable
 group: G1 (serverdeep division)
 member: SERV-024 role=audit model=zai-org/GLM-5.1
 finish_reason: stop | tokens: {"prompt_tokens":1698,"completion_tokens":2225,"total_tokens":3923,"prompt_tokens_details":{"cached_tokens":32,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1469,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 24s
 generated: 2026-06-13T12:15:00.775Z -->
| severity | location | issue | fix |
|---|---|---|---|
| **HIGH** | `searchGovData` → `fetch()` | **No request timeout.** If `catalog.data.gov` hangs, the MCP call blocks indefinitely with no cancellation path. | Create an `AbortController`, wire `signal` into `fetch`, and race it with `setTimeout` (e.g. 15 s). Clean up the timer in a `finally` block. |
| **HIGH** | `execute` (both branches) | **Missing MCP `isError` flag.** Validation errors and runtime errors are returned as plain `content` without `isError: true`, so MCP clients cannot distinguish failure from success. | Return `{ content: […], isError: true }` for every error path (validation failure, API error, caught exception). |
| **MEDIUM** | `searchGovData` → `!response.ok` branch | **Response body never consumed or aborted.** Throwing without draining/aborting the body leaks the underlying TCP connection back to the pool. | Call `response.body?.cancel()` (or `await response.text()`) before throwing, or reuse the same `AbortController` signal to abort. |
| **MEDIUM** | `searchGovData` → `catch` block | **`JSON.stringify` inside `catch` can itself throw** (e.g. circular references on `error`), producing an unhandled rejection that crashes the MCP transport. | Wrap the catch-body in a secondary try/catch; fall back to a hard-coded string if stringification fails. |
| **MEDIUM** | `searchGovData` → `fetch()` | **No `AbortController` at all.** Server shutdown / client cancellation cannot abort in-flight requests. | Accept an `AbortSignal` parameter (or create one from `AbortController.timeout()`), pass it to `fetch`, and surface abort errors via the same error envelope. |
| **MEDIUM** | `execute` → `searchGovData(parsed.data)` | **If `searchGovData` were to throw past its own catch** (e.g. `JSON.stringify` failure in the happy path), the rejection propagates unhandled to the MCP transport, which may terminate the session. | Wrap the `await searchGovData(…)` call in a try/catch that returns an `isError: true` content result. |
| **LOW** | `formatGovData` → `stripHtml(notes)` | **`stripHtml` invoked twice per dataset** (once for `.slice(0,240)`, once for `.length > 240`). Wasteful on large result sets. | Cache: `const clean = stripHtml(notes);` then use `clean.slice(…)` / `clean.length`. |
| **LOW** | `formatGovData` | **No overall output size cap.** With `rows=100` and many resources per dataset, the text payload can exceed practical MCP message limits. | Truncate the final string to a sane ceiling (e.g. 32 KB) or cap the number of resources/datasets rendered. |
| **LOW** | `searchGovData` → `catch` | **`error` typed as `any`**; loses type safety and can mask non-Error throws. | Type as `unknown`; use `error instanceof Error ? error.message : String(error)`. |
| **LOW** | `GovDataToolInputSchema` → `.default(10)` | **Zod `.default()` may not round-trip to MCP JSON Schema correctly** depending on the zod-to-json-schema version used by the MCP SDK, causing clients to omit `rows` and receive a schema validation error. | Verify the schema conversion includes `"default": 10`; if not, set the default inside `execute` manually after parsing. |
| **INFO** | `catalogBaseUrl()` | **No URL validation.** A malformed `DATAGOV_CATALOG_BASE_URL` (e.g. missing protocol) causes `new URL()` to throw inside the already-running request, producing a confusing error message. | Validate/normalise the URL at module load or on first call; surface a clear config error. |
