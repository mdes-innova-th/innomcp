<!-- cc-team deliverable
 group: G2 (serverdeep division)
 member: SERV-047 role=audit model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":2210,"completion_tokens":5756,"total_tokens":7966,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":4867,"image_tokens":0},"cache_creation_input_tokens":0} | 107s
 generated: 2026-06-13T12:25:31.010Z -->
| severity | location | issue | fix |
|---|---|---|---|
| High | `fetch` calls (CSRF & POST) | **Missing Timeouts**: Node.js `fetch` has no default timeout. If the backend accepts the connection but hangs, the MCP tool call blocks indefinitely, risking client timeouts and event loop/resource exhaustion. | Add `signal: AbortSignal.timeout(15000)` (or appropriate ms) to the options object of both `fetch` calls. |
| High | `catch` blocks (Error returns) | **Error Envelope Correctness**: Returning error messages in `content` without `isError: true` violates the MCP protocol. Clients will interpret the error as a successful tool execution that just happened to return an error string, confusing the LLM. | Add `isError: true` to the returned object in all `catch` block returns (e.g., `return { content: [...], isError: true }`). |
| High | `!csrfRes.ok` / `!postRes.ok` checks | **Resource Leaks (Unhandled Body)**: Throwing immediately on non-OK responses without consuming the response body prevents Node's `undici` from releasing the socket back to the connection pool, causing connection leaks under load. | Consume or cancel the body before throwing: `await res.body?.cancel()` or `await res.text()` prior to the `throw new Error(...)`. |
| Medium | `registerTool` definition | **MCP Protocol Edge Cases (Missing Schema)**: The tool lacks an `inputSchema` (Zod). The handler uses `args: any`, bypassing MCP input validation. Malformed LLM inputs will cause runtime crashes or unexpected backend behavior. | Add `inputSchema: z.object({ query: z.string().optional() })` to the tool configuration and type `args` using `z.infer<typeof inputSchema>`. |
| Medium | `set-cookie` header extraction | **Cookie Parsing Bug**: `Headers.get("set-cookie")` returns a single comma-joined string (which breaks on `Expires` dates) and never returns an array. The `else` block is unreachable. This results in malformed `Cookie` headers being sent to the backend. | Replace the entire manual parsing block with `const setCookieHeaders = csrfRes.headers.getSetCookie?.() || [];` (available in Node 18+ undici). |
| Medium | `await res.json()` calls | **Unhandled JSON Parse Errors**: If the backend returns a non-JSON response (e.g., 502 HTML from a reverse proxy), `.json()` throws a generic `SyntaxError`, resulting in an unhelpful "Unexpected token" error message for the user. | Read as text first (`const text = await res.text()`), then `JSON.parse(text)`. Catch parse errors and throw a descriptive error including a snippet of the raw text. |
| Low | `catch` block error inspection | **Fragile Error Cause Checking**: Checking `error.cause?.code === 'ECONNREFUSED'` is brittle. Network errors in Node 18+ `fetch` are wrapped in `TypeError`, and the code misses other critical network failures like `ENOTFOUND` or `ECONNRESET`. | Standardize checking: `const code = error.cause?.code || error.code; if (['ECONNREFUSED', 'ENOTFOUND', 'ECONNRESET'].includes(code)) ...` |
| Low | Tool Handler Signature | **Type Safety**: Using `args: any` and casting to `WebdInput` defeats TypeScript's purpose and requires manual, unsafe casting inside the handler. | Remove `any` and use the inferred type from the Zod schema (e.g., `async (args: z.infer<typeof inputSchema>)`). |
| Low | Concurrent Tool Calls | **Potential CSRF Race Condition**: Fetching a new CSRF token and session per request is inefficient. If the backend invalidates previous tokens upon generating a new one (common in some frameworks), concurrent tool calls will fail with 403 Forbidden. | Cache the CSRF token and cookies in a module-level variable with a TTL, or ensure the backend supports multiple active tokens per API key. |
