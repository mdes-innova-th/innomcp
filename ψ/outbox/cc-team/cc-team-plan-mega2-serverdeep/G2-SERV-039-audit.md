<!-- cc-team deliverable
 group: G2 (serverdeep division)
 member: SERV-039 role=audit model=zai-org/GLM-5.1
 finish_reason: stop | tokens: {"prompt_tokens":2531,"completion_tokens":3802,"total_tokens":6333,"prompt_tokens_details":{"cached_tokens":32,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2808,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 64s
 generated: 2026-06-13T12:20:36.364Z -->
| severity | location | issue | fix |
|---|---|---|---|
| **CRITICAL** | `MariaDbHistoryDb.search()` catch block | **Unhandled rejection**: if the fallback `LIKE` query inside `catch {}` also throws, the rejection propagates unhandled — no outer try/catch exists, and the caller may not catch it either. | Wrap the fallback query in its own try/catch; return `[]` on double-failure rather than letting a rejection escape. |
| **CRITICAL** | `MariaDbHistoryDb.search()` — `like` construction | **LIKE wildcard injection**: user input containing `%` or `_` is not escaped before wrapping in `%…%`. Searching `"100%"` becomes `LIKE '%100%%'`, matching far more than intended. | Escape LIKE metacharacters: `const escaped = q.replace(/[%_]/g, '\\$&'); const like = \`%${escaped}%\`;` |
| **HIGH** | `MariaDbHistoryDb.search()` — both query calls | **No query timeout**: a slow/hung MariaDB connection blocks the MCP tool indefinitely, stalling the entire server. | Pass a timeout option to `query()` (e.g. `query(sql, params, { timeout: 5000 })`) or wrap with `Promise.race` + a timer. |
| **HIGH** | Module-level `let historyDb` + `setHistoryDb()` | **Race condition on mutable global**: `setHistoryDb()` can swap the adapter while `search()` is mid-flight on the old adapter, or between the fulltext and LIKE calls, producing inconsistent results. | Make `historyDb` an immutable reference set once at startup, or guard with a read-write lock / async-safe initialisation pattern. |
| **HIGH** | `MariaDbHistoryDb.search()` — `catch {}` | **Silent error swallowing**: fulltext index absence or permission errors are silently masked; the server will forever fall back to LIKE with no logging, hiding misconfiguration. | Log the caught error (at least `console.warn` / structured logger) before falling back, or emit a metric. |
| **MEDIUM** | Module top-level — `const now = new Date().toISOString()` | **Stale timestamp**: `now` is computed once at module-load time. Every seed entity gets the same `updated_at` forever, even across server restarts with stale in-memory data. | Compute `updated_at` per entity at construction time, or derive it dynamically when returning results. |
| **MEDIUM** | `normalizeDbRowToEntity()` — `attributes`, `relations`, `aliases` | **No runtime schema validation**: `safeJsonParse` returns raw parsed JSON cast as the expected type. Zod is imported but never used to validate DB rows, so malformed `attributes` (e.g. missing `entity_type`) passes through silently and crashes downstream consumers. | Validate with a Zod schema: `HistoryAttributesSchema.parse(rawAttributes)` inside `safeJsonParse` or after it. |
| **MEDIUM** | `ThaiHistoryToolError` envelope | **Non-standard MCP error format**: the custom `{ success: false, error_code, message }` shape is not the MCP SDK's `isError: true` content format. MCP clients expect `{ content: [{ type: "text", text: "…" }], isError: true }`. A custom envelope forces every client to parse a proprietary schema. | Return MCP-native error content: `server.tool(…, async () => { … return { content: [{ type: "text", text: JSON.stringify(data) }], isError: true }; })` for errors. |
| **MEDIUM** | `MariaDbHistoryDb.search()` — `limit` param | **No input validation**: `limit` can be `0`, negative, or `NaN`. A `LIMIT 0` query returns nothing; a negative value may throw or be coerced. | Clamp: `limit = Math.max(1, Math.min(limit ?? 5, 50))` before use. |
| **MEDIUM** | `MariaDbHistoryDb.search()` — fulltext → LIKE fallthrough | **Double latency on miss**: when fulltext returns 0 rows (not an error), the method still fires a second LIKE query sequentially, doubling response time for the common "no fulltext match" case. | Run both queries concurrently with `Promise.allSettled` and merge, or cache fulltext-miss queries, or accept LIKE-only for short queries. |
| **LOW** | `query()` calls — no visible connection cleanup | **Potential connection leak**: if `query()` borrows from a pool and the call throws, there is no `finally` block ensuring the connection is released. (Depends on `db.ts` implementation.) | Verify `query()` uses a pool with auto-release; otherwise wrap in try/finally to release. |
| **LOW** | `normalizeDbRowToEntity()` — `source` field (truncated) | **Incomplete parsing**: the `sourceR…` line is cut off; if `row.source` is a JSON string from MariaDB but parsed as a plain string, the resulting `source` field will be a `string` instead of `ThaiKnowledgeSource`, breaking the type contract. | Ensure `safeJsonParse<ThaiKnowledgeSource>(row.source, fallback)` is applied, not `String(row.source)`. |
| **LOW** | `InMemoryHistoryDb.score()` | **No relevance threshold**: entities with score 0.75 (description match) are returned alongside 0.95 exact matches. For broad queries this can flood results with low-quality matches. | Add a configurable minimum-score threshold (e.g. `filter(s => s.score >= 0.8)`). |
| **LOW** | `ThaiHistoryToolSuccess.confidence` | **Ambiguous aggregation**: the success envelope has a single `confidence` number, but results may have widely varying scores. It's unclear whether this is min, max, or average. | Document the aggregation strategy; consider returning per-result confidence. |
