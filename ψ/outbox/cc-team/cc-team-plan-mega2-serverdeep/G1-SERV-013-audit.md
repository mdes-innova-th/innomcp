<!-- cc-team deliverable
 group: G1 (serverdeep division)
 member: SERV-013 role=audit model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":2157,"completion_tokens":2558,"total_tokens":4715,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1983,"image_tokens":0},"cache_creation_input_tokens":0} | 36s
 generated: 2026-06-13T12:11:19.253Z -->
| Severity | Location | Issue | Fix |
|----------|----------|-------|-----|
| High | `searchArchive`, `fetch()` call | Missing request timeout – a slow or unresponsive Internet Archive API can block the MCP server indefinitely, causing unresponsiveness and potential DoS on the caller. | Create an `AbortController` with a sensible timeout (e.g., 15 seconds) and pass its `signal` to `fetch`. Handle `AbortError` to return a clear timeout message. |
| High | `archiveTool.execute()` return objects | Protocol violation: the tool never sets `isError: true` in the `CallToolResult`. Both validation errors and `searchArchive` failures return normal `content` without indicating an error, so clients treat all responses as successful. | When returning an error (validation failure or `searchArchive` error), include `isError: true` in the result object. Optionally throw an `McpError` with the correct error code, but at minimum set `isError: true` and keep the error details in `content`. |
| Low | `searchArchive` error handling | The catch block returns a JSON string with `success: false`, but the caller (`execute`) doesn’t detect it and still returns `isError: false`. This is a sub-issue of the above. | Restructure so that `execute` checks the result or throws an error to be caught in the `execute` wrapper to set `isError: true` and return a proper error structure. |
| Low | `searchArchive`, `response.json()` | In the unlikely event that `response.json()` throws while the response body is partially consumed, the underlying connection may not be properly drained. | After a JSON parse failure, call `response.body?.cancel()` or `response.clone()` before parsing, but this is a minor edge case; Node.js’s `fetch` typically cleans up. |
| Low | `searchArchive` logging (`logBoth`) | The full URL including query parameters is logged, which could expose user-provided search terms if logs are captured externally. | Sanitise or redact the `q` parameter before logging, or log only a checksum/length. |
| Medium | `archiveTool.execute`, validation error content | The tool returns validation error details as a single JSON string inside a `text` content item. MCP clients may expect structured errors via `isError` + `content` with `type: "text"` and `text` containing a message, but the current JSON blob is opaque. | Set `isError: true` and provide a human‑readable error message in the text, e.g. `"Invalid input: …"`, possibly with details as a separate field. |
