<!-- cc-team deliverable
 group: G2 (serverdeep division)
 member: SERV-034 role=audit model=zai-org/GLM-5.1
 finish_reason: stop | tokens: {"prompt_tokens":1369,"completion_tokens":3045,"total_tokens":4414,"prompt_tokens_details":{"cached_tokens":32,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1776,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 45s
 generated: 2026-06-13T12:18:54.995Z -->
| severity | location | issue | fix |
|---|---|---|---|
| **Critical** | `execute` catch block & validation-fail branch | **Missing `isError: true` in MCP tool result** — MCP spec requires `isError: true` on error responses; without it, clients treat the error payload as a successful result | Add `isError: true` to every error return: `return { content: […], isError: true }` |
| **Critical** | `actualUrl` resolution / `parseURL` call | **SSRF via arbitrary URL** — any URL (internal network, metadata endpoints, file:// via redirect) is fetched server-side with no allowlist or domain restriction | Validate `actualUrl` against an allowlist of domains; reject non-HTTPS; block private/loopback IPs (`10.x`, `127.x`, `169.254.x`, `::1`); use a URL allowlist or proxy |
| **High** | `parser.parseURL(actualUrl)` | **No AbortController / cancellation support** — MCP supports `CancelledNotification`; in-flight HTTP requests cannot be aborted, wasting sockets and delaying shutdown | Accept an `AbortSignal` from the MCP transport; pass `signal` to `parseURL` (or wrap with `AbortController` + `fetch`); reject on abort |
| **High** | `parser.parseURL` / `timeout: 10000` | **Parser timeout is unreliable** — `rss-parser`'s `timeout` sets a socket timeout but does **not** abort the request on timeout; the promise can hang indefinitely if the server sends headers slowly | Wrap `parseURL` in a `Promise.race` with a `setTimeout`-based rejection; call `request.destroy()` on timeout; or replace `rss-parser` with `fetch` + `AbortController.timeout()` |
| **High** | `parseURL` response | **No response size limit** — a malicious feed can return an arbitrarily large body, causing unbounded memory consumption and OOM | Stream the response through a `Transform` that counts bytes; reject if body exceeds a sane ceiling (e.g., 2 MB); or set `responseType: 'text'` with a max content-length check |
| **High** | `parser` (module-level) | **No redirect policy / redirect limit** — `rss-parser` follows redirects by default with no limit; an attacker-controlled feed can redirect infinitely or to internal hosts | Configure `maxRedirects: 5` (or switch to `fetch` with `redirect: 'follow'` + manual limit); validate redirect target URLs against the same allowlist |
| **Medium** | `rssFeedToolSchema` — `limit` field | **Zod allows any number** — negative, zero, `Infinity`, `NaN` pass validation; code silently clamps, masking bad input from the LLM | Add `.min(1).max(20)` to the Zod schema so invalid values are rejected with a clear validation error |
| **Medium** | `items.map` — `description`, `author`, `pubDate` | **`undefined` values silently dropped by `JSON.stringify`** — keys disappear from output, producing inconsistent response shapes that confuse clients | Use `null` instead of `undefined` for optional fields, or explicitly strip `undefined` entries; ensures deterministic output schema |
| **Medium** | Error catch block — `feedUrl: input.feedUrl` | **Error envelope uses unresolved preset name** — if user passes `"bbc"`, the error reports `feedUrl: "bbc"` instead of the actual URL, breaking debugging | Use `actualUrl` (already resolved) in the error result instead of `input.feedUrl` |
| **Medium** | `POPULAR_FEEDS` — `"bbc"` entry | **HTTP (non-TLS) preset URL** — `http://feeds.bbci.co.uk/…` is fetched over plaintext; MITM can inject content or poison the feed | Change to `https://feeds.bbci.co.uk/news/rss.xml`; audit all preset URLs for HTTPS |
| **Medium** | `feedUrl.toLowerCase()` cast | **Loose preset matching** — `"bbc "` (trailing space) or `"BBC "` won't match and will be treated as a raw URL, producing a confusing error | Trim whitespace before lookup: `feedUrl.trim().toLowerCase()` |
| **Medium** | `execute` — overall | **No overall execution timeout** — even if `parseURL` eventually resolves, DNS + TLS + parsing can take arbitrarily long; MCP transports have their own timeouts but the tool doesn't respect them | Add a top-level `Promise.race` with a configurable deadline (e.g., 15 s); reject cleanly on expiry |
| **Low** | `parser` — module-level singleton | **Shared HTTP agent with no connection pool limits** — concurrent tool calls share one agent; no `maxSockets` cap can exhaust file descriptors | Create `parser` per-call or configure the underlying agent with `maxSockets: 10` and `keepAlive: false` for short-lived tools |
| **Low** | `execute` — no caching | **No request deduplication or cache** — identical feed URLs requested concurrently or within a short window each trigger a full HTTP round-trip | Add an in-memory TTL cache (e.g., 60 s) keyed by `actualUrl`; dedupe concurrent requests with a pending-request map |
| **Low** | `POPULAR_FEEDS` — `"reuters"` URL | **Stale/broken preset URL** — `https://www.reutersagency.com/feed/` redirects or 404s; tool silently returns an error with no hint | Verify all preset URLs at startup; add a health-check or at minimum document last-verified date; consider removing broken presets |
| **Low** | Error messages | **Mixed Thai/English error messages** — `"URL ไม่ถูกต้อง…"` vs `"Invalid input"` vs `"เกิดข้อผิดพลาด…"": inconsistent locale makes debugging harder | Standardize on one locale for machine-facing errors (English); keep Thai only for user-facing descriptions |
| **Low** | `console.error` in catch | **Unstructured logging** — raw `console.error` with no structured metadata (request ID, URL, timestamp) makes log aggregation difficult in production | Use the server's logger with structured fields: `logger.error({ err, url: actualUrl }, "RSS feed parse failed")` |
| **Low** | `rssFeedToolSchema` — `feedUrl` | **No URL format validation in schema** — after preset resolution, URL format is checked with `startsWith` which allows `http://` without a domain (e.g., `http://`) | Use Zod `.url()` or a regex that validates a minimum host component; or validate with `new URL(actualUrl)` and catch the error |
