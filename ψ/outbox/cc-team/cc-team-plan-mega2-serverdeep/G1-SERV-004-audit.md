<!-- cc-team deliverable
 group: G1 (serverdeep division)
 member: SERV-004 role=audit model=zai-org/GLM-5.1
 finish_reason: stop | tokens: {"prompt_tokens":1319,"completion_tokens":3649,"total_tokens":4968,"prompt_tokens_details":{"cached_tokens":1312,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2091,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 43s
 generated: 2026-06-13T12:12:56.262Z -->
| severity | location | issue | fix |
|---|---|---|---|
| 🔴 Critical | `select()` → calculator `hasMathFn` branch | **Expression injection / garbage passed to calculator**: When `hasMathFn` matches but `isPureMath` is false, the *entire raw query* becomes the expression. `"what is sin of 90"` → `expression: "what is sin of 90"`. Worse, `"sin(alert('xss'))"` passes both checks and injects arbitrary content into the calculator. | Validate that the full expression is parseable before returning; when `hasMathFn` is true but `isPureMath` is false, extract only the math sub-expression or return `null` to let the LLM handle it. |
| 🔴 Critical | `select()` → weather generic branch | **Hardcoded default location "Bangkok"**: When weather keywords match but no province is found, `args: { location: "Bangkok", country: "TH" }` is silently returned. A user asking "อากาศภูเก็ต" with a typo gets Bangkok weather — silently wrong data. The inline comments even debate returning `null` instead. | Return `null` when no province is detected, letting the LLM do entity extraction. At minimum, set `confidence` to ≤ 0.4 and add a `needsClarification` flag. |
| 🟠 High | `select()` → EXCHANGE_KEYWORDS | **"rate" substring false-positive**: `q.includes("rate")` matches "corporate", "generate", "separate", "moderate", "celebrate", "inflate rate", etc. Any sentence containing these words routes to `currencyExchangeTool`. | Use word-boundary regex `/\b(rate|exchange)\b/` for English keywords; remove "rate" or scope it to "exchange rate". |
| 🟠 High | `select()` → TIME_KEYWORDS | **"date" substring false-positive**: `q.includes("date")` matches "update", "candidate", "validate", "accommodate", "sophisticated", etc. | Use word-boundary regex `/\bdate\b/` or `/\b(time\|date)\b/` for English keywords. |
| 🟠 High | `select()` → calculator `isPureMath` | **Whitespace-only / operator-only strings pass**: `/^[\d\s\+\-\*\/\(\)\.]+$/` matches `"  +  "` and `"---"`. The `/[0-9]/` guard on `q` catches some but not all (e.g. `"1+"` is valid pure-math but not a computable expression). | Add a guard: expression must contain at least two operands, e.g. `/\d.*[\+\-\*\/].*\d/` or validate via a proper expression parser before returning. |
| 🟠 High | `select()` → no input validation | **Null/undefined query crashes**: `query.toLowerCase()` throws `TypeError` if `query` is `null` or `undefined`. Unhandled exception propagates up, potentially crashing the MCP request handler. | Add guard: `if (!query || typeof query !== "string") return null;` |
| 🟠 High | `select()` → calculator precedence | **Math matches before weather/time**: Query `"อากาศ 50 + 30"` (weather with numbers) hits the calculator branch first and returns `calculatorTool` — semantically wrong. | Reorder: check domain-specific keywords (weather, exchange, time) before the generic math regex, or require math patterns to not co-occur with domain keywords. |
| 🟡 Medium | `select()` → EXCHANGE_KEYWORDS defaults | **Hardcoded USD→THB amount 1**: `"ค่าเงินยูโร"` (Euro rate) still returns `{ from: "USD", to: "THB", amount: 1 }`. Silent wrong-tool invocation. | Return `null` when currency pair can't be extracted, or set `confidence ≤ 0.5` and pass raw query in args for downstream parsing. |
| 🟡 Medium | `select()` → THAI_WEATHER_KEYWORDS | **Thai substring false-positives**: `"ร้อน"` (hot) appears inside `"ร้อนใจ"` (anxious/heart-burned) and other compound words. Thai has no word boundaries, so `includes` is the only option, but it over-matches. | Consider prefix-matching or a minimal Thai tokenizer; alternatively, lower confidence for keyword-only matches vs. keyword+province matches. |
| 🟡 Medium | `select()` → MATH_KEYWORDS constant | **Dead code**: `MATH_KEYWORDS` array is defined but never referenced. Confuses maintainers; suggests incomplete refactor. | Remove `MATH_KEYWORDS` or use it in the matching logic. |
| 🟡 Medium | `FlashSelectionSchema` | **Schema defined but never enforced**: `z.number()` for `confidence` allows values outside 0.0–1.0 (e.g. `2.5`, `-1`, `NaN`, `Infinity`). No runtime validation on `select()` output. | Wrap `select()` return in `FlashSelectionSchema.parse()` or `safeParse()`; tighten schema to `z.number().min(0).max(1)`. |
| 🟡 Medium | `select()` → weather province `find()` | **First-match ambiguity**: `knownProvinces.find(p => q.includes(p))` returns the first array entry that matches. `"นครราชสีมา"` also contains `"ราช"` — if a shorter string in the array matches first, the wrong province is selected. Also, queries mentioning two provinces get only the first. | Sort `knownProvinces` longest-first so more specific names match first; consider `findAll` + disambiguation for multi-province queries. |
| 🟡 Medium | `select()` → weather province extraction | **Province name not normalized**: `"เชียงใหม่"` is matched but the downstream tool may expect `"Chiang Mai"` or a code. Raw Thai string is passed as `province`. | Add a normalization map: `{ "เชียงใหม่": "Chiang Mai", ... }` to align with downstream API expectations. |
| 🔵 Low | `select()` → no observability | **No logging/tracing**: Flash selections are invisible in production. When a wrong tool is invoked, there's no audit trail. | Add a structured log entry per selection: `{ query, toolName, confidence, reason }`. |
| 🔵 Low | `select()` → return type | **`null` return not documented in MCP envelope**: Downstream callers that don't null-check will dereference `null`, causing `TypeError`. The MCP spec expects a well-formed error envelope, not a crash. | Document that callers must handle `null`; alternatively return `{ toolName: "none", confidence: 0, args: {}, reason: "no_match" }` so callers never need null guards. |
| 🔵 Low | `select()` → Thai keyword `"ลบ"` | **Polysemy**: `"ลบ"` means "subtract" but also "delete/erase" (e.g., `"ลบข้อความ"` = delete message). Calculator false-positive. | Require a digit nearby: only match `"ลบ"` if `/\d.*ลบ|ลบ.*\d/` is satisfied. |
