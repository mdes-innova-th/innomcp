<!-- cc-team deliverable
 group: G1 (serverdeep division)
 member: SERV-009 role=audit model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":1243,"completion_tokens":4188,"total_tokens":5431,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":3501,"image_tokens":0},"cache_creation_input_tokens":0} | 57s
 generated: 2026-06-13T12:10:32.532Z -->
| Severity | Location                                     | Issue                                                                                                                                                                                                                       | Fix                                                                                                                                                                                                                                                                                                   |
|----------|----------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| High     | `HistoryToolSuccess`, `HistoryToolError` (lines ~89–109) | Tool response contracts do not comply with MCP `CallToolResult` schema. Missing required `content` array and `isError` flag, which would break MCP clients if exposed directly.                                              | Wrap these internal DTOs in a translation layer that produces proper MCP results (`content: [{ type: "text", text: "..." }]`, `isError: true/false`). Alternatively, extend the contracts to include MCP fields if they represent final responses.                                                      |
| Medium   | `HistoryToolSuccess.data[].aliases` (line ~94) | `aliases` is declared as `string[]` (required), but the source entity `ThaiHistoryEntityTyped` defines `aliases?` as optional. Populating the response without defaulting will produce `undefined`, violating the contract. | Change response data to `aliases?: string[]` or ensure the mapping function defaults missing aliases to `[]`. Add a type-level test to guarantee consistency between the entity and response contracts.                                                                                               |
| Medium   | `HistoryToolSuccess.source` (line ~101)       | `source` is typed as `string[]`, but the underlying entity uses `ThaiKnowledgeSource` (likely a structured object). This mismatch forces the mapper to transform a structured source into a flat string array, risking data loss. | Audit `ThaiKnowledgeSource` type. If it carries more detail, either change response `source` to the structured type or define a dedicated representation. Ensure bidirectional mapping does not silently drop fields.                                                                               |
| Low      | `HistoryToolError` (lines ~106–109)           | Error envelope omits the `domain` discriminator present in `HistoryToolSuccess`. A consumer that switches on `domain` to route or log will lose context on errors.                                                          | Add `domain: "history"` to `HistoryToolError` for symmetry. Alternatively, document that error responses are domain-agnostic.                                                                                                                                                                          |
| Low      | `HistoryEvent.year`, `HistoryPerson.year_birth/year_death` | Years are typed as `number` without brand or range constraints. Buddhist Era years may be confused with CE (the comments say CE, but field `period` is BE). Ambiguity could cause incorrect calculations.                   | Use nominal types or template literal types (e.g., `type CEYear = number & { __brand: 'ce' }`) and add explicit `year_be` if needed. Enforce validation in the mapper.                                                                                                                             |
| Info     | `HistoryToolError.error_code` (line ~108)     | Custom error codes `INVALID_QUERY`, `NOT_FOUND`, `DB_ERROR` are not part of the MCP error model. If clients expect machine-readable error discrimination, these codes must be embedded inside the text content or a custom meta field. | Embed the error code into the text content (e.g., `[INVALID_QUERY] message`) or adopt JSON-RPC style error object within `content`.                                                                                                                                                                   |
