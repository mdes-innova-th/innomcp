<!-- cc-team deliverable
 group: G2 (serverdeep division)
 member: SERV-027 role=audit model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":581,"completion_tokens":4212,"total_tokens":4793,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":3542,"image_tokens":0},"cache_creation_input_tokens":0} | 78s
 generated: 2026-06-13T12:16:59.055Z -->
| severity | location | issue | fix |
|---|---|---|---|
| High | `prop.object` | **Nested required fields are ignored:** `prop.object` directly assigns `properties` without calculating the `required` array for its children. Furthermore, `McpSchemaProperty` lacks a `required` field, making it impossible to define required fields for nested objects. | Add `required?: string[]` to `McpSchemaProperty`. Update `prop.object` to iterate over its `properties`, extract required fields, and populate the `required` array, mirroring the logic in `createObjectSchema`. |
| High | `createObjectSchema` | **Mocked Zod schema lacks validation methods:** The returned object mocks Zod's internal structure (`_def`, `typeName`) but lacks actual Zod methods (`.parse()`, `.safeParse()`, `.shape`). If the MCP SDK or middleware attempts to validate tool inputs by calling `schema.parse()`, it will throw a fatal `TypeError`. | Use the actual `zod` library to construct real `ZodObject` instances. If mocking is strictly necessary for JSON schema generation, ensure this object is only used for `ListTools` responses and never passed to a validation pipeline. |
| Medium | `prop.*` builders | **Non-standard `optional` property in JSON Schema:** The `optional` boolean is included in the generated schema properties. Standard JSON Schema (which MCP relies on) does not recognize `optional` as a valid keyword. Strict validators used by MCP clients may reject the schema or throw warnings. | Remove the `optional` property from the returned objects in the `prop.*` builders, or strip it during a final serialization step before sending the schema to the MCP client. |
| Medium | `createObjectSchema` | **Crash on null/undefined properties:** `Object.entries(properties)` will throw a `TypeError: Cannot convert undefined or null to object` if the `properties` argument is passed as `null` or `undefined`. | Add a fallback to an empty object: `Object.entries(properties \|\| {})` or explicitly validate/throw a descriptive error if the input is invalid. |
| Low | `createObjectSchema` | **Brittle Zod internal mocking:** Hardcoding `_def: { typeName: "ZodObject" }` relies on undocumented Zod internals. If the MCP SDK updates its Zod dependency or changes how it inspects schemas (e.g., checking `_def.shape`), this hack will break silently. | Pin the MCP SDK version if relying on this hack, and add a prominent warning comment. Ideally, migrate to using the actual `zod` library to generate schemas to ensure forward compatibility. |
| Low | `prop.array` | **Missing runtime validation for `items`:** While TypeScript enforces the `items` parameter, at runtime (e.g., if called dynamically), if `items` is omitted or invalid, the generated array schema will be malformed according to JSON Schema specifications. | Add runtime validation to ensure `items` is a valid object before assigning it, or provide a safe default (e.g., `items: { type: "string" }`) if strict enforcement isn't possible. |
