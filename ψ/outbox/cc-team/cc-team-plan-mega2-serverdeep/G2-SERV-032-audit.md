<!-- cc-team deliverable
 group: G2 (serverdeep division)
 member: SERV-032 role=audit model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":1339,"completion_tokens":5270,"total_tokens":6609,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":4560,"image_tokens":0},"cache_creation_input_tokens":0} | 112s
 generated: 2026-06-13T12:19:13.721Z -->
| severity | location | issue | fix |
|---|---|---|---|
| High | `Tesseract.recognize(...)` | **Resource Leak, OOM & Race Conditions:** `recognize()` spawns a new heavy worker per request, causing CPU starvation and OOM under concurrent load. If refactored to a persistent worker, concurrent language switching causes state corruption (race condition). | Implement a worker pool with a concurrency limiter (e.g., `p-limit`). If using persistent workers, dedicate workers to specific languages or use a mutex for `loadLanguage`. Ensure `worker.terminate()` is in a `finally` block. |
| High | `await Tesseract.recognize(...)` | **Missing Timeout:** OCR processing or language model downloading can hang indefinitely on malformed/huge images or network stalls, blocking the MCP tool execution and potentially crashing the server. | Wrap the execution in a `Promise.race` with a configurable timeout (e.g., 30-60s) or use `AbortController` if supported by the Tesseract worker API to cancel hung operations. |
| Medium | `catch` & validation blocks | **MCP Error Envelope Incorrectness:** Error responses lack the `isError: true` flag required by the MCP specification. The LLM client won't natively recognize the response as a tool failure. | Add `isError: true` to the returned object in both the Zod validation failure block and the `catch` block. |
| Medium | `logger: (m) => { ... }` | **Unhandled Exception in Callback:** `m.progress` is `undefined` for certain statuses (e.g., 'loading tesseract core'). Calling `.toFixed()` on `undefined` throws a `TypeError`, crashing the worker or Node process. | Guard the progress check: `if (m.status === 'recognizing text' && typeof m.progress === 'number')`. |
| Medium | `imageData` validation | **SSRF, LFI & OOM Vulnerabilities:** Accepting arbitrary URLs allows SSRF. Tesseract in Node might read local file paths (LFI). Unlimited base64 strings cause memory exhaustion. | Restrict inputs strictly to base64 (enforce max length/size) or HTTP/HTTPS URLs (with SSRF IP blocking). Explicitly reject local file paths. |
| Low | `ocrToolSchema` (`confidence`) | **Missing Bounds Validation:** `confidence` accepts any number. Values outside 0-100 will cause logical errors or crash the filtering step. | Update schema to `z.number().min(0).max(100).optional().default(50)`. |
| Low | `ocrToolSchema` (`language`) | **Invalid Language Codes:** Passing invalid strings (e.g., "english" instead of "eng") causes Tesseract to fail obscurely or attempt to download non-existent models. | Use `z.enum([...])` for supported languages or add regex validation, and map common names to Tesseract codes. |
| Low | `catch (error)` block | **Redundant Error Payload:** Returning a mock success-like object with `success: false` and empty arrays inside the text payload confuses the LLM and wastes context tokens. | Return a concise error message in the `text` field and rely on `isError: true` for protocol-level error handling. |
