<!-- cc-team deliverable
 group: G1 (serverdeep division)
 member: SERV-008 role=audit model=moonshotai/Kimi-K2.6
 finish_reason: stop | tokens: {"prompt_tokens":3240,"completion_tokens":4319,"total_tokens":7559,"prompt_tokens_details":{"cached_tokens":54,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":4126,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 40s
 generated: 2026-06-13T12:09:35.632Z -->
**Audit scope note:** This module is a synchronous, static data file with zero async I/O, Promises, timers, or network resources. Consequently, **unhandled rejections**, **missing timeouts**, and explicit **resource leaks** (handles/sockets) are **not applicable** here. The issues below focus on **race conditions** (via shared mutable state in a concurrent server), **MCP protocol edge cases**, and **error-envelope correctness** risks introduced by this data layer.

| severity | location | issue | fix |
|---|---|---|---|
| **Medium** | `export const HISTORY_KINGS` (line 7) | **Race condition – mutable shared state:** The exported array and all nested objects are mutable. In a concurrent MCP server, an accidental in-place mutation by one request handler (e.g., `.sort()`, `.push()`, or enriching an entity in-place) corrupts global state for every subsequent request/response. | Freeze the structure: `Object.freeze()` on the array and every nested object; export as `ReadonlyArray<PersonEntity>`; return deep clones if handlers need to mutate data locally. |
| **Medium** | `source: RTGG_SOURCE` (all entities) | **Resource leak / shared-reference corruption:** All entries hold a reference to the *same* `RTGG_SOURCE` object. If any consumer mutates it (e.g., `HISTORY_KINGS[0].source.name = "..."`), the change leaks globally and persists for the process lifetime, effectively corrupting the knowledge graph. | Freeze the source object (`Object.freeze(RTGG_SOURCE)`) or create per-entity copies (`source: { ...RTGG_SOURCE }`). |
| **Low** | `const now = new Date().toISOString()` (line 4) | **MCP protocol edge case – stale metadata:** `updated_at` is computed once at module load and never refreshed. MCP clients relying on this timestamp for cache invalidation or sync will see stale values, violating freshness expectations for a "knowledge source." | Compute `updated_at` dynamically inside the data accessor/tool handler, or omit the field if the dataset is truly immutable. |
| **Low** | `id` fields (e.g., `"person:naresuan"`) | **MCP protocol edge case – identifier encoding:** Colon-delimited IDs can be misinterpreted as URI schemes or namespace prefixes by MCP routers, validators, or logging pipelines, causing routing or validation failures under strict transport encoding. | Ensure IDs conform to the MCP URI scheme (e.g., `mcp://history/person/naresuan`) or percent-encode colons when serializing into tool results. |
| **Low** | Various entries (`person:sriindraditya`, `person:borommatrailokanat`, `person:rama1`, `person:mongkut`, `person:vajiravudh`, `person:sunthorn-phu`) | **Error envelope correctness (downstream risk):** Inconsistent optional fields (`reign_period`, `year_birth`, `year_death`) may cause downstream tool handlers or validators to throw. If the server does not catch these, raw exceptions propagate instead of proper MCP JSON-RPC `-32602` / internal error envelopes. | Normalize all entities to include consistent fields, or explicitly mark them optional in the `PersonEntity` type and add strict pre-flight validation before returning data in tool handlers. |

**Summary:** The file contains no async bugs, but its **mutable exports** and **shared object references** are genuine server-side defects in an MCP context where multiple requests may be in-flight. Harden it by freezing all data and treating the export as an immutable, read-only source of truth.
