<!-- cc-team deliverable
 group: G1 (thaii18n division)
 member: THAI-006 role=i18n model=deepseek/deepseek-v4-flash
 finish_reason: stop | tokens: {"prompt_tokens":1179,"completion_tokens":1477,"total_tokens":2656,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":887,"image_tokens":0},"cache_creation_input_tokens":0} | 18s
 generated: 2026-06-13T12:08:46.211Z -->
| Severity | Location | Issue | Fix |
|----------|----------|-------|-----|
| medium   | `route()` method, lines 63, 72, 78–79 | Hardcoded Thai-language error/reason strings (e.g., "ไม่มีโมเดลที่พร้อมใช้งาน", "ไม่สามารถเลือกโมเดลได้", "เลือกโมเดล ... สำหรับโดเมน ... (ตรงตามค่ากำหนด) ความมั่นใจ: ...%") prevent localization and violate i18n best practices. | Externalize all user-facing strings to a locale file (e.g., `th.json` for Thai, `en.json` for English) and use an i18n function like `t('no_models_available')`. |
| medium   | `route()` input parameter `text` (line 52) | No normalization applied to the input Thai text before passing to `ThaiNLPService.detectIntent()`. Different Unicode normalization forms (NFC vs NFD), full-width characters, zero-width spaces, or invisible characters can cause tokenization or intent detection failures. | Normalize the text using `text.normalize('NFC')` and optionally strip non‑printing control characters (U+200B, U+200C, etc.) before calling `detectIntent()`. |
| low      | `route()` / `selectModelForDomain()` (entire routing logic) | No explicit handling of mixed-language input (e.g., Thai+English). The domain mapping may incorrectly route a Thai‑heavy query to an English‑optimized model, or vice versa, because the intent detection does not consider language distribution. | Perform language identification (e.g., using a compact Thai/English detector) and bias the domain selection toward models known to perform well on the detected primary language. Alternatively, add a separate `lang` field to `RoutingDecision` and allow fallback. |
| low      | File encoding (entire file) | If the TypeScript file is not saved as UTF‑8 (or contains a BOM), the hardcoded Thai characters may be corrupted at compile time or runtime. | Ensure the file is saved with UTF‑8 encoding **without** BOM. Add a `.editorconfig` or build‑time check to enforce UTF‑8. |
| low      | `reason` string construction (lines 78–79) | The Thai message uses template literals that interpolate model name and domain directly. If either contains special Unicode characters (e.g., Thai tone marks that could be misinterpreted) the string is still valid, but the approach is fragile for future localization (e.g., plural forms, gender). | Use a parameter‑based i18n function (e.g., `t('model_selected', { model, domain, confidence })`) to ensure proper locale‑aware formatting and escaping. |
