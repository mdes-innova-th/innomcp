<!-- cc-team deliverable
 group: G1 (thaii18n division)
 member: THAI-007 role=i18n model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":2506,"completion_tokens":5887,"total_tokens":8393,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":4817,"image_tokens":0},"cache_creation_input_tokens":0} | 109s
 generated: 2026-06-13T12:10:26.364Z -->
| severity | location | issue | fix |
|---|---|---|---|
| High | `tokenize()` | **Invalid Thai Tokenization**: Splitting by whitespace/ZWSP does not segment Thai words, as Thai script lacks inter-word spaces. This returns whole sentences/clauses instead of individual words. | Implement a dictionary-based longest-matching segmenter (e.g., using a Trie) or integrate a WASM-compiled Thai NLP library (like PyThaiNLP) for proper word boundary detection. |
| High | `clean()`, `thaiRatio()`, `detectIntent()` | **Missing Unicode Normalization**: Thai text frequently uses decomposed characters (e.g., separate tone marks, Sara Am). Without NFC normalization, string matching, regex, and length calculations will be inconsistent or fail. | Apply `text = text.normalize('NFC')` at the entry point of all text processing methods to ensure canonical equivalence. |
| High | `extractEntities()` (Agencies) | **Flawed Thai Character Class & Greedy Matching**: The regex uses `[ก-ฮ0-9\s]*`. `ก-ฮ` only matches Thai consonants, completely ignoring vowels (e.g., ะ, า, ิ) and tone marks. Furthermore, including `\s` causes greedy over-matching across spaces, merging multiple agencies into one string. | Replace `[ก-ฮ]` with the full Thai Unicode block `[\u0E00-\u0E7F]` (or specifically letters/digits). Remove `\s` to prevent matching across word/sentence boundaries. |
| High | `extractEntities()` (Numbers) | **Ignores Thai Digits & Formatting**: The regex `/[-+]?\b\d+\b/g` only matches Arabic numerals (0-9). It completely ignores Thai digits (๐-๙) and fails on numbers with thousand separators (e.g., "1,000" or "1 000"). | Update regex to include Thai digits (`[0-9๐-๙]`) and handle commas/spaces. Map Thai digits to Arabic equivalents before parsing with `parseInt()`. |
| High | `isThaiChar()` (Assumed) | **Potential Consonant-Only Check**: If `isThaiChar` is implemented using the common `[ก-ฮ]` range, it will fail to identify Thai vowels, tone marks, and digits as Thai characters, severely skewing the `thaiRatio` and language detection. | Ensure `isThaiChar` checks the full Thai Unicode block (`/[\u0E00-\u0E7F]/`) rather than just the consonant range. |
| Medium | `extractEntities()` (Dates) | **Incomplete Thai Date Parsing**: The regex only matches full month names and Arabic numerals. It misses common abbreviated months (ม.ค., ก.พ.), Thai digits, and lacks context for Buddhist Era (BE) years (CE + 543). | Expand regex to include abbreviated months and Thai digits. Add logic to detect 4-digit years > 2500 and convert them from BE to CE if standard CE dates are required downstream. |
| Medium | `detectIntent()`, `extractEntities()` | **Hardcoded Lexicons**: Intent keywords, province lists, and agency keywords are hardcoded in the source. This makes it difficult to update, maintain, or support regional variations/slang without code deployments. | Externalize these dictionaries into JSON configuration files or a database, and load them dynamically at runtime. |
| Medium | `extractEntities()` (Currency) | **Missing Currency Extraction**: No extraction logic for Thai Baht (฿, บาท), which is critical for financial, e-commerce, or transactional intents in the Thai market. | Add a currency regex pattern to capture amounts followed by `฿` or `บาท`, and normalize the extracted values to standard numeric formats. |
| Low | `thaiRatio()` | **Incorrect Empty String Handling**: Returns `1` (100% Thai) when the cleaned text is empty (`cleaned.length === 0`). This causes false positives for Thai detection in downstream routing logic. | Return `0` for empty strings, or throw/handle empty inputs explicitly before calculating the ratio. |
| Low | `suggestModel()` | **Hardcoded Fallback Model**: The fallback model `'mdes/llama3:latest'` is hardcoded. If the model registry changes or the model is deprecated, it requires a code change. | Move the default model identifier to an environment variable (e.g., `process.env.DEFAULT_MDES_MODEL`) or a centralized config file. |
| Low | `detectIntent()`, `extractEntities()` | **Unnecessary Regex Flags**: The `i` (case-insensitive) flag is used on regexes matching Thai characters (e.g., `/อะไร|ไหม/i`). Thai script has no uppercase/lowercase, making the flag redundant and slightly impacting regex engine performance. | Remove the `i` flag from regex patterns that exclusively or primarily match Thai characters. |
