<!-- cc-team deliverable
 group: G1 (thaii18n division)
 member: THAI-005 role=i18n model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":1768,"completion_tokens":2179,"total_tokens":3947,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1651,"image_tokens":0},"cache_creation_input_tokens":0} | 29s
 generated: 2026-06-13T12:08:37.445Z -->
| severity | location                                     | issue                                                                                                                                                    | fix                                                                                                  |
|----------|----------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------|
| medium   | `getWeatherForecast` error throw (line ~73)  | Hardcoded English error message `'Forecast days must be between 1 and 14'` – no Thai localization for end‑user‑facing errors.                             | Use an i18n library or resource bundle to return errors in Thai when the locale is `th-TH`.          |
| medium   | `searchEvidence` error throw (line ~107)     | Hardcoded English error message `'Limit must be between 1 and 100'` – same missing localization.                                                          | Same as above; provide a Thai translation (e.g., `'จำนวนต้องอยู่ระหว่าง 1 และ 100'`).               |
| low      | `getWeatherReport` default province (line ~53) | Hardcoded Thai default `'กรุงเทพมหานคร'`. While correct, if the API later expects names in a different script, a mismatch could occur.                  | Document that the default is Thai script; alternatively, make the default configurable or use a constant from a province list. |
| low      | All date fields in interfaces (e.g., `timestamp`, `issuedAt`, `date`) | Dates are stored as ISO‑8601 strings; no explicit handling for Buddhist Era (พ.ศ.) or Thai locale formatting. Thai government data often expects พ.ศ. output. | Ensure the consumption layer converts Gregorian ISO dates to Thai Buddhist year and localized formats using `Intl.DateTimeFormat('th-TH-u-ca-buddhist')`. |
| low      | `ForecastDay.date` comment (line ~27)        | Comment says `// YYYY-MM-DD` – the extra `Y` is a typo (should be `YYYY-MM-DD` or `yyyy-MM-dd`).                                                           | Correct the comment to `// YYYY-MM-DD` or `// ISO 8601 date`.                                        |
| info     | General Thai text handling                   | No Thai tokenization or normalization is performed; strings are passed directly to MCP tools. This is acceptable if the backend handles it, but could lead to inconsistent Unicode normalisation (e.g., composed vs. decomposed forms). | If queries may come from user input, consider normalizing Thai text to NFC (or NFD) before sending to MCP tools to avoid lookup mismatches. |
