<!-- cc-team deliverable
 group: G1 (thaii18n division)
 member: THAI-002 role=i18n model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":1173,"completion_tokens":3763,"total_tokens":4936,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":3086,"image_tokens":0},"cache_creation_input_tokens":0} | 76s
 generated: 2026-06-13T12:07:59.166Z -->
| severity | location | issue | fix |
|---|---|---|---|
| Medium | `PROVINCE_REQUEST_RE` | **Thai Tokenization/Spacing:** The regex expects exact contiguous Thai words (e.g., `กรุณาระบุจังหวัด`). Thai IMEs, OCR, or copy-pasted text often include optional spaces or zero-width spaces between words, causing false negatives. | Make whitespace optional between Thai words in the regex (e.g., `กรุณา\s*ระบุ\s*(จังหวัด\|พื้นที่)`) or normalize/collapse whitespace before testing. |
| Medium | `checkNaturalness` (Rule 6) | **Brittle Thai Heuristics:** The regexes for detecting follow-ups (`มี(คำถาม\|ข้อเสนอ)`) and plan frames (`(แนวทาง\|เกณฑ์)`) are too rigid. They miss common Thai morphological variations and compound words (e.g., "มีข้อสงสัย", "แนวทางปฏิบัติ", "วิธีการ"). | Expand the keyword lists to include common variations, use a lightweight Thai tokenizer to check for root words, or delegate the "shallowness" check to the LLM itself rather than using rigid regexes. |
| Low | `checkNaturalness` (Rule 2) | **Mixed-Language Edge Case:** `startsWithEnglish` only checks for `^[A-Za-z]`. If the answer starts with a number, bullet point, or emoji (e.g., "1. Here is the plan..."), it bypasses the English-first guard. | Update the regex to look for the first alphabetical character regardless of leading punctuation/numbers: `/^[^ก-๙a-zA-Z]*[a-zA-Z]/.test(s.trim())`. |
| Low | `hasThaiCharacter` | **Unicode Script Matching:** Using the hardcoded range `/[฀-๿]/` works for basic Thai but is less maintainable and doesn't leverage modern JS Unicode features. | Use the modern Unicode property escape: `/\p{Script=Thai}/u.test(s)`. This is cleaner, standard, and automatically handles script extensions. |
| Low | `checkNaturalness` (Rule 2) | **Grapheme Cluster Splitting:** `trimmed.slice(0, 50)` slices by UTF-16 code units. While Thai is in the BMP (avoiding surrogate pair splits), it can split combining character sequences (e.g., separating a tone mark or upper vowel from its base consonant). This causes rendering glitches if the slice is ever logged. | Use `Intl.Segmenter('th')` to slice by grapheme clusters, or simply check a larger safe bound / the whole string since `hasThaiCharacter` only checks for presence. |
| Info | `hint` properties | **Hardcoded i18n Strings:** All `hint` messages are hardcoded in Thai. While acceptable for internal LLM prompt injection, it violates strict i18n externalization if these hints are ever surfaced to end-users or if the system needs to support multiple agent languages. | Extract hints to an i18n dictionary (e.g., `i18next`) or a centralized constants file. Add JSDoc comments explicitly stating these are LLM prompt injections, not UI strings. |
