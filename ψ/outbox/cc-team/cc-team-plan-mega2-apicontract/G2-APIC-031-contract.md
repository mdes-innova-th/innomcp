<!-- cc-team deliverable
 group: G2 (apicontract division)
 member: APIC-031 role=contract model=deepseek/deepseek-v4-flash
 finish_reason: stop | tokens: {"prompt_tokens":1304,"completion_tokens":2084,"total_tokens":3388,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1064,"image_tokens":0},"cache_creation_input_tokens":0} | 23s
 generated: 2026-06-13T12:15:42.394Z -->
| severity | endpoint | issue | fix |
|----------|----------|-------|-----|
| high | `GET /api/mother/inbox` | **Missing input validation for `limit`** – non‑numeric or out‑of‑range values are silently clamped (e.g. `limit=abc` → 5, `limit=100` → 20). No 4xx error is returned. | Validate `limit` early; reject invalid values with `400 Bad Request` and an error message. Example: `if (isNaN(limit) || limit < 1) return res.status(400).json({ error: "limit must be a positive integer" })`. |
| high | `GET /api/mother/inbox` | **Missing input validation for `since`** – invalid date strings (e.g. `since=notadate`) produce an `Invalid Date` object that is silently ignored, leading to incorrect filtering. | Validate that `since`, if provided, is a valid ISO‑8601 timestamp. Return `400 Bad Request` with a descriptive error when it is not. |
| high | `GET /api/mother/inbox` | **Inconsistent response shape** – the `inboxPath` field is present when the directory exists but absent when it does not (or on error). Clients cannot rely on a stable schema. | Always include `inboxPath` if it is intended to be part of the contract, or remove it entirely. If removed, update documentation and avoid breaking existing consumers by versioning. |
| high | `GET /api/mother/inbox` | **Sensitive path leak** – `inboxPath` reveals the absolute filesystem path of the JIT inbox (e.g. `C:/Users/…/Jit/ψ/inbox`). This is a security concern. | Remove `inboxPath` from the response, or only include it behind a feature flag/debug mode. If absolutely needed, sanitise or use a relative identifier. |
| high | `GET /api/mother/inbox` | **Incorrect HTTP status codes** – all responses are `200 OK`, even for invalid inputs (should be 400) and internal errors (should be 500). | Return appropriate status codes: `400` for validation errors, `500` for unexpected server errors, `200` only on success. |
| high | `GET /api/mother/inbox` | **Missing error responses** – any exception (e.g. filesystem permission error) is caught and silently returns a `200 OK` with empty data. Clients cannot distinguish between a genuine empty inbox and a server fault. | Return a `500` response with an error message and, in production, log the error server‑side. Do not swallow exceptions. |
| medium | `GET /api/mother/inbox` | **Undocumented query parameters** – `limit` and `since` are not described in the route comment; `since`’s expected format (ISO 8601) is not stated. | Add explicit JSDoc or OpenAPI documentation covering parameter names, types, defaults, and constraints (e.g. `limit` between 1‑20, `since` in ISO‑8601). |
| medium | `GET /api/mother/inbox` | **Ambiguous response field `total`** – currently `total` equals the number of returned messages, not the total available in the directory. This violates common API conventions (e.g. paginated responses). | Rename to `count` or provide a separate `total` that reflects the number of **all** filtered messages before pagination (requires scanning all files). Alternatively, document that `total` is the returned message count. |
| medium | `GET /api/mother/inbox` | **Breaking‑change risk of adding/removing fields** – if `inboxPath` is removed or the `total` behaviour is corrected, existing clients expecting those exact values will break. | Introduce a new API version (e.g. `/api/v1/mother/inbox`) or a deprecation notice before altering the contract. |
| low | `GET /api/mother/inbox` | **Synchronous file operations** – `readdirSync`, `readFileSync`, `statSync` block the event loop, hurting scalability under concurrent requests. | Replace with asynchronous versions (`fs.promises.readdir`, etc.) and `await` them inside an `async` handler. |
| low | `GET /api/mother/inbox` | **Hard‑coded fallback path** – `"C:/Users/USER-NT"` is a placeholder that will fail on most systems if `USERPROFILE` and `HOME` are unset. | Remove the fallback or use a more sensible default (e.g. `os.homedir()` from the `os` module). |
