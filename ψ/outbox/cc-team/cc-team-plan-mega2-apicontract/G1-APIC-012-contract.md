<!-- cc-team deliverable
 group: G1 (apicontract division)
 member: APIC-012 role=contract model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":2177,"completion_tokens":4482,"total_tokens":6659,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":3849,"image_tokens":0},"cache_creation_input_tokens":0} | 116s
 generated: 2026-06-13T12:11:49.785Z -->
| severity | endpoint | issue | fix |
|---|---|---|---|
| High | POST /api/chat/stream | **Missing input validation & prompt injection risk**: `message` lacks max-length limits, risking memory/DB issues. `messages` array items are not validated; allowing `sender: "system"` could enable prompt injection if the LLM backend doesn't strictly separate roles. | Use a schema validator (e.g., Zod). Enforce max-length for `message`. Strictly validate `messages` items to ensure `sender` is only `"user"` or `"ai"` and `text` is a bounded string. |
| High | POST /api/chat/stream | **Undocumented/Unused parameter mismatch**: `projectId` is documented in JSDoc and parsed but never passed to `runConductor`. Conversely, `messages` is parsed and used but missing from the JSDoc contract. | Pass `projectId` to `runConductor` if needed for scoping/context. Update JSDoc to explicitly document the `messages` array structure to keep the contract accurate. |
| Medium | POST /api/chat/stream | **Missing enum validation for mode parameters**: `preferredMode`, `responseMode`, and `reasoningMode` are expected to be specific string literals but are passed directly without validation. Invalid strings could cause downstream crashes or silent fallbacks. | Validate these fields against their respective enums (`ChatMode`, `AgentRunMode`) during request parsing. Return a `400 Bad Request` with a descriptive error if invalid values are provided. |
| Medium | POST /api/chat/stream | **Inconsistent pre-stream vs in-stream error shapes**: Middleware (rate limiting, auth) and early validation return JSON errors (e.g., 400, 401, 429), while runtime errors are emitted as SSE `error` events. Clients might crash if they blindly parse the response body as SSE. | Ensure the client SDK checks HTTP status and `Content-Type` before parsing. Document the pre-stream JSON error contract explicitly in the API spec/OpenAPI definition. |
| Low | POST /api/chat/stream | **Silent failure on socket write errors**: `writeEvent` and `writeComment` do not handle errors if the socket is closed or `res.write` fails, which could lead to unhandled exceptions or prevent the `cleanup` function from triggering properly. | Check `res.writable` before writing, or wrap `res.write` in a try-catch block that triggers `cleanup()` and clears the heartbeat interval on failure. |
| Low | POST /api/chat/stream | **Loose type coercion for boolean params**: `thinkingMode` is expected to be a boolean, but if sent as a string (e.g., `"false"` from form-data or query), it will evaluate as truthy in JS. | Use strict type checking or a schema validator that properly coerces/parses types (e.g., `z.coerce.boolean()`) to prevent unexpected truthy evaluations. |
