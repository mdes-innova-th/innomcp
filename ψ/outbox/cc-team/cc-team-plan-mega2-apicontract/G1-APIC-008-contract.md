<!-- cc-team deliverable
 group: G1 (apicontract division)
 member: APIC-008 role=contract model=moonshotai/Kimi-K2.6
 finish_reason: stop | tokens: {"prompt_tokens":354,"completion_tokens":2920,"total_tokens":3274,"prompt_tokens_details":{"cached_tokens":3,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2281,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 49s
 generated: 2026-06-13T12:09:08.384Z -->
| severity | endpoint | issue | fix |
|---|---|---|---|
| Critical | `POST /api/analyze` | **Path traversal / arbitrary file read:** `filePath` is forwarded to the service without verifying it resolves inside `WORKSPACE_ROOT`. A client can send `filePath: "../../../etc/passwd"` and potentially read files outside the workspace. | Resolve the path with `path.resolve(WORKSPACE_ROOT, filePath)` and assert the normalized result `startsWith(WORKSPACE_ROOT + path.sep)`; return `400` if traversal is detected. |
| High | `POST /api/analyze` | **Missing runtime input validation:** `req.body` is cast with `as { csv?: string; filePath?: string }`. No runtime check ensures `csv` is actually a string (vs. object/array), `filePath` is a string, or that payload size is bounded. | Add a Zod / Joi schema: `csv?: z.string().max(LIMIT)`, `filePath?: z.string()`, and reject non-conforming bodies with `400` and a detailed validation error. |
| High | `POST /api/analyze` | **Incorrect status-code mapping:** The catch block returns `400` for *every* error, including unexpected internal failures (e.g., disk I/O, out-of-memory, downstream service crash). | Categorize errors: malformed CSV / bad input → `400`; missing file → `404`; CSV too large → `413`; unexpected / internal failures → `500` (or `502` if downstream). |
| Medium | `POST /api/analyze` | **Inconsistent response envelope:** Success returns a flat `AnalysisResult` object (`{ rowCount, colCount, … }`), while errors return `{ error: string }`. Optional fields (`chartSvg?`, `artifactPath?`) may be omitted, causing `undefined` properties for consumers. | Standardize on a stable envelope (e.g., `{ data?: AnalysisResult, error?: { code, message } }`) or explicitly return `null` for absent optional fields instead of omitting keys. |
| Medium | `POST /api/analyze` | **Missing documented error responses:** The contract comment only describes the `200` shape. It does not specify `400`, `404`, `413`, `500` response schemas, leaving clients without a stable error contract. | Document and return typed error payloads for every status code (e.g., `{ error: { code: "CSV_PARSE_ERROR", message: "…" } }`) and add `express.json({ limit: "…" })` to enforce payload bounds. |
| Medium | `POST /api/analyze` | **Undocumented parameter semantics:** The handler silently passes `{ workspaceRoot: WORKSPACE_ROOT }` as a second argument to `analyzeData` when `csv` is provided, but this behavior is absent from the JSDoc. When both `csv` and `filePath` are sent, `filePath` wins without warning. | Update the docstring / OpenAPI spec to describe injected `workspaceRoot` and the precedence rule; alternatively reject requests that contain both fields with `400` + `"Provide only csv or filePath, not both."` |
| Medium | `POST /api/analyze` | **Breaking-change risk:** Optional fields (`chartSvg?`, `artifactPath?`) can appear or disappear based on the analysis result. Clients that do not defensively check for key presence will break if the field set changes. | Version the route (`/v1/analyze`); guarantee that optional keys are always present in the payload set to `null` when absent; publish a JSON Schema / OpenAPI definition and enforce it in tests. |
| Low | `POST /api/analyze` | **Error message leakage:** Raw `err.message` is returned directly to the client, which may expose internal file paths, stack traces, or implementation details. | Map internal errors to safe, public error codes/messages; log the original `err` server-side only. |
