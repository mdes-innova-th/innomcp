<!-- cc-team deliverable
 group: G2 (apicontract division)
 member: APIC-027 role=contract model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":1344,"completion_tokens":3239,"total_tokens":4583,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2564,"image_tokens":0},"cache_creation_input_tokens":0} | 61s
 generated: 2026-06-13T12:14:43.906Z -->
| severity | endpoint | issue | fix |
| :--- | :--- | :--- | :--- |
| **High** | `GET /api/mother/config` | **Missing error responses & status-code correctness:** The handler lacks a `try/catch` block. If `getDisabledProviders()`, `isProviderEnabled()`, or `errorRecovery` throws an exception, Express will return a default 500 HTML error or leak stack traces instead of a structured JSON error, breaking the API contract. | Wrap the handler logic in a `try/catch` block. In the `catch`, return a standardized JSON error: `res.status(500).json({ error: "CONFIG_FETCH_FAILED", message: "Unable to retrieve system configuration." })`. |
| **Medium** | `GET /api/mother/config` | **Inconsistent response shapes:** The response uses magic strings like `"(none)"` for empty `envKey` and `"UNKNOWN"` for `circuitState`. This forces clients to handle arbitrary strings instead of standard `null` values or strict enums, complicating client-side typing and validation. | Return `null` instead of `"(none)"` (`envKey: p.envKey || null`). Define a strict TypeScript union/enum for `circuitState` (e.g., `'OPEN' \| 'CLOSED' \| 'HALF_OPEN' \| 'UNKNOWN'`) and ensure the fallback strictly matches it. |
| **Medium** | `GET /api/mother/config` | **Breaking-change risks (Implicit coupling):** The `disabledProviders` field directly passes the output of `getDisabledProviders()`. If the internal service's return type changes (e.g., from `string[]` to `object[]`), the API response shape breaks silently without compiler warnings at the route level. | Map the output to a strictly defined DTO/Interface before returning it (e.g., `disabledProviders: disabled.map(id => ({ id }))`). This decouples the public API contract from internal service implementations. |
| **Medium** | `GET /api/mother/config` | **Breaking-change risks (Hardcoded types):** `featureFlags.motherTimeout` and `minAgents` are hardcoded numeric constants. If these are later migrated to environment variables, they risk being parsed as strings (e.g., `"20000"`), silently breaking the numeric type contract for API consumers. | If migrating to env vars, use explicit type casting (e.g., `Number(process.env.MOTHER_TIMEOUT) || 20000`). Document the exact expected types (integer vs string) in an OpenAPI/Swagger specification. |
| **Low** | `GET /api/mother/config` | **Missing input validation & undocumented params:** The route ignores all query parameters. If filtering (e.g., `?kind=ollama` or `?enabled=true`) is added later without a validation schema, invalid inputs will be silently ignored or cause unhandled crashes. | Implement a schema validator (like `zod` or `joi`) for `req.query`. Explicitly define allowed parameters and return a `400 Bad Request` with a structured error if invalid or undocumented parameters are provided. |
