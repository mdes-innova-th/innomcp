<!-- cc-team deliverable
 group: G2 (apicontract division)
 member: APIC-030 role=contract model=MiniMaxAI/MiniMax-M3
 finish_reason: stop | tokens: {"prompt_tokens":638,"completion_tokens":1681,"total_tokens":2319,"prompt_tokens_details":{"cached_tokens":114,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":0,"image_tokens":0},"cache_creation_input_tokens":0} | 38s
 generated: 2026-06-13T12:15:45.739Z -->
| Severity | Endpoint | Issue | Fix |
|----------|----------|-------|-----|
| **Medium** | `GET /api/mother/history` | `req.query["limit"]` is `unknown`; cast to `string` lies to TypeScript. If the client sends `limit[]=5&limit[]=10`, `parseInt` on the joined array string is undefined behavior. | Define a typed query schema (e.g. `zod`/`express-validator`). Normalize: `const raw = Array.isArray(q.limit) ? q.limit[0] : q.limit;` then `Number(raw)`. Reject with 400 if a `limit` was provided but non-numeric/out-of-range, instead of silently falling back to default. |
| **Low** | `GET /api/mother/history` | Default `limit=10` is applied silently when input is invalid, masking client mistakes and producing inconsistent response shapes (a "bad" request looks identical to a valid one). | Distinguish `400` (explicit bad value) from `200` with default. Document the default in OpenAPI; return `{ runs, total, limit, timestamp }` so callers can echo back what was used. |
| **Medium** | `GET /api/mother/history` | No pagination beyond a single `limit`. For datasets >50, clients have no way to page; this is a breaking-change risk once consumers depend on `runs` being "the last N." | Add `before`/`after` cursor (or `offset`+`limit`) and a `hasMore` boolean. Sort runs by an immutable `runId` or `startedAt` for stable cursors. |
| **Medium** | `GET /api/mother/history` | `runs` typed as `MotherRun[]` but the handler never validates the service return — if `getHistory` throws, Express returns a default HTML 500, breaking the JSON contract. | Wrap in `try/catch`; on error return `res.status(500).json({ error: "internal_error", code: "HISTORY_FETCH_FAILED" })`. Use a shared error middleware so the shape is uniform across routes. |
| **Low** | `GET /api/mother/history` | `total: runs.length` is misleading — it reports the *returned* count, not the true system total. Consumers will treat it as a pagination total. | Either remove `total` or rename to `count`/`returned`, and add a separate `total` field backed by `getHistory().length` independent of `limit`. Document the distinction. |
| **Low** | `GET /api/mother/history` | `timestamp` is a bare ISO string with no `generatedAt`/`asOf` semantics; if a cache is later added downstream the field name becomes ambiguous. | Rename to `generatedAt` and document it as "server time at response generation." |
| **High** | `GET /api/mother/history` | **No auth, but it exposes per-provider dispatch results** — provider names, latencies, success/failure, and (depending on `MotherRun`) potentially request payloads. The JSDoc claim "no auth required (read-only metrics endpoint)" is unsafe for an internal dispatch log. | Add auth (at minimum a service token / mTLS / IP allowlist). If genuinely public, strip `MotherRun` down to metric-only fields (provider, durationMs, status, errorClass) before returning. Mark the route `internal` in OpenAPI. |
| **Medium** | `GET /api/mother/history/:runId` | `runId` validation regex `/^[a-zA-Z0-9_-]{1,128}$/` is good, but the 400 error shape `{ error: "invalid runId" }` is not consistent with the 404 shape (`{ error: "run not found" }`) — both bare strings, no `code`, no `details`. | Standardize error envelope, e.g. `{ error: { code: "INVALID_RUN_ID", message: "...", param: "runId" } }`. Document it once and reuse across the module. |
| **Low** | `GET /api/mother/history/:runId` | No `Content-Type`/`ETag`/caching headers. The single-run response is a perfect candidate for `ETag` based on `runId`+`updatedAt` to avoid refetching immutable history rows. | Add `res.set('ETag', ...)` and handle `If-None-Match` → 304. |
| **Low** | `GET /api/mother/history/:runId` | Route ordering risk: `/` and `/:runId` are fine here, but if a sibling route like `/search` or `/export` is later added *without* re-ordering, Express will match `/:runId` first. | Use a named param (`/runs/:runId`) or stricter path prefixing to leave room. Add an integration test asserting `/search` ≠ `/:runId`. |
| **Medium** | Both routes | **Response shapes differ between the two endpoints** (`{ runs, total, timestamp }` vs `{ run, timestamp }`). No `total` on single-run, plural vs singular key, and no `limit` echo on the list endpoint. | Normalize: `{ data, meta: { timestamp, ... } }` envelope on both, or document the asymmetry as intentional. Pick one and apply consistently — current asymmetry is a breaking-change risk the moment a client SDK is generated. |
| **Medium** | Both routes | No OpenAPI/Swagger spec, JSDoc is the only contract. `limit` range, `runId` charset, error shapes, and status codes are not machine-readable. | Add `@openapi/...` annotations or a sibling `openapi.yaml`. Regenerate client types so the `as string` cast in `req.query["limit"]` disappears. |
| **Low** | Both routes | No `requestId` / correlation ID in responses or logs; debugging a failed run fetch across services is painful. | Middleware: attach `req.id = crypto.randomUUID()`, echo it in responses as `X-Request-Id`, and include it in the error envelope. |
| **Low** | `GET /api/mother/history` | `parseInt(..., 10)` then `Number.isFinite` is correct, but `parseInt("10.5", 10)` silently truncates to `10`. Either reject decimals or floor explicitly. | `const raw = Number(rawStr); if (!Number.isInteger(raw)) return 400;` |
| **Info** | Module-level | No rate limiting on a read endpoint that may scan in-memory history; a bad client can hammer it. | Add `express-rate-limit` keyed by IP/token; return `429` with `Retry-After`. |
| **Info** | Module-level | No `Content-Security-Policy`/`X-Content-Type-Options` set; while the body is JSON, defense-in-depth is cheap. | Apply a shared `helmet()` or per-route `res.set` middleware. |
| **Breaking-change risk** | `GET /api/mother/history` | If `MotherRun` schema gains a new required field, every existing client of `/api/mother/history` breaks at deserialization. | Version the route (`/api/mother/history` → `/v2/...`) or guarantee `MotherRun` is additive-only and document the policy. |

**Top 3 to fix first:**
1. **High** — auth/secret-leakage on the unauthenticated list endpoint.
2. **Medium** — unify error envelope and add input validation via a schema (eliminates the `as string` cast and the silent-default behavior).
3. **Medium** — add cursor pagination + honest `total` semantics, or document the current contract as "last N, capped at 50" to set expectations before consumers bake it in.
