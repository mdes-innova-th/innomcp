<!-- cc-team deliverable
 group: G2 (apicontract division)
 member: APIC-026 role=contract model=deepseek/deepseek-v4-flash
 finish_reason: stop | tokens: {"prompt_tokens":1101,"completion_tokens":2509,"total_tokens":3610,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1807,"image_tokens":0},"cache_creation_input_tokens":0} | 150s
 generated: 2026-06-13T12:15:49.485Z -->
| Severity | Endpoint | Issue | Fix |
|----------|----------|-------|-----|
| **High** | `GET /api/mother/compare/:id1/:id2` | **Composite score calculation bug**: `s.avgLatency || 1000` treats `0` as falsy (realistic low latency) and substitutes 1000, skewing the composite score and potentially the overall winner. | Replace with `(s.avgLatency ?? 1000)` to correctly handle a true zero value. |
| **Medium** | `GET /api/mother/compare/:id1/:id2` | **Inconsistent error handling**: When both provider IDs are missing from stats, returns 404 with `{ error: "Neither provider has stats yet" }`. When only one is missing, silently returns 200 with zero-filled stats for the missing provider, with no indication that the provider is unknown. This is ambiguous and may lead to misinterpretation of zeros. | Either (a) return a 404 if either provider is missing, or (b) add a field per provider (e.g., `hasStats: boolean`) to clearly indicate whether the stats are real or default. |
| **Medium** | `GET /api/mother/compare/:id1/:id2` | **Missing input validation**: The route accepts arbitrary strings for `id1` and `id2` without any format, length, or character validation. While not exploitable in a typical REST context, it increases the attack surface and does not enforce expected identifier patterns. | Add middleware or validation that checks for allowed character sets (e.g., alphanumerics, hyphens, underscores) and reasonable length limits (e.g., 1–64 chars). Return 400 with a descriptive message for invalid input. |
| **Medium** | `GET /api/mother/compare/:id1/:id2` | **Missing error handling for `getProviderStats`**: The function call is not wrapped in a try/catch block. If the underlying service throws an unexpected exception (e.g., database error), the server will not send a proper error response and may crash or expose stack traces. | Wrap the route handler body in a try/catch and return a 500 status with a generic error message. |
| **Low** | `GET /api/mother/compare/:id1/:id2` | **No representation of zero-existence distinction**: A provider that has never made a request is indistinguishable from a provider that does not exist, because both produce all-zero stats. Clients cannot differentiate between a “new” provider and an invalid ID. | Consider storing an explicit “exists” flag or returning `null` for the entire provider when absent, while still returning 200 for a partial comparison (or switch to 404 as suggested above). |
| **Low** | `GET /api/mother/compare/:id1/:id2` | **Risk of breaking change if error behaviour is tightened**: The current API allows one missing provider to succeed (200). Changing this to always return 404 (or 400) when any ID is invalid would break existing clients that rely on the current lenient behaviour. | Document the current contract clearly; if a future change is needed, communicate a deprecation period and consider adding a query flag (e.g., `?strict=true`) to opt in. |
