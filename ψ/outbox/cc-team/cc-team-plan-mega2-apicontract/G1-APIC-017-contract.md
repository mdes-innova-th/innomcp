<!-- cc-team deliverable
 group: G1 (apicontract division)
 member: APIC-017 role=contract model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":2478,"completion_tokens":3866,"total_tokens":6344,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2689,"image_tokens":0},"cache_creation_input_tokens":0} | 51s
 generated: 2026-06-13T12:11:52.306Z -->
| Severity | Endpoint          | Issue                                                                                                                                                                                                                     | Fix                                                                                                                                                                                                                                                                                                |
|----------|-------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| High     | GET /api/files (path listing) | No try/catch around `await listDirectoryEntries()`. Any filesystem error (e.g., path exists but is a file, permission denied) causes an unhandled rejection → Express returns a 500 with stack trace instead of a clean error. | Wrap `listDirectoryEntries` call in try/catch; return `500 { error: "Failed to list directory" }` (or a more specific error like 400/403 if the error is known). Ensure the response body is JSON.                                                                                                  |
| High     | GET /api/files/list            | Same as above — missing error handling for `listDirectoryEntries`.                                                                                                                                                        | Same fix as above.                                                                                                                                                                                                                                                                                 |
| High     | ALL endpoints that read/write files | The `isSafeExtension()` check is defined but never enforced. A client can read (and presumably write/append) files with blocked extensions (`.exe`, `.sh`, etc.). This bypasses the intended sandbox hardening.          | Add validation in every file operation (read, write, append, delete) that checks `isSafeExtension` on the resolved path. Return `400 { error: "Blocked file type" }` if the extension is blocked. If you never intend to enforce it, remove the dead code to avoid audit confusion.          |
| Medium   | GET /api/files (pinned=true)   | Response shape differs from the non‑pinned variant: pinned returns `{ files }` while path listing returns `{ files, root }`. Clients relying on the presence of `root` will break when switching modes.                  | Always include a `root` property (even if empty or `null` for pinned) or adopt a consistent envelope, e.g., `{ success: true, data: { files, root? } }`. Document the expected shape for each query.                                                                                             |
| Medium   | GET /api/files/read            | Returns `404` when the resolved path is a directory (EISDIR). A directory exists but cannot be read as a file — a more appropriate status is `400` (bad request) or `422` (unprocessable).                                 | Before calling `readFile`, `stat` the safe path. If it’s a directory, return `400 { error: "Path is a directory, not a file" }`. Keep `404` for genuinely missing files.                                                                                                                           |
| Medium   | GET /api/files/read            | No validation that the user‑supplied path points to a regular file. Attempting to read a directory currently throws, which is caught and turned into a generic `404`. This obscures the real error.                        | Add a preliminary `stat` check: if the path is not a file, return `400` with a descriptive error. This also prevents attempting to read huge binary files that might consume memory.                                                                                                              |
| Medium   | GET /api/files (path listing)  | When the requested path does not exist, the endpoint silently returns an empty array `[]` with status `200`. This makes it impossible for the client to distinguish between “directory does not exist” and “directory is empty”. | Return `404 { error: "Directory not found" }` when the resolved safe path does not exist. Only return `200` with an empty array if the directory exists but has no entries.                                                                                                                      |
| Low      | GET /api/files/read            | The response includes `path` as the raw user input (`userPath`), while other endpoints (e.g., listing) return a normalised, sanitised relative path. This inconsistency may break clients expecting canonical paths.      | Always return the normalised relative path (using `normalizeRelativePath(userPath)`) in the response under `path`. Keep the raw input for logging if needed, but not in the public field.                                                                                                        |
| Low      | GET /api/files (root)          | The `pinned` query parameter is parsed with `String(req.query.pinned || "").toLowerCase() === "true"`. Any truthy-looking value like `"1"`, `"yes"` is ignored. This is unintuitive for a boolean flag.                    | Use a robust boolean parser like `req.query.pinned === "true"` or accept common truthy strings (`"1"`, `"yes"`). Or be explicit in documentation that only `?pinned=true` (exact string) works.                                                                                                  |
| Low      | GET /api/files/read            | All read errors (ENOENT, EACCES, EISDIR) are collapsed into a single `404` with no detail. A client cannot programmatically act on a permission problem versus a missing file.                                           | Inspect the error code (`err.code`) and return `403 { error: "Permission denied" }` for `EACCES`, `404 { error: "File not found" }` for `ENOENT`, etc. Include the file path in the error body for easier debugging.                                                                              |
| Low      | GET /api/files/list            | The `/list` endpoint duplicates the functionality of `GET /api/files?path=…`. This creates maintenance burden and potential for drift.                                                                                    | Deprecate `/list` and redirect clients to the canonical root endpoint with `path` query param, or at least ensure both endpoints share the exact same code and documentation.                                                                                                                     |
