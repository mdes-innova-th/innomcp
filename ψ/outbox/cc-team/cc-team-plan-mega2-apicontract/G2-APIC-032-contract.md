<!-- cc-team deliverable
 group: G2 (apicontract division)
 member: APIC-032 role=contract model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":573,"completion_tokens":3863,"total_tokens":4436,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":3138,"image_tokens":0},"cache_creation_input_tokens":0} | 71s
 generated: 2026-06-13T12:16:53.844Z -->
| severity | endpoint | issue | fix |
|---|---|---|---|
| High | GET /api/mother/intent-leaders | **Missing error responses & inconsistent error shape**: No `try/catch` around `getIntentWinsSnapshot()`. If the data source fails, Express returns a default unhandled exception response (often HTML or generic JSON), which is inconsistent with the structured success response and may leak stack traces. | Wrap the handler logic in a `try/catch` block. Return a standardized JSON error response (e.g., `{ error: { code: "INTERNAL_ERROR", message: "Failed to fetch metrics" } }`) with a `500` status code. |
| Medium | GET /api/mother/intent-leaders | **Breaking-change risk (Unstable sorting)**: `leaders.sort((a, b) => b.wins - a.wins)` is unstable when wins are tied. Map iteration order dictates the tie-breaker, causing the "leader" to randomly flap between tied providers across requests, breaking client-side caching and UI stability. | Add a deterministic secondary sort key. E.g., `leaders.sort((a, b) => b.wins - a.wins \|\| a.intent.localeCompare(b.intent));` to ensure consistent ordering. |
| Medium | GET /api/mother/intent-leaders | **Status-code correctness**: The route implicitly returns `200 OK` in all success cases. There is no mechanism to return `503 Service Unavailable` if the underlying metrics service is degraded, disconnected, or returning critically stale/empty data. | Check the health/freshness of the snapshot. If the data source is down or critically stale, explicitly return `res.status(503).json({ error: { code: "SERVICE_UNAVAILABLE" } })`. |
| Low | GET /api/mother/intent-leaders | **Missing input validation & undocumented params**: The route silently ignores all query parameters (e.g., `?intent=code`, `?limit=10`). This causes client confusion and CDN cache-poisoning (caching different query strings as the exact same response). | Implement query parameter validation using a schema library (like `zod`). Either support filtering/pagination or explicitly reject unknown query parameters with a `400 Bad Request`. |
| Low | GET /api/mother/intent-leaders | **Inconsistent response shape (Redundancy)**: The `intents` array is strictly derived from `leaders.map(l => l.intent)`. This redundancy increases payload size and creates a risk of future inconsistency if one field is modified or deprecated without the other. | Remove the `intents` array from the response to enforce a single source of truth, or explicitly document it as a deprecated convenience field to prepare clients for future removal. |
| Low | GET /api/mother/intent-leaders | **Breaking-change risk (Hardcoded fallback)**: `INTENT_LABELS` is hardcoded. New intents fall back to the raw string (`?? intent`). If a future developer "fixes" this by throwing an error or returning `null` for unknown labels, it will break the API contract for existing clients. | Move `INTENT_LABELS` to a shared config/database. Explicitly document in the API contract that `intentLabel` will fall back to the raw `intent` string for unmapped types, preventing future "fixes" from breaking the contract. |
