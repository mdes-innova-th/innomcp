<!-- cc-team deliverable
 group: G1 (apicontract division)
 member: APIC-021 role=contract model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":1690,"completion_tokens":2918,"total_tokens":4608,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2178,"image_tokens":0},"cache_creation_input_tokens":0} | 41s
 generated: 2026-06-13T12:12:40.020Z -->
```plaintext
| Severity | Endpoint             | Issue                                                                                                                     | Fix                                                                                                                                                             |
|----------|----------------------|---------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Critical | DELETE /api/memories/:id | No ownership or access check allows any caller (even unauthenticated) to delete any memory by ID.                        | Add a check that the memory belongs to the authenticated user (or session/project associated with the user). Return 404 or 403 if not permitted.              |
| High     | GET / and GET /search  | Database or unexpected errors are silently swallowed; the endpoint returns 200 with an empty array.                      | Return 500 with a meaningful error message when an unhandled exception occurs. Do not mask errors as normal responses.                                         |
| High     | All endpoints        | `ensureProjectAccess` and its callers treat all exceptions as “access denied” (403). A database failure incorrectly returns 403 instead of 500. | In `ensureProjectAccess`, throw on unexpected errors instead of returning false. In callers, catch throwables separately from the access-denied case and return 500. |
| Medium   | GET / and GET /search | Response shapes are inconsistent: `GET /` returns snake_case fields and no `tag`; `GET /search` returns camelCase aliases and includes `tag`. | Unify the response format (prefer camelCase) and consistently include or exclude `tag` across both endpoints.                                                 |
| Medium   | DELETE /api/memories/:id | No input validation on `:id`. Non-numeric or negative values may cause SQL errors or unexpected behavior.               | Validate that `req.params.id` is a positive integer; return 400 if invalid.                                                                                    |
| Medium   | DELETE /api/memories/:id | Always returns `{"success":true}` even if the ID does not exist, misleading the client.                                 | Check the number of affected rows from the DELETE query; if zero, return 404 with an appropriate message.                                                      |
| Medium   | All endpoints        | Success response envelopes differ: GET endpoints return `{"memories":[...]}`, POST/DELETE return `{"success":true}`.    | Standardize the response shape, e.g., always use `{"data":...}` or include `success` consistently.                                                              |
| Low      | POST /api/memories    | Scope, keyName length, value size, and allowed values for `scope` are not validated.                                    | Add validation: scope must be one of predefined values, keyName ≤ 255 chars, value ≤ a reasonable limit. Return 400 for violations.                            |
| Low      | GET /, /search, POST  | Query parameter `scope`, `sessionId`, `projectId` lack format/enum validation. Large or malformed values may cause backend issues. | Validate scope against allowed enum, ensure sessionId/uuid format, and verify projectId is numeric.                                                             |
| Low      | GET /search           | Undocumented parameter `tag` is returned in the search response but is not mentioned in the route comment or documentation. | Document all response fields, and decide whether `tag` should also appear in the list response for consistency.                                                |
| Low      | All listing endpoints | Hardcoded LIMITs (50/20) may change later and break clients that depend on the current limit.                            | Consider adding optional pagination parameters (`limit`, `offset`) to avoid breaking changes, and document the default limits.                                  |
```
