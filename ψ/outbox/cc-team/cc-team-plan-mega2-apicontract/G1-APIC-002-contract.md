<!-- cc-team deliverable
 group: G1 (apicontract division)
 member: APIC-002 role=contract model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":1780,"completion_tokens":5859,"total_tokens":7639,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":5010,"image_tokens":0},"cache_creation_input_tokens":0} | 108s
 generated: 2026-06-13T12:09:50.593Z -->
| severity | endpoint | issue | fix |
|---|---|---|---|
| High | `GET /users` | **Breaking-change risk / Schema exposure**: Returns raw database rows (`user_id`, `user_dispname`, etc.). Renaming DB columns will break API clients. | Map database rows to a consistent DTO (e.g., `id`, `displayName`, `email`, `roleId`, `isActive`, `createdAt`) before returning. |
| Medium | `GET /users` | **Missing input validation / DoS risk**: No pagination or filtering. Fetching all users at once can cause memory exhaustion and slow responses as the user base grows. | Add `limit`, `offset`, and optional filter query parameters. Update the response shape to match the paginated structure used in `/audit-log`. |
| Medium | `PATCH /users/:id/role` | **Missing input validation**: Only checks if `roleId` is a number. Allows negative numbers or non-existent role IDs, which could violate FK constraints (causing 500) or assign invalid roles. | Validate `roleId` against a predefined list of valid role IDs or check its existence in the DB. Return 400 Bad Request if invalid. |
| Medium | `GET /audit-log` | **Missing input validation**: Flawed parsing for `limit` and `offset`. Negative values (e.g., `?limit=-5`) bypass the `\|\| 20` fallback and are passed to SQL, potentially causing DB errors. | Explicitly validate that `limit > 0` and `offset >= 0`. Return 400 Bad Request for invalid pagination parameters instead of passing negatives to the DB. |
| Medium | All Endpoints | **Inconsistent response shapes**: `GET /users` uses `{ data }`, `GET /audit-log` uses `{ entries, total, hasMore }`, and PATCH routes use `{ message }`. | Standardize response envelopes. Use `{ data: { items, total, hasMore } }` for paginated lists, `{ data: ... }` for single items, and `{ message }` for mutations. |
| Medium | `PATCH /users/:id/role`<br>`PATCH /users/:id/active` | **Status-code correctness / Side-effect leak**: If `logAdminAction` throws, the client receives a 500 error, but the user's role/active status was already successfully updated in the DB. | Wrap `logAdminAction` in its own try/catch block, or execute it asynchronously without awaiting, ensuring the main update response is 200 OK even if audit logging fails. |
| Low | `PATCH /users/:id/role`<br>`PATCH /users/:id/active` | **Missing input validation**: `targetId` bounds are not checked. `parseInt` allows negative numbers or zero, resulting in a misleading 404 instead of a proper 400 validation error. | Add a check to ensure `targetId > 0` after parsing. Return 400 Bad Request if the ID is zero or negative. |
| Low | `PATCH /users/:id/role`<br>`PATCH /users/:id/active` | **Missing error responses / Silent failure**: Checks `if (req.user)` before audit logging. If middleware fails to attach the user, the action succeeds but isn't audited. | Since auth middleware guarantees `req.user` exists, remove the `if (req.user)` check and access `req.user.userId` directly. Let it throw (500) if missing. |
| Low | `GET /audit-log` | **Inconsistent response shapes**: Uses `entries` instead of `items` or `data`, and mixes `targetId` with `adminUserId` in the mapped response. | Standardize pagination response to use `data: { items: [...] }` and ensure consistent naming (e.g., `targetUserId` instead of `targetId` to match `adminUserId`). |
