<!-- cc-team deliverable
 group: G2 (apicontract division)
 member: APIC-035 role=contract model=MiniMaxAI/MiniMax-M3
 finish_reason: stop | tokens: {"prompt_tokens":1178,"completion_tokens":2269,"total_tokens":3447,"prompt_tokens_details":{"cached_tokens":114,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":0,"image_tokens":0},"cache_creation_input_tokens":0} | 55s
 generated: 2026-06-13T12:17:17.005Z -->
| # | Severity | Endpoint | Issue | Fix |
|---|----------|----------|-------|-----|
| 1 | **Critical** | `GET /api/mother/rankings` | No try/catch around `getProviderStats()` — if the service throws (DB read failure, internal error), the request 500s with Express's default HTML error, breaking the JSON contract consumers expect. | Wrap the handler body in `try/catch`; on error return `res.status(500).json({ error: "internal_error", message: "Failed to compute rankings", timestamp: new Date().toISOString() })`. Optionally log via a structured logger. |
| 2 | **High** | `GET /api/mother/rankings` | No error responses are ever returned. The empty-state path returns `200` with `rankings: []` — there is no documented way for a client to distinguish "no data yet" from "service healthy". No `4xx`/`5xx` paths exist at all. | Define an explicit error response shape (e.g. `{ error: string, message: string, timestamp: string }`) and document it. Return `503` when upstream stats service is unavailable and `500` for unexpected errors. Keep `200` + empty array only for legitimate "no providers with requests yet" state. |
| 3 | **High** | `GET /api/mother/rankings` | Response shape is inconsistent between the no-data branch (`{ rankings, totalRanked, timestamp }`) and the populated branch — but the populated branch silently omits any pagination/limit metadata while the docstring advertises "1–14 providers". Once a 15th provider is added the shape is fine but unbounded. | Add a `limit` query parameter (default e.g. 14, max e.g. 100) and a `totalProviders` (unfiltered count) field to the response so clients can detect truncation. Validate `limit` is a positive integer; reject with `400` otherwise. |
| 4 | **High** | `GET /api/mother/rankings` | Breaking-change risk: the `RankingEntry` shape has 15 fields today. The composite-weighting logic is server-side, but `speedScore`/`reliabilityScore`/etc. are also returned. Any future change to weights silently reorders providers — clients that cache or store `rank` will see reordered results with no API-version signal. | Add a `schemaVersion: 1` field to the response and an `X-API-Version` header. Document weights in the response (e.g. `weights: { speed: 0.25, reliability: 0.30, ... }`) so clients can detect and react to formula changes. |
| 5 | **High** | `GET /api/mother/rankings` | Status-code correctness: a synchronous exception produces `500` (default) but with wrong content type; the empty-state returns `200` instead of `200` with an explicit `Cache-Control` header; there is no `ETag`/`Last-Modified` so clients cannot poll efficiently and will hammer the route. | Set `Cache-Control: public, max-age=30` (or similar) and emit an `ETag` based on `timestamp` so polling clients get `304` when unchanged. Return `200` for the empty state with a `degraded: true` flag if appropriate. |
| 6 | **Medium** | `GET /api/mother/rankings` | Undocumented query parameters: route accepts `?_req` (Express parses anything) but the docstring/JSDoc lists zero query params, while the route has no declared inputs at all. No `?limit`, `?tier`, `?sort` is supported, and yet consumers may assume them from the response field names. | Add an `@openapi`/JSDoc block documenting `?limit` (int, 1–100, default 14), `?minRequests` (int, default 1), and `?metric` (enum: `composite|speed|reliability|popularity|wins`, default `composite`). Reject unknown params with `400`. |
| 7 | **Medium** | `GET /api/mother/rankings` | Missing input validation on the response-building pipeline. `s.p95Latency` and `s.avgLatency` are trusted blindly — if upstream returns `NaN`, `Infinity`, or negative numbers, `speedScore` becomes `NaN`, which serializes to `null` in JSON and silently corrupts rankings. `s.successRate` > 100 or < 0 would also produce out-of-range scores. | Sanitize at the boundary: `const safeNum = (n, fallback=0) => Number.isFinite(n) ? n : fallback;` and clamp all scores to `[0, 100]`. If upstream data is malformed, log and return `502`/`503` rather than shipping `null` scores. |
| 8 | **Medium** | `GET /api/mother/rankings` | `tier` is computed from raw array index (`i < 3 ? "gold" : ...`) which ties tiering to *returned* rank, not *true* rank. If a `limit=5` is later added, providers ranked 4–7 in the full set will be mislabeled `gold`. | Compute tier from `e.rank` after ranking the full set, then apply pagination. Alternatively, return `tier` only when the full 14 are present. |
| 9 | **Medium** | `GET /api/mother/rankings` | No `Content-Type` charset, no `X-Request-Id`, no correlation id, and `timestamp` is a server-rendered `new Date().toISOString()` per-request — clients cannot reason about cache windows. | Add `res.setHeader("X-Request-Id", crypto.randomUUID())` and surface a `generatedAt` distinct from any upstream `timestamp`; document both. |
| 10 | **Medium** | `GET /api/mother/rankings` | `qualityScore` is `Math.round(s.avgQuality \|\| 0)` — `avgQuality` of exactly `0` is treated identically to missing, which could mis-trigger the `hasQuality` branch and shift the composite weighting for a provider whose true quality is genuinely 0. | Use `s.avgQuality != null` (explicit null check) or a sentinel like `undefined`/`null` from upstream to distinguish "missing" from "zero". |
| 11 | **Medium** | `GET /api/mother/rankings` | No rate limiting / abuse protection on this route. Because it iterates `getProviderStats()` and sorts in memory, a flood of requests can DoS the process. | Apply `express-rate-limit` middleware (e.g. 60 req/min/IP) and consider caching the result for 5–30s in-process. |
| 12 | **Low** | `GET /api/mother/rankings` | Response field `totalRanked` counts providers with `requests > 0`, not providers in the system — same name as the docstring's "ranked 1-14 (or fewer if no stats)" suggests. Confusing for clients wanting provider-count metadata. | Rename to `rankedProviders` (filtered) and add `totalProviders` (unfiltered) to disambiguate. |
| 13 | **Low** | `GET /api/mother/rankings` | Tie-breaking is non-deterministic: when two providers share `compositeScore`, `Array.prototype.sort` order is stable in modern V8 but undocumented in the contract, so client tests against exact rank order will flake across runtimes. | Add a deterministic secondary sort (e.g. `providerId` ascending) and document the sort key. |
| 14 | **Low** | `GET /api/mother/rankings` | Docstring claims 14 providers but the code handles any number — the hardcoded tier thresholds (`<3`, `<7`, `<10`) implicitly assume exactly 14. With 12 providers, rank 11–12 still get `"none"`; with 20, ranks 11–20 also get `"none"`. The tier meaning changes silently with dataset size. | Either: (a) make tier thresholds data-driven (e.g. top 20% gold, next 30% silver, next 25% bronze, rest none), or (b) document tier thresholds as absolute rank cutoffs and reject datasets that don't match. |
| 15 | **Low** | `GET /api/mother/rankings` | `efficiencyScore` and `healthScore` are surfaced on the response but never influence ranking or tier — a client may reasonably assume they do, given they're in the same object as `compositeScore`. | Either include them in the composite formula with documented weight, or move them under a separate `extras` sub-object to make their informational-only role explicit. |
| 16 | **Low** | `GET /api/mother/rankings` | The `Router` is exported as default with no `mergeParams`, no `strict` mode, and no path-level middleware (e.g. auth, request logging). Whether that is intentional is undocumented. | Add a JSDoc note on the export describing required upstream middleware (auth, request-id, rate-limit) and consider composing with a `withApiContract(router)` helper that injects them. |
| 17 | **Info** | `GET /api/mother/rankings` | No OpenAPI / JSON Schema artifact is referenced. Consumers must hand-derive the contract from the TypeScript interface, which (a) can drift from runtime and (b) doesn't capture the empty-state shape or error shape. | Generate or hand-author an OpenAPI 3.1 component for this route and reference it in the JSDoc; export the `RankingEntry` and error schemas as runtime Zod/TypeBox validators and use them in the handler. |

**Summary of highest-priority fixes:**

1. **Wrap the handler in try/catch** and return a documented JSON error envelope (covers #1, #2).
2. **Validate and clamp numeric inputs** from `getProviderStats()` before scoring (covers #7, #10).
3. **Decouple tier computation from returned rank** and add `limit`/`minRequests`/`metric` query params with input validation (covers #3, #6, #8).
4. **Add a `schemaVersion` field, deterministic tie-breaking, and an ETag/`Cache-Control`** to make the contract robust to future formula changes (covers #4, #5, #13).
