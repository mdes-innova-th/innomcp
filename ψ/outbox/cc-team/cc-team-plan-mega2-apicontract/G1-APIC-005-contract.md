<!-- cc-team deliverable
 group: G1 (apicontract division)
 member: APIC-005 role=contract model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":722,"completion_tokens":3564,"total_tokens":4286,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":2953,"image_tokens":0},"cache_creation_input_tokens":0} | 49s
 generated: 2026-06-13T12:08:19.821Z -->
| Severity | Endpoint               | Issue                                                                                                                                                                                                 | Fix                                                                                                                                                                                                                                                                                           |
|----------|------------------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| High     | POST /api/agent/stream | `projectId` is accepted in the request body but never passed to `runAgentLoop` or used anywhere, making it a misleading no‑op parameter.                                                            | Either pass `projectId` to the agent loop / tool registry if it should influence execution, or remove `projectId` from the `StreamRequestBody` interface entirely.                                                                                                                           |
| High     | POST /api/agent/stream | The SSE event structure (`AgentEvent`) is not versioned. Any change to the `AgentEvent` shape (fields added, removed, or renamed) is a silent breaking change for all clients consuming the stream. | Introduce API versioning – e.g., prefix the URL with `/v1/` or negotiate a media type version. Alternatively, emit a `version` event at stream start so clients can adapt.                                                                                                                  |
| High     | POST /api/agent/stream | No public documentation exists for the request body fields, the SSE event format, error responses, or the meaning of `projectId` – all parameters are effectively undocumented.                     | Provide an OpenAPI specification or thorough JSDoc comments that describe every accepted field, all possible response shapes (200 SSE stream, 400 JSON error), and the structure of each event type (data, done, error).                                                                       |
| Medium   | POST /api/agent/stream | `task` validation allows whitespace‑only strings (e.g. `"   "`), which pass the truthiness check and may result in an LLM call with empty content.                                                    | Validate `task` with `typeof task === 'string' && task.trim().length > 0` and return 400 if it fails.                                                                                                                                                                                         |
| Medium   | POST /api/agent/stream | No type validation is performed on `projectId` when present. A client could send a number, object, or array, leading to unexpected runtime errors if the code later treats it as a string.           | Add a check: `if (projectId !== undefined && typeof projectId !== 'string') { res.status(400).json({ error: 'projectId must be a string' }); return; }`                                                                                                                                      |
| Low      | POST /api/agent/stream | Inconsistent error response shapes: pre‑stream validation errors return a JSON body with HTTP 400, while in‑stream errors are transmitted as `event: error` SSE events (HTTP 200).                     | Accept this standard pattern but document it clearly: “Validation errors respond with 400 + JSON; errors during streaming are delivered as an `error` SSE event.” If strict uniformity is required, convert all errors to SSE by establishing the stream first (at cost of HTTP status 200).  |
