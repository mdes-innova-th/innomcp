<!-- cc-team deliverable
 group: G1 (apicontract division)
 member: APIC-001 role=contract model=deepseek/deepseek-v4-pro
 finish_reason: stop | tokens: {"prompt_tokens":2342,"completion_tokens":2020,"total_tokens":4362,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1624,"image_tokens":0},"cache_creation_input_tokens":0} | 32s
 generated: 2026-06-13T12:07:12.922Z -->
| Severity | Endpoint         | Issue                                                                                     | Fix                                                                                                                                                                |
|----------|------------------|-------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| High     | GET /api/activity | Missing error handler — no `catch` block visible; unhandled promise rejections leave requests hanging or cause default Express 500 without proper logging. | Add a `catch` block that logs the error and sends a structured 500 response (`{ error: 'Internal server error' }`). Ensure all code paths return a response. |
| Medium   | GET /api/activity | Schema‑altering side‑effects on each request (`ensureProjectsTable`, `ensureTaskProjectColumn`) run DDL under traffic, risking performance degradation and deadlocks. | Move table/column checks to a startup migration script (or run once on app initialisation). Use a `schemaVersion` guard or a standalone migration tool.            |
| Low      | GET /api/activity | Undocumented query‑parameter aliases `project_id` and `user_id` are accepted but not mentioned in the route documentation.                                 | Either remove the undocumented aliases or explicitly add them to the documentation and keep them for backward compatibility.                                    |
| Low      | GET /api/activity | No input validation on `projectId` / `userId` parameters — they accept arbitrary strings, which could be extremely long or malformed.                 | Add validation: enforce max length (e.g., 36 chars for UUIDs if that is the expected format), reject unexpected characters, or use a sanitizer.                   |
| Low      | GET /api/activity | `ActivityItem` interface declares a `message_sent` type that is never produced by the handler, misleading API consumers and documentation.             | Either remove the `message_sent` type from the interface until it is implemented, or implement the corresponding query and include it in the union.              |
