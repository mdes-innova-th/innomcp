<!-- cc-team deliverable
 group: G1 (apicontract division)
 member: APIC-022 role=contract model=Qwen/Qwen3.7-Max
 finish_reason: stop | tokens: {"prompt_tokens":1447,"completion_tokens":5243,"total_tokens":6690,"prompt_tokens_details":{"cached_tokens":0,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":4434,"image_tokens":0},"cache_creation_input_tokens":0} | 97s
 generated: 2026-06-13T12:13:55.644Z -->
| severity | endpoint | issue | fix |
|---|---|---|---|
| Critical | `GET /advanced` | **Route Shadowing**: Defined *after* `GET /:name`. Express will intercept requests to `/advanced` using the `/:name` route (with `name="advanced"`), making this endpoint completely unreachable. | Move the `GET /advanced` route definition **above** `GET /:name` and `DELETE /:name`. Literal paths must always precede parameterized paths in Express. |
| High | `GET /performance` | **Missing Error Handling**: Lacks a `try/catch` block. If `getMetrics()` or `getSlowRoutes()` throws, it results in an unhandled exception, potentially crashing the request or leaking stack traces via Express's default error handler. | Wrap the handler logic in a `try/catch` block and return a standardized `500` JSON error response. |
| High | `GET /advanced` | **Missing Input Validation (DoS Risk)**: The `days` query parameter lacks bounds checking. Passing a massive value (e.g., `days=999999`) or a negative number could trigger an expensive downstream DB/Redis query, causing OOM or timeouts. | Validate `days` to ensure it falls within a safe range (e.g., `Math.min(Math.max(days, 1), 30)`). Return `400 Bad Request` for out-of-bounds or non-numeric inputs. |
| Medium | All Endpoints | **Inconsistent Response Shapes**: Timestamp keys vary (`timestamp` vs `generatedAt`), and some endpoints (`/summary/overview`, `/advanced`) omit timestamps entirely. Top-level response structures are also inconsistent. | Standardize the response envelope (e.g., `{ data: ..., meta: { timestamp: ... } }`) or consistently use `timestamp: new Date().toISOString()` at the root of all successful JSON responses. |
| Medium | `GET /:name`, `DELETE /:name` | **Missing Input Validation**: The `name` path parameter is not validated. Malformed, excessively long, or special-character strings could cause downstream storage/lookup issues (e.g., Redis key injection, path traversal, or DoS). | Validate `name` against a strict regex (e.g., `/^[a-zA-Z0-9_:-]+$/`) and enforce a maximum length. Return `400 Bad Request` if invalid. |
| Low | `GET /performance` | **Synchronous `require` in Handler**: Uses `require()` inside the request handler. This bypasses TypeScript type checking, incurs file-system I/O on the first request, and can cause runtime crashes if the module path is incorrect. | Move the `import { getMetrics, getSlowRoutes }` to the top of the file alongside the other ES6 imports. |
| Low | `DELETE /:name` | **Status-Code Correctness**: Returns `200 OK` with a JSON body. While acceptable in some APIs, `204 No Content` is the standard RESTful status code for a successful DELETE operation that doesn't need to return a representation. | Change response to `res.status(204).send()`, or document the 200 behavior in the OpenAPI/Swagger spec if the API strictly mandates JSON bodies for all responses. |
| Low | `GET /summary/overview` | **Hardcoded Magic Numbers**: The health score calculation (`100 - (slowCount * 10)`) and slow threshold (`p95 > 2000`) are hardcoded. This makes tuning difficult and risks breaking changes if business logic shifts. | Extract magic numbers into configuration constants or environment variables (e.g., `config.metrics.slowThresholdMs`). |
