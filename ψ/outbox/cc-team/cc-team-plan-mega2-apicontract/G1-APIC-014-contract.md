<!-- cc-team deliverable
 group: G1 (apicontract division)
 member: APIC-014 role=contract model=zai-org/GLM-5.1
 finish_reason: stop | tokens: {"prompt_tokens":1041,"completion_tokens":1968,"total_tokens":3009,"prompt_tokens_details":{"cached_tokens":32,"audio_tokens":0,"video_tokens":0},"completion_tokens_details":{"reasoning_tokens":1127,"reasoning_tokens_estimated":true,"image_tokens":0},"cache_creation_input_tokens":0} | 23s
 generated: 2026-06-13T12:10:29.415Z -->
| severity | endpoint | issue | fix |
|---|---|---|---|
| **Critical** | GET /api/dashboard | `catch` block returns HTTP 200 with zeroed data instead of 500 — silently masks DB failures, makes debugging impossible, clients can't distinguish real empty state from errors | Return `res.status(500).json({ error: "Failed to load dashboard" })` or a structured error envelope |
| **Critical** | GET /api/dashboard | `ensureTasksProjectColumn` runs DDL (`ALTER TABLE … ADD COLUMN`, `CREATE INDEX`) inside a read endpoint with a non-atomic module-level flag — concurrent requests race to alter the table; swallowed errors hide real failures | Remove DDL from the read path. Run migrations in a separate startup/migration step. If a runtime check is needed, use `SELECT COLUMN_NAME FROM INFORMATION_SCHEMA` instead |
| **High** | GET /api/dashboard | `projectId` query param has no input validation — accepts arbitrary strings (SQL relies on parameterisation but no length/type/format check). No 400 response for invalid values | Validate `projectId` as UUID v4 format; return `400 Bad Request` with `{ error: "Invalid projectId" }` if it fails |
| **High** | GET /api/dashboard | `avgRating` type is `string \| null` (`toFixed(1)` returns string) while all other numeric stats are `number` — breaks typed clients expecting a consistent numeric type | Use `Math.round(Number(feedbackRow.avg_rating) * 10) / 10` to return `number \| null` |
| **High** | GET /api/dashboard | Two undocumented param names (`projectId` and `project_id`) accepted for the same value — clients may depend on either; removing one is a breaking change | Pick one canonical name (e.g. `projectId`), document it, and reject the other with 400. Add OpenAPI/JSON Schema spec |
| **Medium** | GET /api/dashboard | `feedbackRows` and `shellRows` queries are global (no project/user filter) while `taskStats`/`recentTasks` are scoped — response mixes scoped and unscoped data, misleading consumers | Add `WHERE project_id = ? AND user_id = ?` (or equivalent) to feedback and shell queries, or explicitly document them as global and separate them in the response |
| **Medium** | GET /api/dashboard | `recentTasks` returns raw DB rows (`id, title, intent, status, elapsed_ms, created_at, project_id`) — exposes internal schema; any column rename is a breaking change | Map to a defined DTO: `{ id, title, intent, status, elapsedMs, createdAt, projectId }` with explicit field selection and camelCase |
| **Medium** | GET /api/dashboard | Success and error response shapes differ in semantics — success returns real data, error returns zeroed data with no `error` field; clients cannot programmatically detect failures | Adopt a consistent envelope: `{ ok: boolean, data?: T, error?: { code, message } }` or at minimum add an `error` field on failure |
| **Medium** | GET /api/dashboard | `resolveDashboardUserId` returns `null` when `req.user` is missing, but `authenticateToken` middleware should already reject unauthenticated requests — the null path is dead code that silently disables ownership filtering if middleware is bypassed | Remove the null path; throw or return 401 if `req.user` is absent, or assert its presence |
| **Low** | GET /api/dashboard | `tasksProjectColumnEnsured` is a module-level mutable flag — it resets on process restart, causing redundant ALTER attempts; it's also not thread-safe under Node worker threads | Move migration to a dedicated startup script. If a runtime guard is kept, use `SELECT … INFORMATION_SCHEMA.COLUMNS` to check existence instead of try/catch on DDL |
| **Low** | GET /api/dashboard | `LIMIT 8` for recent tasks is a magic number with no pagination support — clients can't request more/fewer, and changing the limit is a silent breaking change | Accept `?limit=N` (validated, capped at e.g. 50) with a default of 8; document it |
| **Low** | GET /api/dashboard | No `Cache-Control` or `ETag` headers — repeated polling hits the DB every time for data that changes infrequently | Add `Cache-Control: max-age=30` or `ETag`/`Last-Modified` support |
