// Placeholder for dbNotifyDashb functionality
export const dbNotifyDashb = {
  addClient: (id: string, ws: any, type: string) => {
    console.log(`Client ${id} added for type ${type}`);
  },
  removeClient: (id: string) => {
    console.log(`Client ${id} removed`);
  },
};